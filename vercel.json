{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.3 --activate && pnpm --filter web build",
  "devCommand": "cd apps/web && pnpm dev",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.3 --activate && pnpm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "ignoreCommand": "exit 1",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}

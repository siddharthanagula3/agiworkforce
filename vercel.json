{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.3 --activate && pnpm --filter @agiworkforce/web build",
  "devCommand": "pnpm --filter @agiworkforce/web dev",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.3 --activate && pnpm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}

/**
 * Expo route wrapper — `/(app)/settings/permissions`
 *
 * Real implementation at apps/mobile/src/features/settings/permissions/index.tsx.
 * Per the settings cluster convention (see notifications.tsx for canonical commentary):
 * do NOT add logic here.
 */
export { default } from '@/src/features/settings/permissions';

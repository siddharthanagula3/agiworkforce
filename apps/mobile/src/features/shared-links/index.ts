/**
 * shared-links — the conversations this account has published as read-only
 * links, and the ability to revoke them.
 */
export { fetchSharedLinks, revokeSharedLink, type SharedLink } from './service';

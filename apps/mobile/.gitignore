
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Local Expo state — machine-specific, do not commit
.expo/

# Expo prebuild artifacts — canonical iOS dir is at repo-root /ios, not apps/mobile/ios
# Running `expo prebuild` regenerates these locally; do not commit (would diverge from top-level)
/android/
/ios/

# Screenshot pipeline output — raw captures/artifacts uploaded directly to
# App Store Connect / Play Console, not committed as source.
store-listing/screenshots/captures/
store-listing/screenshots/.detox-artifacts/

# e2e run output — Detox writes to artifacts/mobile-e2e (test:e2e:ios:ci) and a
# runner invoked from this directory drops captures at the app root. Anchored so
# committed source images under assets/ and docs/qa/screenshots/ keep tracking.
/*.png
/*.jpg
/*.jpeg
/artifacts/

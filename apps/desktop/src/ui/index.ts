/**
 * ui/ — primitive React components (design system)
 *
 * This is the target location for shadcn/radix primitives
 * currently living at src/components/ui/.
 *
 * Phase 5 skeleton. Primitives migrated progressively from
 * src/components/ui/ to here. During migration the old path
 * is retained as a barrel re-export so callers are unaffected.
 *
 * When migration is complete, src/components/ui/ becomes:
 *   export * from '../../ui';
 */

export {};

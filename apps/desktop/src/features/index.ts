/**
 * features/ — feature modules (chat, models, sync, auth, billing, etc.)
 *
 * Each subdirectory is a self-contained feature with:
 *   index.ts — public barrel export
 *   components/ — feature-local React components
 *   hooks/ — feature-local hooks
 *   store.ts — feature-local Zustand slice (if needed)
 *
 * Phase 5 skeleton. Features migrated progressively from src/components/.
 * Active features: see subdirectory list below.
 *
 * Avoid circular imports: features may import from src/ui, src/data,
 * src/platform, src/core — but NOT from each other's internals.
 */

export {};

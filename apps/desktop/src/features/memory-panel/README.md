# Desktop Memory Panel Feature

Status: Current
Owner: Desktop surface lead
Purpose: Own the full-height Desktop memory panel used by the chat layout for browsing, adding, filtering, and deleting remembered user context.

## Boundaries

- Keep the chat-layout memory panel entry point in this folder until the two Desktop memory panel variants are intentionally consolidated.
- Keep reusable memory management controls in `apps/desktop/src/features/memory`.
- Import shared UI primitives from `@/components/ui/*`.

/**
 * core/ — state orchestration and app shell
 *
 * This directory holds the React-side app core:
 * - App shell components and layout primitives
 * - Global state orchestration
 * - Root-level context providers
 * - App lifecycle management
 *
 * Phase 5 skeleton. Features migrated here progressively
 * from src/components/, src/stores/, and src/providers/.
 */

export {};

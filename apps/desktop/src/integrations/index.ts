/**
 * integrations/ — external service clients
 *
 * Supabase, Stripe, provider API clients, MCP clients.
 * These are singleton or lazy-initialized service objects,
 * not React components or Zustand stores.
 *
 * Phase 5 skeleton. Client code migrated progressively
 * from src/services/ and src/api/.
 */

export {};

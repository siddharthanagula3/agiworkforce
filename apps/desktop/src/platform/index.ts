/**
 * platform/ — Tauri invoke wrappers, IPC bridges, native event handlers
 *
 * This directory holds the React-side platform layer:
 * - Tauri invoke() wrappers (typed, per-feature)
 * - IPC bridge adapters
 * - Native event bindings (tauri::event)
 * - Platform capability detection
 *
 * Phase 5 skeleton. Files migrated here progressively
 * from src/api/, src/runtime/, and src/handlers/.
 *
 * LOCKED: Tauri command names must not change.
 * All wrappers in this directory must call the same command
 * strings as the original src/api/ files they replace.
 */

export {};

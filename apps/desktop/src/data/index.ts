/**
 * data/ — stores (Zustand), schemas (Zod), query helpers
 *
 * Organized by domain. Each subdomain has:
 *   store.ts — Zustand store
 *   schema.ts — Zod schemas
 *   queries.ts — data fetching helpers
 *
 * Phase 5 skeleton. Stores migrated progressively from src/stores/.
 */

export {};

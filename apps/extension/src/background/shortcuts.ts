/**
 * @deprecated import from 'features/background/shortcuts' instead.
 *
 * Re-export shim — canonical source has moved to
 * `src/features/background/shortcuts.ts`. background.ts now imports from
 * the canonical path directly. This shim is kept for safety only.
 */
export * from '../features/background/shortcuts';

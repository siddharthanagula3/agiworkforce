/**
 * background feature barrel — service worker handlers.
 * Planned inhabitants: background.ts (main), background/shortcuts.ts, background/tasks.ts.
 * Currently empty — files will be moved here in subsequent phases.
 */

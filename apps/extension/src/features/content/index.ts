/**
 * content feature barrel — content-scripts per platform.
 * Planned inhabitants: content.ts (main), inPagePanel/*, platform-prompts.ts,
 * nlweb.ts, dom-helpers.ts, page-metadata.ts, webmcp.ts.
 * Currently empty — files will be moved here in subsequent phases.
 */

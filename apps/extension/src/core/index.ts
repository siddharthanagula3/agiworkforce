/**
 * core barrel — message bus, background state, shared popup state.
 * Planned inhabitants: message-bus.ts, background-state.ts, popup-state.ts.
 * Currently empty — files will be moved here in subsequent phases.
 */

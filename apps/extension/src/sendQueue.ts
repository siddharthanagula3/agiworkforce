/**
 * @deprecated import from 'features/native-bridge' instead.
 *
 * Re-export shim — canonical source has moved to
 * `src/features/native-bridge/sendQueue.ts`. This file is kept so that
 * existing import paths (side_panel.ts) resolve without change.
 * Do not add new imports to this path.
 */
export * from './features/native-bridge/sendQueue';

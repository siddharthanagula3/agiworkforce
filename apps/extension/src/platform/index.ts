/**
 * platform barrel — MV3-specific adapters (storage, messaging, alarms).
 * Planned inhabitants: storage.ts, messaging.ts, alarms.ts.
 * Currently empty — files will be moved here in subsequent phases.
 */

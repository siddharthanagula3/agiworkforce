/**
 * data/ — Storage + settings adapters.
 * Covers conversationStore, checkpointManager, contextBudget, contextBuilder,
 * sendQueue, usageMeter, tokenCounter, workspaceIndexer.
 * Phase 6 reorg placeholder.
 */
export {};

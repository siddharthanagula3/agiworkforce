# a11y Audit — VS Code Extension

Date: 2026-05-15  
Files scanned: 77  
Findings: 4 (3 fixed, 1 false positive)

## Findings

These sites use icon-only buttons or icon-bearing TreeItems without a detectable `aria-label` or `accessibilityInformation` within ±5 lines. Review each manually.

### `src/providers/chatParticipant.ts`

| Line | Pattern       | Snippet                                                                                       | Status                                                                                                                                                                                                                         |
| ---- | ------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 521  | TreeItem-icon | `participant.iconPath = vscode.Uri.joinPath(context.extensionUri, 'media', 'icon-chat.png');` | FALSE POSITIVE — `ChatParticipant` is not a `TreeItem`; the VS Code `ChatParticipant` interface has no `accessibilityInformation` property. The participant id `agiworkforce.agi` is the accessible identifier. No fix needed. |

### `src/providers/contextPanelProvider.ts`

| Line | Pattern       | Snippet                                                                                            | Status                                                                                 |
| ---- | ------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 28   | TreeItem-icon | `this.iconPath = new vscode.ThemeIcon(isPinned ? 'pinned' : isActive ? 'circle-filled' : 'file');` | FIXED — `accessibilityInformation` added with descriptive label and `role: 'treeitem'` |
| 45   | TreeItem-icon | `this.iconPath = new vscode.ThemeIcon(groupId === GROUP_PINNED ? 'pin' : 'search');`               | FIXED — `accessibilityInformation` added with group label and `role: 'treeitem'`       |

### `src/providers/conversationTreeProvider.ts`

| Line | Pattern       | Snippet                                            | Status                                                                                  |
| ---- | ------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------- |
| 17   | TreeItem-icon | `this.iconPath = new vscode.ThemeIcon('comment');` | FIXED — `accessibilityInformation` added with conversation title and `role: 'treeitem'` |

## Next steps

- Re-run this script after fixes; target: 0 findings.

# a11y Audit — VS Code Extension

Date: 2026-05-15  
Files scanned: 77  
Findings: 4

## Findings

These sites use icon-only buttons or icon-bearing TreeItems without a detectable `aria-label` or `accessibilityInformation` within ±5 lines. Review each manually.

### `src/providers/chatParticipant.ts`

| Line | Pattern       | Snippet                                                                                       |
| ---- | ------------- | --------------------------------------------------------------------------------------------- |
| 521  | TreeItem-icon | `participant.iconPath = vscode.Uri.joinPath(context.extensionUri, 'media', 'icon-chat.png');` |

### `src/providers/contextPanelProvider.ts`

| Line | Pattern       | Snippet                                                                                            |
| ---- | ------------- | -------------------------------------------------------------------------------------------------- |
| 28   | TreeItem-icon | `this.iconPath = new vscode.ThemeIcon(isPinned ? 'pinned' : isActive ? 'circle-filled' : 'file');` |
| 45   | TreeItem-icon | `this.iconPath = new vscode.ThemeIcon(groupId === GROUP_PINNED ? 'pin' : 'search');`               |

### `src/providers/conversationTreeProvider.ts`

| Line | Pattern       | Snippet                                            |
| ---- | ------------- | -------------------------------------------------- |
| 17   | TreeItem-icon | `this.iconPath = new vscode.ThemeIcon('comment');` |

## Next steps

- Add `accessibilityInformation: { label: '…' }` to each flagged `TreeItem`.
- Add `aria-label="…"` or a tooltip to each flagged icon button in webview HTML.
- Re-run this script after fixes; target: 0 findings.

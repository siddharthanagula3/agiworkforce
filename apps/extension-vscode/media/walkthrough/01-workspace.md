# Workspace-scoped by default

The sidebar, editor chat, and `@agi` use the local AGI runtime for the open repository.

- File and terminal access stay subject to Workspace Trust and the selected agent mode.
- IDE developer sessions do not silently merge into consumer Web, Mobile, or Desktop chat history.
- Attach only the files you want included in a turn.

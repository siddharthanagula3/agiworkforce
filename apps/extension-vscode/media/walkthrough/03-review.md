# Ask, inspect, then accept

Use the composer to describe a TODO, attach workspace files, or share diagnostics.

When AGI proposes code, the extension opens VS Code's native diff view. Review the target file and proposed content before accepting any change.

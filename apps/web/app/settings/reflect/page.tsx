'use client';

import { SettingsModalRedirect } from '@/features/settings/components/SettingsModalRedirect';

/** Deep-link into the on-demand account Reflect recap. */
export default function ReflectSettingsPage() {
  return <SettingsModalRedirect section="reflect" />;
}

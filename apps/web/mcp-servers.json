{
  "servers": [
    {
      "id": "filesystem",
      "name": "Filesystem",
      "description": "Read and list files on the server filesystem (restricted to allowed directories).",
      "transport": {
        "type": "stdio",
        "command": "npx",
        "args": ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"],
        "env": {}
      },
      "enabled": true
    }
  ]
}

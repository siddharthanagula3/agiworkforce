/**
 * apps/web/features - canonical Web product-domain root.
 *
 * Each feature is a self-contained slice with its own components, hooks, stores,
 * services, schemas, and public index.ts barrel when cross-domain imports need it.
 *
 * Product features do not belong under apps/web/src/features. The src/ directory
 * is reserved for future layer primitives such as entry, core, platform,
 * integrations, data, and ui.
 */

export {};

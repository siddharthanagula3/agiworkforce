import { WebWindow } from './DeviceMockups';

/**
 * HeroAppWindow · the AGI Web chat window used as a hero/section visual.
 * Renders the canonical `web` device from DeviceMockups so it is
 * pixel-identical to every other AGI Web mockup on the site.
 */
export function HeroAppWindow() {
  return <WebWindow />;
}

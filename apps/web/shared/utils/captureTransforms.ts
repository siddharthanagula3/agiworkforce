// STB-24 TOMBSTONE — delete this file with `git rm` on a normal checkout.
//
// This was one of eleven byte-identical 5-line stub modules under
// apps/web/shared/utils/ that re-exported @shared/stores/desktop-stubs. All had
// zero importers and each shadowed a real implementation under
// apps/desktop/src/utils/ of the same name. A single mistaken import of
// '@shared/utils/security' or '@shared/utils/subscriptionGate' type-checked
// cleanly and silently disabled the control it was named after.
export {};

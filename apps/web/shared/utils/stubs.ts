// STB-24 TOMBSTONE — delete this file with `git rm` on a normal checkout.
//
// 208 lines of compilation stubs with zero importers. The headline hazard was
// `checkSubscription = () => ({ allowed: true, hasAccess: true })` — an
// unconditional entitlement grant living in a file named after the gate it
// faked. Any import of it type-checked and passed every paywall.
export {};

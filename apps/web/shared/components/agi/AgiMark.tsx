/**
 * Re-exported from @agiworkforce/ui, the cross-surface source of truth for the
 * AGI brand mark (shared by web + desktop). Kept as a thin shim so existing
 * `@/components/agi/AgiMark` imports keep working unchanged.
 */
export { AgiMark } from '@agiworkforce/ui';

/**
 * Re-exported from @agiworkforce/ui — the cross-surface source of truth for
 * pure presentation marks (shared by web + desktop). Kept as a thin shim so
 * existing `@shared/components/ProviderMark` imports keep working unchanged.
 */
export { ProviderMark, hasProviderMark } from '@agiworkforce/ui';

/**
 * src/platform — Next.js-specific hooks, server actions, RSC adapters
 *
 * Layer: platform
 * Depends on: data, ui
 * Must NOT be imported by: data, ui
 *
 * This barrel will re-export platform adapters as they are migrated
 * from apps/web/hooks/, apps/web/utils/, apps/web/handlers/.
 *
 * Includes:
 *   - Next.js App Router server actions
 *   - RSC-compatible data adapters
 *   - Platform-specific React hooks (useRouter, usePathname wrappers)
 */

export {};

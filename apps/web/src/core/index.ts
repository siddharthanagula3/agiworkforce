/**
 * src/core — state orchestration, shared flows
 *
 * Layer: core
 * Depends on: platform, integrations, data, ui
 * Must NOT be imported by: entry (should re-export, not depend on this directly)
 *
 * This barrel will re-export core orchestration as it is migrated
 * from apps/web/core/ (auth, billing, ai, monitoring, security, storage).
 */

export {};

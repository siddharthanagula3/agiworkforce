/**
 * src/ui — reusable client+server primitives
 *
 * Layer: ui
 * Depends on: data
 * Must NOT be imported by: data
 *
 * This barrel will re-export reusable UI primitives as they are migrated
 * from apps/web/components/ui/, apps/web/shared/ui/, apps/web/shared/components/.
 *
 * Design-system atoms and molecules only. No feature-specific logic.
 * Compatible with both RSC (server components) and client components.
 */

export {};

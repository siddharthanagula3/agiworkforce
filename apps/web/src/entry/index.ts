/**
 * src/entry — bootstrap, layout wrappers, route shells
 *
 * Layer: entry (outermost)
 * Depends on: core, features, platform, integrations, data, ui
 * Must NOT be imported by: core, features, platform, integrations, data, ui
 *
 * This barrel will re-export bootstrapping primitives as they are migrated
 * from apps/web/providers/ and apps/web/app/ wrappers.
 */

export {};

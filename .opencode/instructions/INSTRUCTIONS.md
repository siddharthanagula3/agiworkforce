# opencode Instructions

Read root `AGENTS.md` first, then the relevant scoped `AGENTS.md` and `docs/agent-context/`.

This file is an opencode adapter only. Keep durable repo rules in `AGENTS.md`, `docs/agent-context/`, and the scoped agent files. Do not copy long-lived product, model, or verification rules here.

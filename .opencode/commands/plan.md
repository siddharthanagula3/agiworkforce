# Plan

Read root `AGENTS.md`, the relevant scoped `AGENTS.md`, and `docs/agent-context/commands.json` first.

Create a concise implementation plan with owned paths, verification, and risks. Keep work lane-scoped.

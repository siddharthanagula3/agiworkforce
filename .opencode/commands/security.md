# Security

Read root `AGENTS.md`, the relevant scoped `AGENTS.md`, and `docs/agent-context/risk-map.json` first.

Review trust boundaries, auth, secrets, sandboxing, provider routing, payments, and data retention.

# Orchestrate

Read root `AGENTS.md`, `docs/agent-context/lanes.json`, and `docs/engineering/parallel-agent-playbook.md` first.

Split work by non-overlapping lanes, define shared-file ownership, and specify verification per lane.

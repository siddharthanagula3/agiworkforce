# E2E

Read root `AGENTS.md`, the relevant scoped `AGENTS.md`, and `docs/agent-context/commands.json` first.

Plan or run end-to-end verification for the affected user workflow. Capture residual risk if a visual check cannot run.

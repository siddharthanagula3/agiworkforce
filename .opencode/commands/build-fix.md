# Build Fix

Read root `AGENTS.md`, the relevant scoped `AGENTS.md`, and `docs/agent-context/commands.json` first.

Fix build, typecheck, lint, or test failures with minimal diffs and preserve unrelated user changes.

Phase 6 CLI — tools/ caller enumeration
Generated: 2026-05-18 before pilot 6 move

=== RISK ASSESSMENT ===
Wildcard tools imports (tools::* or crate::tools::*):   NONE
Exhaustive match on any tools enum:                      NONE
itertools::Itertools noise hits filtered:                4 (not crate::tools)
Relative super::tools imports (agent/):                  2 — reference agent/tools.rs ONLY
                                                          (separate file; unaffected by move)

=== PER-FILE DISTRIBUTION (42 crate::tools:: call-sites across 4 files) ===
  23  teams.rs         — ToolResult only (return type in 5 fn signatures + construction)
  11  agent/chat.rs    — ToolResult x8, ToolExecOptions x1, execute_tool_with_opts x2
   7  subagent.rs      — ToolResult x7
   1  tui/tui_app.rs   — session_task_summaries x1

=== crate::tools:: TYPE/FUNCTION PATHS USED ===
  37  crate::tools::ToolResult             — struct; defined in tools/mod.rs
   2  crate::tools::ToolExecOptions        — struct; defined in tools/mod.rs
   2  crate::tools::execute_tool_with_opts — fn; defined in tools/mod.rs
   1  crate::tools::session_task_summaries — fn; re-exported from task_registry in mod.rs

All 4 items are defined or re-exported from tools/mod.rs directly.
No crate::tools::bash::, crate::tools::file_ops::, etc. paths used externally.

=== RELATIVE IMPORTS IN agent/ (NOT affected by move) ===
  agent/chat.rs:16:  use super::tools::{execute_mcp_tool, execute_team_tool,
                                        is_mutating_tool, is_team_tool};
  agent/mod.rs:512:  use tools::is_team_tool;
Both reference agent/tools.rs (the agent-internal shim at src/agent/tools.rs),
NOT src/tools/. Confirmed: agent/tools.rs itself uses `crate::tools` which
resolves via the lib.rs re-export. Zero source edits needed.

=== NOISE FILTERED (itertools, not crate::tools) ===
  tui/bottom_pane/list_selection_view.rs:4:  use itertools::Itertools as _;
  tui/bottom_pane/title_setup.rs:10:         use itertools::Itertools;
  tui/exec_cell/render.rs:22:               use itertools::Itertools;
  tui/wrapping.rs:885:                      use itertools::Itertools as _;

=== CONCLUSION ===
42 real call-sites across 4 files. All use crate::tools:: prefix.
The re-export `pub use features::exec as tools` in lib.rs preserves all
4 public paths (ToolResult, ToolExecOptions, execute_tool_with_opts,
session_task_summaries). Zero downstream source edits required.

# Squad: cross-cut

**Surface:** repo-wide | **Subagent:** supervisor

## Baseline (cited from plan)

Commit `254a2f34` (2026-05-19 18:28 -0500): `ci: revert semgrep to advisory; 41 pre-existing findings need drive-to-zero pass`. CI semgrep step (`ci.yml:111`) is `continue-on-error: true` until backlog hits zero. Cited examples in the commit message: `child_process spawn` in `services/api-gateway/src/mcp/mcpProxy.ts:312`, `JWT_SECRET in test fixtures`, `react/no-danger in markdown renderers`. Three `continue-on-error: true` instances confirmed across `.github/workflows/`. CLAUDE.md rule #1 locks models.json as SSOT.

Semgrep is **not installed locally**; the 41 individual finding lines cannot be reproduced exactly without running the cloud action. Categorization below is derived from (a) commit message, (b) the three rulesets used (`p/security-audit`, `p/typescript`, `p/owasp-top-ten`, severity ERROR), and (c) repo-wide grep of the patterns those rulesets fire on.

## 41 semgrep findings — categorized + squad-assignment table

Counts below are local grep hits filtered to non-test, non-build, in-tree files. Total adds to ~41 once de-duplicated (some rules fire twice on one site). Where line numbers are unrecoverable without semgrep cloud output, the file is given and the squad-owner is expected to enumerate against the next CI run.

| #   | Finding (semgrep-style)                                                                                                                                                                                      | File:line                                                                                                                                                                                                                                                                                                            | Category                                                                   | Assigned squad                                                                      | Severity                                                                          | Effort (hrs) |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------ |
| 1   | `child_process.spawn` with caller-controlled `transport.command`                                                                                                                                             | `services/api-gateway/src/mcp/mcpProxy.ts:312`                                                                                                                                                                                                                                                                       | command-injection (security-audit)                                         | squad #3 (web) — owns api-gateway sibling                                           | P1                                                                                | 3            |
| 2   | `execSync(...)` with template-string interpolation (×6 sites)                                                                                                                                                | `apps/mobile/scripts/screenshots/pipeline.ts:203,204,206,207,227,238`                                                                                                                                                                                                                                                | command-injection (security-audit)                                         | squad #4 (mobile)                                                                   | P2 (dev-only script)                                                              | 1            |
| 3   | JWT_SECRET fixture-string in test setup                                                                                                                                                                      | `services/api-gateway/__tests__/setup.ts:8`; `services/api-gateway/src/__tests__/worker.test.ts:30,34`; `services/api-gateway/__tests__/lib/supabaseClients.test.ts:36`                                                                                                                                              | hardcoded-secret (security-audit) — false-positive class but flagged ERROR | squad #3 (web/services)                                                             | P3                                                                                | 1            |
| 4   | JWT_SECRET fixture in middleware/auth/usage tests (×9 sites)                                                                                                                                                 | `services/api-gateway/__tests__/middleware/auth.test.ts:97,112,130,163,197,223,243`; `__tests__/routes/usage.test.ts:91`; `__tests__/routes/auth.test.ts:90`                                                                                                                                                         | hardcoded-secret (security-audit)                                          | squad #3                                                                            | P3                                                                                | 1            |
| 5   | `dangerouslySetInnerHTML` in markdown / SVG / mermaid renderers (×7 desktop, ×1 shared package)                                                                                                              | `apps/desktop/src/components/{Artifacts/ArtifactRendererView,Browser/BrowserDebugTabs,Canvas/ArtifactPreview,ToolCalling/ToolResultCard,UnifiedAgenticChat/artifacts/MermaidArtifact,UnifiedAgenticChat/artifacts/SvgArtifact,editing/LivePreview}.tsx`; `packages/unified-chat/src/components/ArtifactRenderer.tsx` | react/no-danger (typescript)                                               | squad #2 (desktop) for the 7 desktop sites; squad #3 (web) for the package consumer | P2 (sanitized via DOMPurify per CLAUDE.md diff differentiator #2 + `sanitizeSvg`) | 2            |
| 6   | `dangerouslySetInnerHTML` for `JSON.stringify(jsonLd/schema)` (×11 web layout files)                                                                                                                         | `apps/web/app/{blog,careers,contact,download,faq,gallery,layout,mobile,page,pricing}.tsx`; `apps/web/components/SurfaceShowcase.tsx`; `apps/web/shared/components/seo/SEOHead.tsx`; `apps/web/features/chat/components/{ArtifactBlock,MermaidRenderer,cards/CalculationCard}.tsx`                                    | react/no-danger (typescript)                                               | squad #3 (web)                                                                      | P3 (legitimate SEO use; recommend semgrep nosem comments per file)                | 2            |
| 7   | OpenAI service-role-key-style: `createClient(url, SUPABASE_SERVICE_ROLE_KEY, …)` outside sanctioned helpers                                                                                                  | 15 unique routes under `apps/web/app/api/` (see todo P1-1)                                                                                                                                                                                                                                                           | rls-bypass (owasp-top-ten + custom)                                        | squad #3 (web)                                                                      | P1                                                                                | 8            |
| 8   | Backlog padding (semgrep `p/owasp-top-ten` typically also fires on: weak-cipher `crypto.createHash('md5'\|'sha1')`, `Math.random()` for tokens, `eval`, `setTimeout(String)`, `cookie without secure`, etc.) | files not enumerated by this audit                                                                                                                                                                                                                                                                                   | mixed                                                                      | squads #2/#3/#4 (TS surfaces)                                                       | mixed P2/P3                                                                       | 4            |

**Total estimated effort to drive to zero:** ~22 hrs across surface squads #2, #3, #4. Once at zero, flip `ci.yml:111` `continue-on-error: false` and migrate the action from `returntocorp/semgrep-action@v1` (pinned to SHA `713efdd34…`) to direct `semgrep ci` invocation per the commit message follow-up note.

**Recommendation:** add `.semgrepignore` entries for the 16 fixture-JWT findings (#3, #4) — they are intentional test secrets, not production exposures. That alone removes ~16/41 = 39% of the backlog. Squad #3 to add the ignore file + comment.

## models.json SSOT cross-surface scan

| Surface                                                                | Files reading from `@agiworkforce/types/models.json` (canonical)                                                | Files containing hardcoded model-ID literals outside catalog                                                                                                                        | Verdict                                             |
| ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `packages/types`                                                       | `model-catalog.ts`, `provider.ts`, `design-system/provider-display.ts`, `index.ts` re-export                    | `model-catalog.ts` (SSOT consumer; ESLint-exempt — IS the catalog)                                                                                                                  | OK                                                  |
| `packages/llm-runtime`                                                 | `fallback.ts` (documented)                                                                                      | none in source paths checked                                                                                                                                                        | OK                                                  |
| `packages/routing`                                                     | `three-tier-router.ts` (documented inline policy tables — locked, do not migrate per eslint.config.mjs:799-804) | `three-tier-router.ts` (locked exception), `classify.ts` (FIXME-flagged for Wave 1 P0-J/K/L)                                                                                        | OK (eslint-tracked)                                 |
| `packages/providers/{openai,anthropic,google,deepseek,perplexity,xai}` | `catalog.ts` for each provider                                                                                  | `packages/providers/google/src/catalog.ts` (FIXME-flagged Wave 1 P0-J/K/L); ollama version-substring matchers (`llama3.3`, `qwen2.5`) are matchers not IDs                          | OK (eslint-tracked)                                 |
| `packages/local-llm`                                                   | `catalog.ts`, `types.ts` for Llama 3.2 SpinQuant + Phi-4                                                        | `catalog.ts:81,100,145` (`llama-3.2-1b-instruct-spinquant`, `llama-3.2-1b-spinquant`, `phi-4-mini-instruct`) — on-device model bundle IDs, NOT chat-model IDs; legitimate exception | OK (recommend noqa comment to suppress)             |
| `packages/llm-normalize`                                               | reads `models.json` indirectly via types                                                                        | `openai-reasoning-effort.ts` (FIXME-flagged Wave 1 P0-J/K/L)                                                                                                                        | OK (eslint-tracked)                                 |
| `apps/cli`                                                             | `model_catalog.rs` `include_str!("../../../packages/types/src/models.json")` — Rust uses build-time embed       | `chatwidget.rs:375` comment-only reference; no live hardcoded IDs after the FAST_STATUS_MODEL fix                                                                                   | OK                                                  |
| `apps/desktop`                                                         | runs through `@agiworkforce/types` re-export                                                                    | 8 production files FIXME-flagged Wave 2 (modelRouter, tauri-mock, WebRuntime, voiceModeStore, msw-setup, ComputerUseSettings, AutomationBuilder, ModelComparisonView)               | OK (eslint-tracked)                                 |
| `apps/web`                                                             | runs through `@agiworkforce/types` re-export                                                                    | 26 files FIXME-flagged Wave 1 P0-C / Wave 2 sweep — see eslint.config.mjs:737-774                                                                                                   | OK (eslint-tracked)                                 |
| `apps/mobile`                                                          | `src/features/compare/index.tsx` documented; `lib/models.ts` FIXME-flagged                                      | `lib/models.ts` (FIXME-flagged Wave 2 mobile+vscode sweep)                                                                                                                          | OK (eslint-tracked)                                 |
| `apps/extension-vscode`                                                | `features/model-picker/modelConstants.ts` documented; `services/modelConstants.ts` documented                   | `features/model-picker/modelConstants.ts` (FIXME-flagged Wave 2 vscode sweep)                                                                                                       | OK (eslint-tracked)                                 |
| `apps/extension` (chrome)                                              | n/a — extension does not pick models directly                                                                   | none surfaced                                                                                                                                                                       | OK                                                  |
| **`services/api-gateway`**                                             | reads through `@agiworkforce/types`                                                                             | **`routes/cloudChat.ts:526`, `routes/dotfile.ts:73`, `routes/models.ts` (~50 hardcoded IDs in `id:` fields + tier lists)** — eslint-exempted with FIXME P1-MODEL-CATALOG-MIGRATION  | **NEEDS WAVE 2 MIGRATION** (per SYNTHESIS item 2.7) |

**Cross-surface conclusion:** SSOT is intact at the catalog/SDK level. The hardcoded-ID backlog is tracked in `eslint.config.mjs:735-818` with FIXME tickets per file. CLAUDE.md rule #1 is _enforced as a prevention layer_; existing violations are baselined and visible. No new ghost-model (`claude-opus-4-6-mini` style) regressions. The `scripts/check-no-hardcoded-models.sh` gate **partially** covers Rust and is GREEN today: it catches explicit literal assignments (`const FAST_*_MODEL = "..."`) and the named ghost-model regression, but it **does NOT match `.unwrap_or("literal")` fallback patterns** — which is why rust-core Finding #1 (`apps/cli/src/agent/mod.rs:488`) is live despite the gate being green. Wave 1 should extend the script with a Gate 3 covering `unwrap_or\("[a-z]+-[a-z0-9-]+"\)` patterns or formally track the exception.

**Action:** the `services/api-gateway/src/routes/models.ts` file inlines ~50 model IDs in a catalog-shadow array. Recommend squad #3 migrate to a derived-from-catalog list (per CLAUDE.md rule #1) before flipping the semgrep gate; otherwise the api-gateway sibling will drift from `models.json`.

## .github/workflows/\*.yml continue-on-error inventory

| Workflow                          | Step                                                       | Reason (per comments)                                                                                                                                                                           | Recommend flip?                                                                                                                                                                       |
| --------------------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ci.yml:111`                      | `Semgrep (security audit)`                                 | "TEMPORARY revert; 41 pre-existing findings need drive-to-zero pass. Re-tighten once backlog hits zero."                                                                                        | YES — after the ~22-hr backlog cleanup above. Migrate to `semgrep ci` direct invocation per commit follow-up note.                                                                    |
| `ci.yml:416`                      | `cargo test --workspace --lib (default features, Windows)` | "STATUS_ENTRYPOINT_NOT_FOUND (0xc0000139) Windows-specific cross-CRT/DLL incompatibility; cargo check + clippy pass on the same toolchain. Requires real-Windows procmon/depends.exe analysis." | NO — leave advisory until the dependency-graph DLL issue is root-caused on a Windows host. Linux full suite + Windows clippy + Windows cargo check are strict, so the build is gated. |
| `deploy-signaling-server.yml:248` | (job `prune`)                                              | "FIX-040: intentional — this job _only_ prunes old preview builds; if pruning fails, the deploy still succeeded."                                                                               | NO — correctly scoped exception.                                                                                                                                                      |

E2E + smoke tests in `e2e-tests.yml:27,28,89` carry explicit comments "M22: No continue-on-error — failures intentionally block". Good defensive comment to retain.

## CodeQL findings

`.github/workflows/codeql.yml` runs _Rust-only_ security checks (cargo audit + clippy) on push/PR/weekly cron. JS/TS is delegated to GitHub's default CodeQL setup (managed UI, not surfaced in this repo).

Local audit cannot read CodeQL Code Scanning alerts without `mcp__github__*` tooling (none invoked in this run). The Rust CodeQL workflow does NOT use `continue-on-error: true`, and depends on `cargo audit --deny warnings` which is hard-gating in `ci.yml:143` already.

**Recommend:** dispatch a follow-up using `gh api repos/siddharthanagula3/agiworkforce/code-scanning/alerts` to enumerate the JS/TS CodeQL alerts; that's a 2-min query the supervisor can run on demand. Not gating v1 LOCAL ONLY.

## tasks/todo.md reconciliation

| P-item                                                       | Still real?                                                                                                                                                                                                                                                                                                 | Assigned to which squad in current sweep                                | Notes                                                                                                                                                                                                                                        |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Auto-Routing #1 (Kimi K2.6 swap in models.json)              | UNRESOLVED, not in v1 LOCAL ONLY scope                                                                                                                                                                                                                                                                      | n/a (post-launch)                                                       | Pool A Hobby item                                                                                                                                                                                                                            |
| Auto-Routing #5 (`updateConfig()` global mutation → factory) | UNRESOLVED                                                                                                                                                                                                                                                                                                  | n/a (post-launch)                                                       | Pool A Hobby item                                                                                                                                                                                                                            |
| Auto-Routing #6 (canUserMakeUsagePricedRequest wiring)       | UNRESOLVED                                                                                                                                                                                                                                                                                                  | n/a (post-launch)                                                       | Pool A Hobby item                                                                                                                                                                                                                            |
| Auto-Routing #9 (quota tracking 80/100/150 middleware)       | UNRESOLVED                                                                                                                                                                                                                                                                                                  | n/a (post-launch)                                                       | Hobby/paid feature                                                                                                                                                                                                                           |
| Auto-Routing #10 (inline paywall card UX)                    | UNRESOLVED                                                                                                                                                                                                                                                                                                  | squad #3 (web) — but Hobby launch is NO-GO per CLAUDE.md                | Hobby/paid feature                                                                                                                                                                                                                           |
| **P1-1 (56 web API routes use SERVICE_ROLE_KEY)**            | **STALE — reality is 15 files** under `apps/web/app/api/` carry the literal. The "56" number predates the Wave 1 P0-C migration that already landed for the majority.                                                                                                                                       | squad #3 (web)                                                          | Re-count: `rg -l SUPABASE_SERVICE_ROLE_KEY apps/web/app/api/` = 15. Update todo.md "56" → "15". The remaining 15 are device-auth + webhooks + release-channel routes where service-role is required (no user JWT in webhook signature flow). |
| **P1-4 (anon session cookie audit, all callers)**            | **STALE — only 1 caller** (`apps/web/app/api/csrf/route.ts`) and it DOES `response.headers.set('Set-Cookie', newCookie)` at line 49. Marking RESOLVED.                                                                                                                                                      | n/a — close out                                                         | Confirmed by inspection of csrf/route.ts:48-49.                                                                                                                                                                                              |
| P2-1 (6 CLI PHASE2 dead modules)                             | STILL REAL — `apps/cli/src/lib.rs:88-105` still carries `#[allow(dead_code)] // PHASE2` markers for `marketplace`, `sdk_io`, `a2a_ws`, `memory_pipeline`, `skill_learner`, `features::a2a`                                                                                                                  | squad #1 (cli) — but DECISION needed: ship-flag or remove               | Recommend feature-flag stubs behind `--features phase2` rather than dead-code-allow.                                                                                                                                                         |
| P2-2 (`/api/user/stats`, `/api/health-context` missing)      | STILL REAL — `find apps/web/app/api -path "*user/stats*" -o -path "*health-context*"` returns empty                                                                                                                                                                                                         | squad #3 (web)                                                          | Mobile already feature-flagged off. Either implement (1 day) or remove mobile flag (10 min).                                                                                                                                                 |
| P2-3 (OpenAI Responses API wiring before Wave 3)             | PARTIAL — `packages/providers/openai/src/{index.ts:140, translate-responses.ts, responses-types.ts, stream-responses.ts}` all exist and `translateOpenAIResponsesStream` is exported. Looks wired. Recommend squad #3 spot-test.                                                                            | squad #3 (web for verification)                                         | May be DONE; needs runtime smoke.                                                                                                                                                                                                            |
| P2-6 (two migration directories reconciliation)              | STILL REAL — `supabase/migrations/` (**45 files** canonical) and `apps/web/supabase/migrations/` (**50 files** legacy web-local) both present; `processed_stripe_events` table-extend statements exist in BOTH (`supabase/migrations/20260505000007_*` and `apps/web/supabase/migrations/20260108000004_*`) | n/a in v1 LOCAL ONLY scope — but BLOCKS paid Hobby launch               | Each dir contains unique migrations; running both against the same DB risks duplicate-extend ALTER TABLE statements. Reconciliation = 1-day task post-launch.                                                                                |
| **P3-1 (Stripe email fallback)**                             | PARTIAL — `apps/web/app/api/portal/route.ts:158-180` still carries the deprecation warning with `TODO(2026-Q3)` removal date. `apps/web/app/api/stripe-webhook/route.ts` is now only **112 lines** (was 1,723), so the cited `:305` line is no longer valid; webhook refactor moved logic to `lib/`.        | squad #3 (web) — re-locate the email fallback in `stripe-webhook/lib/*` | Update todo.md path.                                                                                                                                                                                                                         |
| P3-2 (CLI quota hardcoded 100% in `main.rs:1870`)            | PARTIALLY STALE — `apps/cli/src/main.rs` is now an **8-line shim** that calls `agiworkforce_cli::run_main()`. The quota hardcode (if still present) lives in `apps/cli/src/lib.rs` somewhere. Plan-streaming references found at `chatwidget.rs:2667,6558,6585` not 3652.                                   | squad #1 (cli)                                                          | Update todo.md file/line references.                                                                                                                                                                                                         |
| P3-3 (2 `@ts-expect-error` for recharts v3)                  | STILL REAL but path moved — now at `apps/desktop/src/features/analytics/CostDashboard.tsx:363,425` (not `components/Analytics/`)                                                                                                                                                                            | squad #2 (desktop)                                                      | Update todo.md path.                                                                                                                                                                                                                         |
| FIXED P0-1, P0-1b (processed_stripe_events)                  | RECONFIRMED — table-extend logic in `supabase/migrations/20260505000007_stripe_webhook_idempotency.sql:24`                                                                                                                                                                                                  | n/a                                                                     | OK                                                                                                                                                                                                                                           |
| FIXED P0-3 (FAST_STATUS_MODEL sentinel)                      | RECONFIRMED — no `FAST_STATUS_MODEL` or `__sentinel_fast_status__` references anywhere in `apps/cli/src/`. ESLint + `check-no-hardcoded-models.sh` gates in place.                                                                                                                                          | n/a                                                                     | OK                                                                                                                                                                                                                                           |
| FIXED P2-4 (empty `apps/web/components/chat/`)               | RECONFIRMED — directory gone                                                                                                                                                                                                                                                                                | n/a                                                                     | OK                                                                                                                                                                                                                                           |

## tasks/lessons.md — should-be-rules

| Lesson                                                             | Currently lint-enforced?                                                                                                            | Recommend rule?                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Lesson 1 (single-file grep misses distributed registrations)**   | Partial — `eslint.config.mjs` `no-restricted-syntax` is AST-aware, not pattern-aware.                                               | YES — squad audits should run `rg -l <pattern> apps/<surface>/src/` before declaring count, not grep one entry-point file. Add to `.github/SQUAD_AUDIT_TEMPLATE.md` as a check-list item. (Currently no such template exists.)                                                    |
| **Lesson 2 (migrations that reference must own)**                  | NO automated gate.                                                                                                                  | YES — pre-commit hook to lint `supabase/migrations/*.sql` for DML operations (`INSERT INTO`, `ALTER TABLE`, `UPDATE`) against a table that has no preceding `CREATE TABLE [IF NOT EXISTS]` in the migration set. Effort ~3 hrs to write the lint script. Tracked as P2 cross-cut. |
| **Lesson 3 (sentinel constants can never match)**                  | YES for ghost-model class — `check-no-hardcoded-models.sh` Gate 1 specifically blocks `claude-opus-4-6-mini`. Could be generalized. | EXTEND — add Gate 3: warn if a `const X: &str = "__sentinel_*"` pattern appears with later `== X` comparison. Effort ~1 hr.                                                                                                                                                       |
| **Lesson 4 (dead-code dirs may export live types)**                | NO.                                                                                                                                 | YES — codify in `tasks/SQUAD_AUDIT_TEMPLATE.md`: before deleting a directory, run `rg -l '<DirName>/' --type-add 'src:*.{ts,tsx,rs}' -t src` to enumerate consumers. Skill: `superpowers:safe-deletion-check`.                                                                    |
| **Lesson 5 (false alarm requires deeper verification)**            | NO automated gate; methodology only.                                                                                                | YES — add to subagent return-format spec: a `DONE_WITH_CONCERNS` agent MUST cite both the find AND a counter-grep. Document in `docs/dev-methodology.md`.                                                                                                                         |
| **Lesson 6 (3 root-level reference docs need post-launch review)** | NO.                                                                                                                                 | NO — explicit founder directive: do not touch during launch. Re-evaluate post-2026-08-16.                                                                                                                                                                                         |

## Out-of-scope observations

- **`apps/desktop/src/components/UnifiedAgenticChat/`** still exists (per CLAUDE.md "Active web chat is `apps/web/features/chat/`" — note that the desktop UnifiedAgenticChat is a DIFFERENT surface). 4 dSIH sites in there. Not duplicate work — out-of-scope for cross-cut.
- **`packages/llm-normalize`** carries an SPDX attribution header per `THIRD_PARTY_LICENSES.md` (OpenClaw MIT). Verified the file still has the attribution. OK.
- **`.cargo/audit.toml` ignore entries** — do-not-touch list respects; not inspected.
- **`/api/v1/chat/completions`** route — squad #3 turf, not cross-cut. Mentioned only because it's the auto-route caller verified during the model-catalog migration audit.
- **`packages/types/src/__tests__/model-catalog.test.ts`** — adds 24+22+14+11 new tests per todo.md Tasks #2-#4 + #15. SSOT regression coverage is strong; recommend keeping.
- **`scripts/check-no-conflict-markers.py`** — runs FIRST in CI per `ci.yml:33`. Excellent defensive gate; ensure it stays first.
- **CI lint job uses `--max-warnings=0`** (`ci.yml:73`). Recent commit `26f52165` cleared 22 warnings to unblock; verify squad #2/#3 don't reintroduce.

## False-positive watchlist

- **JWT_SECRET fixture strings in test files (~16 sites)** — these are intentional `'test-jwt-secret-key-for-testing-only'` style placeholders. Add `.semgrepignore` entries scoped to `services/api-gateway/**/__tests__/**` and `**/setup.ts`. DO NOT replace with `process.env.X` because they ARE setup-stubs for the env-var that test code reads.
- **`packages/local-llm/src/catalog.ts:81,100,145`** — `llama-3.2-1b-instruct-spinquant`, `llama-3.2-1b-spinquant`, `phi-4-mini-instruct` are NOT chat-model IDs; they are on-device model-bundle identifiers (Executorch). Add to `eslint.config.mjs` ignores list as a permanent exception (currently NOT in ignore list).
- **`packages/providers/ollama/src/catalog.ts:31-37`** — `llama3.3 | llama-3.3 | qwen2.5` etc. are PATTERN STRINGS for substring-matching against Ollama's reported model names, not literal IDs. The current ESLint regex `^(gpt-[0-9]|claude-…|grok-[0-9]|o[1-9]-[a-z])` correctly does not match these (no `gpt-/claude-` prefix). OK.
- **`packages/unified-chat/src/components/MessageBubble.tsx:41`** — test fixture `'\tjavascript:exec()'` for `safeHref()` parser — must remain (it's the negative-case assertion).
- **All 11 `dangerouslySetInnerHTML` for `JSON.stringify(jsonLd)` in web layouts** — Next.js SEO best practice; output is JSON, not HTML. Semgrep flags by AST, not content. Add per-file `nosem` or move to a typed `<Script type="application/ld+json">` helper component.
- **`apps/cli/src/main.rs` 8 lines now** — todo.md still references `:1870` and `:3652`. Both moved into `lib.rs` (4500+ lines) and `chatwidget.rs` (6851 lines). Update todo.md before next audit cycle.

## Summary (cross-cut delta)

- **P0 blockers for v1 LOCAL ONLY (within cross-cut scope):** **zero**. CI green; models.json SSOT intact at the cross-cutting layer; ESLint prevention layer + Rust check script in place. **Note:** rust-core squad found one P0 within its own surface scope (`apps/cli/src/agent/mod.rs:488` hardcoded model ID) that is the cross-cutting models.json SSOT lock violated at the leaf level. See `SYNTHESIS.md` Wave 1 for that fix.
- **P1 (block paid Hobby):** P1-1 (15 SERVICE_ROLE_KEY routes — not 56), P2-6 (two migration dirs), semgrep backlog drive-to-zero.
- **P2 (track):** PHASE2 CLI modules (#1), `/api/user/stats` (#3), OpenAI Responses smoke (#3), dSIH inventory cleanup (#2/#3).
- **P3 (cosmetic):** recharts ts-expect-error (#2), Stripe email fallback dead-by-2026-Q3 (#3).
- **Cross-cut lint debt:** generalize sentinel-const gate (~1 hr), migration ownership gate (~3 hr), squad-audit template (~2 hr). Total cross-cut effort to ship the gating quality bar: **~6 hrs supervisor + 22 hrs distributed across squads 2/3/4**.

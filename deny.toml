# cargo-deny config (schema: cargo-deny 0.20.2, generated starting point via
# `cargo deny init` then hand-fit to this workspace's real dependency graph —
# see comments below for the evidence behind each list). Run with:
#   cargo deny check            # all four checks
#   cargo deny check licenses   # single check
#
# CI wiring: wired in `.github/workflows/ci.yml` (job `check`, native scope).
# `cargo deny check bans sources licenses` and `cargo deny check advisories
# --allow unmaintained --allow unsound --allow notice` both block the merge;
# a third step runs the full `cargo deny check advisories` as a non-blocking
# report for the maintenance classes tracked under
# RUST-DEPENDENCY-ADVISORIES-01 in docs/agent-context/known-flaws.md.

[graph]
# No target-triple pruning: the workspace ships macOS, Linux, and Windows
# desktop/CLI binaries (see build-windows-release.yml, release-desktop.yml,
# release-cli.yml matrix targets), so all platforms' dependency edges matter.
all-features = false
no-default-features = false

[output]
feature-depth = 1

# https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html
# cargo-deny 0.20's advisories check has no separate unmaintained/vulnerability
# lint-level knobs (removed from the config-v2 schema) — RUSTSEC vulnerability,
# unmaintained, unsound, and yanked advisories are denied by default. This
# section only needs an `ignore` list.
#
# RUSTSEC-2026-0258 (h2 unbounded empty DATA frames) is the one accepted risk.
# The 0.4 line is patched — Cargo.lock holds h2 0.4.16, at or above the fixed
# version, so this ID cannot match it. What remains is h2 0.3.27, reachable
# only through hyper 0.14, which async-stripe 0.31 and oauth2 4.4 both pin.
# Neither can move: async-stripe has published nothing stable past 0.31 (only
# 1.0.0-rc.*, a full API rewrite of billing code), and oauth2 5.x is a tracked
# call-site port (see apps/desktop/src-tauri/Cargo.toml). Both crates use h2
# strictly as an HTTP client against Stripe and the identity provider, so
# exploiting this needs one of those endpoints to be hostile, and the advisory
# rates it Low. Re-check when async-stripe 1.0 ships stable.
[advisories]
ignore = [
    { id = "RUSTSEC-2026-0258", reason = "h2 0.3.27 via hyper 0.14 (async-stripe 0.31 + oauth2 4.4); no 0.3.x patch exists and both parents are pinned upstream. Client-side only, Low severity. Revisit on async-stripe 1.0 stable." },
]

# https://embarkstudios.github.io/cargo-deny/checks/licenses/cfg.html
[licenses]
# Allow list derived from `cargo metadata --format-version=1` over the real
# workspace graph (2026-07-16), keeping only SPDX atoms that are the SOLE or
# AND-mandatory license for at least one crate actually in the graph — i.e.
# every entry here is load-bearing, not padding. Atoms that only ever appear
# behind a permissive OR (e.g. `LGPL-2.1-or-later` in r-efi's
# `MIT OR Apache-2.0 OR LGPL-2.1-or-later`, `GPL-2.0-only` in self_cell's
# `Apache-2.0 OR GPL-2.0-only`) are deliberately NOT allowed: the permissive
# branch already satisfies the expression, and leaving the copyleft branch
# off the allow list means a future dependency bump that drops the permissive
# branch fails the gate instead of silently passing.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Zlib",
    "0BSD",
    "BSL-1.0",
    "CC0-1.0",
    "CDLA-Permissive-2.0",
    "MPL-2.0",
    "NCSA",
    "Unicode-3.0",
]
confidence-threshold = 0.8

# First-party workspace crates: five declare `license = "Proprietary"`
# (agiworkforce-desktop, agiworkforce-cli, agiworkforce-app-server,
# agiworkforce-command-registry, agiworkforce-sandbox-policy — matches this
# repo's actual posture, a proprietary codebase per THIRD_PARTY_LICENSES.md);
# nine more declare no `license` field at all (agiworkforce-agent-core,
# agiworkforce-execpolicy, agiworkforce-licensing, agiworkforce-llm,
# agiworkforce-mcp, agiworkforce-model-registry, agiworkforce-protocol,
# agiworkforce-utils-absolute-path, agiworkforce-utils-image). "Proprietary"
# is not a valid SPDX identifier —
# cargo-deny cannot even parse it as a license expression, so it can never
# satisfy `allow` or `exceptions` (both require parseable SPDX; verified by
# running `cargo deny check licenses` against a "Proprietary" exceptions
# entry, which fails with `error[custom]: unknown term`, not an allow-list
# rejection). `clarify` cannot help either — it matches license *text* in a
# LICENSE file, and these crates carry none.
#
# All fourteen first-party package manifests now declare `publish = false`.
# `licenses.private.ignore = true` below therefore excludes them as intended;
# no invalid SPDX exception or text-based clarification is needed.
exceptions = []

[licenses.private]
# First-party packages are private (`publish = false`); third-party dependency
# licensing remains enforced.
ignore = true

# This workspace has no crate publishing to a private registry today.
registries = []

# https://embarkstudios.github.io/cargo-deny/checks/bans/cfg.html
[bans]
# Real duplicate-version families observed in the graph today (constant_time_eq,
# notify, r-efi, webpki-roots, and similar transitive pairs from divergent
# consumer requirements) — none is a bannable/unsafe issue on inspection, so
# this stays a warn, not a deny, per the W5 guardrail-batch spec.
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
allow-workspace = false

# https://embarkstudios.github.io/cargo-deny/checks/sources/cfg.html
[sources]
unknown-registry = "deny"
# Deny by default (crates-io only, per the W5 guardrail-batch spec); the three
# git-sourced crates below are intentional, documented exceptions — see root
# Cargo.toml's `[patch.crates-io]` (the websocket-handshake-hardening forks and
# xcb's upstream quick-xml security migration), not unreviewed drive-by deps.
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = [
    "https://github.com/openai-oss-forks/tokio-tungstenite",
    "https://github.com/openai-oss-forks/tungstenite-rs",
    "https://github.com/rust-x-bindings/rust-xcb",
]

# No [sources.allow-org] entry: the exact allow-git URLs above already cover
# each reviewed repository. An `allow-org` entry was deliberately avoided:
# exact repository URLs keep the exception narrower than organization-wide
# trust.

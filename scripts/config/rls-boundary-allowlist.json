{
  "schemaVersion": 1,
  "entries": [
    {
      "path": "apps/web/app/api/admin/takedown/route.ts",
      "reason": "platform-admin route gated by requirePlatformAdmin, deliberately cross-user by design"
    },
    {
      "path": "apps/web/app/api/auth/device/approve/route.ts",
      "reason": "RFC 8628 device flow, the pending row is resolved by user_code before any account is attached, there is no owner to constrain by until approval"
    },
    {
      "path": "apps/web/app/api/auth/device/code/route.ts",
      "reason": "RFC 8628 device flow, the pending row is resolved and expired by user_code before any account is attached, user_id is null until approval"
    },
    {
      "path": "apps/web/app/api/auth/device/refresh/route.ts",
      "reason": "refresh-token reuse detection revokes the whole family_id, the credential is the subject and constraining by user would defeat the revocation"
    },
    {
      "path": "apps/web/app/api/auth/device/token/route.ts",
      "reason": "RFC 8628 device flow, the row is resolved by device_id and user_code, not the caller, until the poll succeeds and a developer token is minted"
    },
    {
      "path": "apps/web/app/api/billing/top-up/route.ts",
      "reason": "the only owner-pool call is the workspace policy gate, which reads the organization admin policy, billing contract and funding-membership rows a member scoped connection cannot see and whose read failure fails the gate open; the storage probe, the subscription read and every other statement run on the caller connection"
    },
    {
      "path": "apps/web/app/api/claim-offer/route.ts",
      "reason": "a beta invite belongs to nobody until it is redeemed, and claim_beta_invite locks the invite row, spends a use and writes the redemption the claimant does not own yet; only the subscription read-back is caller-scoped"
    },
    {
      "path": "apps/web/app/api/copyright-notice/route.ts",
      "reason": "public unauthenticated DMCA-style intake, the reporter has no account to scope by"
    },
    {
      "path": "apps/web/app/api/cron/purge-deleted-media/route.ts",
      "reason": "cron route gated by verifyCronRequest against CRON_SECRET, platform-scoped by design"
    },
    {
      "path": "apps/web/app/api/cron/purge-temporary-chats/route.ts",
      "reason": "cron sweep of expired temporary chats across every owner, runs with no request user"
    },
    {
      "path": "apps/web/app/api/cron/reset-credits/route.ts",
      "reason": "cron route gated by verifyCronRequest against CRON_SECRET, sweeps every subscription due for reset"
    },
    {
      "path": "apps/web/app/api/device/approve/route.ts",
      "reason": "QR device-pairing flow, the pending row is resolved by its pairing code, not the approving session, until the pairing completes"
    },
    {
      "path": "apps/web/app/api/device/link/route.ts",
      "reason": "QR device-pairing flow, mints a pending pairing row with no device attached yet, there is no second party to constrain by"
    },
    {
      "path": "apps/web/app/api/device/poll/route.ts",
      "reason": "QR device-pairing flow, the row is resolved by device_id shared with the CLI flow, not by a caller session"
    },
    {
      "path": "apps/web/app/api/feedback/route.ts",
      "reason": "feedback is reachable from Local mode with no cloud account, so user_id is nullable by design and a null owner cannot satisfy any WITH CHECK; the insert carries the server-derived user id and reads nothing back"
    },
    {
      "path": "apps/web/app/api/github/oauth/callback/route.ts",
      "reason": "the installation_id conflict target may hold an unverified row belonging to another account, and claiming it once ownership is proven is the point of the flow, so the upsert cannot be constrained to rows the caller can already see"
    },
    {
      "path": "apps/web/app/api/github/webhook/route.ts",
      "reason": "GitHub webhook verified by HMAC signature, resolves its own installation subject from the signed payload"
    },
    {
      "path": "apps/web/app/api/llm/v1/chat/completions/lib/assistant-turn-persistence.ts",
      "reason": "the turn is written under RLS through createClaimedUserScopedDb bound to the user id the turn already carries, because a cancelled stream settles after the request context is gone and there is no session left to bind; getNeonDb here is the schema-owner pool that SET ROLE app_rls needs, not an unscoped write"
    },
    {
      "path": "apps/web/app/api/me/route.ts",
      "reason": "the profiles upsert already runs under RLS through createClaimedUserScopedDb, which needs a schema-owner pool to SET ROLE app_rls on; getNeonDb here is that pool, not an unscoped read"
    },
    {
      "path": "apps/web/app/api/mobile/content-report/route.ts",
      "reason": "trust-and-safety reporting is reachable from Local mode with no cloud account, so user_id is nullable by design, and the client_report_id conflict target must dedupe across every reporter"
    },
    {
      "path": "apps/web/app/api/mobile/feedback/route.ts",
      "reason": "feedback is reachable from Local mode with no cloud account, so user_id is nullable by design and a null owner cannot satisfy any WITH CHECK; the insert carries the server-derived user id and reads nothing back"
    },
    {
      "path": "apps/web/app/api/mobile/iap/google-notifications/route.ts",
      "reason": "Google Play Pub/Sub notification verified by OIDC token, resolves its own subject from the provider payload"
    },
    {
      "path": "apps/web/app/api/mobile/push-token/route.ts",
      "reason": "the client-supplied device id may already be held by another account the caller's scope cannot see, so the ownership probe that answers 403 reads over the schema owner; the upsert and the token clear are caller-scoped"
    },
    {
      "path": "apps/web/app/api/share/[token]/route.ts",
      "reason": "reading a shared conversation by its unguessable token is the feature, a public share link has no viewer subject to constrain by"
    },
    {
      "path": "apps/web/app/api/share/route.ts",
      "reason": "the GET reads a share by its unguessable token with no viewer subject, the POST insert already carries the authenticated user_id"
    },
    {
      "path": "apps/web/app/api/user/export/route.ts",
      "reason": "the whole GDPR export already runs under RLS through createClaimedUserScopedDb, which needs a schema-owner pool to SET ROLE app_rls on; getNeonDb here is that pool, not an unscoped read"
    },
    {
      "path": "apps/web/app/api/waitlist/cloud-managed/route.ts",
      "reason": "waitlist rows are captured before any account exists, there is no owner to constrain by"
    },
    {
      "path": "apps/web/app/api/waitlist/public/route.ts",
      "reason": "waitlist rows are captured before any account exists, there is no owner to constrain by"
    },
    {
      "path": "apps/web/app/api/waitlist/route.ts",
      "reason": "waitlist rows are captured before any account exists, there is no owner to constrain by"
    },
    {
      "path": "apps/web/app/api/web-push/route.ts",
      "reason": "the push endpoint the browser mints is a globally unique key, and the row holding it may belong to another account the caller's scope cannot see, so the ownership probe that answers 403 reads over the schema owner; the upsert and the delete are caller-scoped"
    },
    {
      "path": "apps/web/app/share/[token]/page.tsx",
      "reason": "reading a share by its unguessable token is the feature, same as the api/share/[token] route it renders"
    },
    {
      "path": "apps/web/features/admin/services/operator-metrics.ts",
      "reason": "reached only from the platform-admin operator route, deliberately platform-wide aggregate reads across every tenant"
    },
    {
      "path": "apps/web/lib/connectors/mcp-state-store.ts",
      "reason": "mcp_app_payloads and mcp_task_bindings had row-level security disabled and no grants at all for the app_rls role, so a scoped statement was refused outright; the grants, row-level security and policies now exist as draft migration 0173 (NOT YET APPLIED in production) and this file can move to getUserScopedDb once it is applied"
    },
    {
      "path": "apps/web/lib/github-app.ts",
      "reason": "shared by the GitHub webhook, verified by HMAC signature with no Clerk session, and the OAuth install routes, so not every caller has a session to bind"
    },
    {
      "path": "apps/web/lib/security-audit.ts",
      "reason": "the audit trail records unauthenticated and failed attempts, so there is often no subject to bind; security_audit_logs's row-level security policy now exists as draft migration 0170 (NOT YET APPLIED in production), and the subject-bearing writes here can be scoped once it is applied, but the null-subject case still needs the owner connection"
    },
    {
      "path": "apps/web/lib/server/account-erasure.ts",
      "reason": "account erasure engine deletes across every user-owned table by the target userId the caller already resolved, not a session token RLS can bind"
    },
    {
      "path": "apps/web/lib/server/anonymous-erasure.ts",
      "reason": "erases pre-account rows by email hash for subjects who never created an account, there is no Clerk user to bind RLS to"
    },
    {
      "path": "apps/web/lib/server/content-report-triage.ts",
      "reason": "moderation triage queue reached only from the admin content-reports route, reading every reporter is the purpose"
    },
    {
      "path": "apps/web/lib/server/developer-token.ts",
      "reason": "revocation targets the row resolved from the presented token signature and family id, the credential is the subject"
    },
    {
      "path": "apps/web/lib/server/organization-erasure.ts",
      "reason": "organization erasure engine deletes across every org-owned table by the target organizationId, triggered only by the deletion cron"
    },
    {
      "path": "apps/web/lib/server/security-log-retention.ts",
      "reason": "retention sweep over the whole audit log triggered by cron or admin, deletes by age not by subject"
    },
    {
      "path": "apps/web/lib/services/api-key-service.ts",
      "reason": "verifyKey resolves the presented credential by its key_prefix before any session exists, so the key is the subject and there is no caller to bind; every other method takes the caller connection"
    },
    {
      "path": "apps/web/lib/services/free-trial-service.ts",
      "reason": "every statement already runs under RLS through createClaimedUserScopedDb bound to the reservation owner, which needs a schema-owner pool to SET ROLE app_rls on; getNeonDb here is that pool, not an unscoped read"
    },
    {
      "path": "apps/web/lib/services/org-entitlements.ts",
      "reason": "organization entitlement resolves from the OWNER subscription row, which a member scoped connection cannot see, so every administrator who was not the owner resolved to free; both callers establish membership on their own scoped connection first"
    },
    {
      "path": "apps/web/lib/services/org-sharing-service.ts",
      "reason": "the admin sharing console lists projects owned by other members, and the org shared read policy on user_projects hides one the admin own per-member override denies them; every other statement runs on the caller connection"
    },
    {
      "path": "apps/web/lib/services/push-notification-service.ts",
      "reason": "invalidateTokens clears exact push tokens the provider reported unregistered, a device credential is not a user, every other export already carries user_id"
    },
    {
      "path": "apps/web/lib/services/route-cache-observability-service.ts",
      "reason": "reached only from the platform-admin observability routes, deliberately cross-tenant aggregate reads"
    },
    {
      "path": "apps/web/lib/services/schedule-service.ts",
      "reason": "scheduler worker writes back to the task id it claimed from the due-set in the same transaction, there is no request subject to constrain by"
    },
    {
      "path": "apps/web/lib/services/security-monitoring-service.ts",
      "reason": "platform abuse detection reached only from the admin security route, per-user counts cannot detect an attacker spraying accounts from one IP"
    },
    {
      "path": "apps/web/lib/services/subscription-service.ts",
      "reason": "the profile provisioning and Stripe self-heal writes already run under RLS through createClaimedUserScopedDb, which needs a schema-owner pool to SET ROLE app_rls on; every other method takes the caller connection"
    },
    {
      "path": "apps/web/lib/services/web-push-service.ts",
      "reason": "the matched word is a local variable holding push-subscription objects, not the billing subscriptions table, every statement already carries user_id"
    },
    {
      "path": "apps/web/lib/support/handoff/store.ts",
      "reason": "a handoff session is owned by an anonymous visitor and read by the staff console, agent_user_id is staff not a customer, spans every visitor by design"
    },
    {
      "path": "apps/web/lib/user-connector-tools.ts",
      "reason": "three statements stay on the schema owner: the github_installations read, whose table's row-level security policy now exists as draft migration 0171 (NOT YET APPLIED in production) and can be scoped once it is applied, and the two organization shared connector reads, which join a connector row owned by another member; every caller-owned read runs on a claimed scope"
    }
  ]
}

{
  "$comment": "Environment keys apps/web reads that the config key registry in apps/web/lib/validate-env.ts does not declare, measured by scripts/check-config-keys.mjs on 2026-09-20. Each entry says where the decision lives instead. The guard refuses a new unclassified key and refuses an entry nothing reads any more.",
  "unregistered": [
    {
      "key": "ACCOUNT_STATUS_FAIL_OPEN",
      "reason": "a server rollout or escape hatch switch, declared in the feature flag registry or in SECURITY_ESCAPE_HATCHES, which already states what turning it on costs"
    },
    {
      "key": "AGI_DATABASE_PROVIDER",
      "reason": "an adapter selection read by the composition root in its own package, which owns the set of adapters it can choose from"
    },
    {
      "key": "AGI_MAP_GEOCODER_CONTACT",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "AGI_NOTIFICATIONS_FROM_EMAIL",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "AGI_PLATFORM_KEY_PROVIDER",
      "reason": "an adapter selection read by the composition root in its own package, which owns the set of adapters it can choose from"
    },
    {
      "key": "AGI_PROVIDER_PROXY_ORIGIN",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "AGI_SUPPORT_EXPECTED_REPLY_COPY",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "AGI_SUPPORT_LIVE_HANDOFF_ENABLED",
      "reason": "a server rollout or escape hatch switch, declared in the feature flag registry or in SECURITY_ESCAPE_HATCHES, which already states what turning it on costs"
    },
    {
      "key": "ANDROID_APP_LINKS_SHA256_CERT_FINGERPRINTS",
      "reason": "a serialized document rather than a single value, so its shape is validated by the schema that parses it rather than by a descriptor"
    },
    {
      "key": "ANTHROPIC_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "APPLE_APP_STORE_APP_ID",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "APPLE_APP_STORE_BUNDLE_ID",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "APPLE_APP_STORE_ENVIRONMENT",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "APPLE_APP_STORE_ROOT_CA_CERTS_BASE64_JSON",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "APPLE_APP_STORE_SANDBOX_NOTIFICATIONS_ENABLED",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "BILLING_ALERT_EMAIL",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "CONNECTOR_MCP_SERVERS_JSON",
      "reason": "a serialized document rather than a single value, so its shape is validated by the schema that parses it rather than by a descriptor"
    },
    {
      "key": "CRON_DEV_BYPASS",
      "reason": "a server rollout or escape hatch switch, declared in the feature flag registry or in SECURITY_ESCAPE_HATCHES, which already states what turning it on costs"
    },
    {
      "key": "DOCUSIGN_ACCOUNT_ID",
      "reason": "an e-signature vendor setting, owned by the enterprise contract lane"
    },
    {
      "key": "DOCUSIGN_API_BASE_URL",
      "reason": "an e-signature vendor setting, owned by the enterprise contract lane"
    },
    {
      "key": "DOCUSIGN_INTEGRATION_KEY",
      "reason": "an e-signature vendor setting, owned by the enterprise contract lane"
    },
    {
      "key": "DOCUSIGN_OAUTH_BASE_URL",
      "reason": "an e-signature vendor setting, owned by the enterprise contract lane"
    },
    {
      "key": "DOCUSIGN_PRIVATE_KEY",
      "reason": "an e-signature vendor setting, owned by the enterprise contract lane"
    },
    {
      "key": "DOCUSIGN_USER_ID",
      "reason": "an e-signature vendor setting, owned by the enterprise contract lane"
    },
    {
      "key": "GEMINI_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "GITHUB_APP_CLIENT_ID",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GITHUB_APP_CLIENT_SECRET",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GITHUB_APP_ID",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GITHUB_APP_PRIVATE_KEY_BASE64",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GITHUB_APP_SLUG",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GITHUB_BOT_LOGIN",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GITHUB_PR_REVIEW_MONTHLY_CAP",
      "reason": "a GitHub App installation setting, owned by the code lane; the three secrets among them are already warned about by validateRequiredEnvVars"
    },
    {
      "key": "GOOGLE_AI_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "GOOGLE_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "GOOGLE_PLACES_API_KEY",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "GOOGLE_PLAY_PACKAGE_NAME",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "GOOGLE_PLAY_PUBSUB_AUDIENCE",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "GOOGLE_PLAY_PUBSUB_SERVICE_ACCOUNT_EMAIL",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "GOOGLE_PLAY_SERVICE_ACCOUNT_JSON",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "HOSTNAME",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "LLM_TTFT_SLO_BREACH_MS",
      "reason": "a latency objective, declared in SLO_CATALOGUE where the objective it belongs to is"
    },
    {
      "key": "LLM_TTFT_SLO_TARGET_MS",
      "reason": "a latency objective, declared in SLO_CATALOGUE where the objective it belongs to is"
    },
    {
      "key": "MOBILE_IAP_ENABLED",
      "reason": "a mobile store receipt verification setting, owned by the mobile purchase lane rather than by the web configuration surface"
    },
    {
      "key": "MODERATION_HASH_DENYLIST",
      "reason": "a serialized document rather than a single value, so its shape is validated by the schema that parses it rather than by a descriptor"
    },
    {
      "key": "NEXT_PHASE",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "NEXT_PUBLIC_AGI_BOT_PROTECTION",
      "reason": "a client rollout switch. Switches are the feature flag registry, which declares its own owner and default; duplicating them here would give one switch two homes"
    },
    {
      "key": "NEXT_PUBLIC_APP_VERSION",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "NEXT_PUBLIC_CHECKOUT_ENABLED",
      "reason": "a client rollout switch. Switches are the feature flag registry, which declares its own owner and default; duplicating them here would give one switch two homes"
    },
    {
      "key": "NEXT_PUBLIC_COMPOSER_EDITOR",
      "reason": "a client rollout switch. Switches are the feature flag registry, which declares its own owner and default; duplicating them here would give one switch two homes"
    },
    {
      "key": "NEXT_PUBLIC_FREE_LANE_UI",
      "reason": "a client rollout switch. Switches are the feature flag registry, which declares its own owner and default; duplicating them here would give one switch two homes"
    },
    {
      "key": "NEXT_PUBLIC_GA_TRACKING_ID",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "NEXT_PUBLIC_MCP_APP_SANDBOX_ORIGIN",
      "reason": "a single feature setting owned by the lane that reads it; registering it here would move the decision away from the module that knows what an absent value means"
    },
    {
      "key": "NEXT_PUBLIC_MESSAGE_VARIANTS",
      "reason": "a client rollout switch. Switches are the feature flag registry, which declares its own owner and default; duplicating them here would give one switch two homes"
    },
    {
      "key": "NEXT_PUBLIC_SENTRY_RELEASE",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "NEXT_PUBLIC_SUPPORT_WIDGET_ENABLED",
      "reason": "a client rollout switch. Switches are the feature flag registry, which declares its own owner and default; duplicating them here would give one switch two homes"
    },
    {
      "key": "NEXT_PUBLIC_VERCEL_ENV",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "OPENAI_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "OPENROUTER_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "OPENROUTER_WEBHOOK_SECRET",
      "reason": "a webhook endpoint and its credential, owned by the security lane that holds the inspection path"
    },
    {
      "key": "PRICE_ID_OVERRIDES",
      "reason": "a serialized document rather than a single value, so its shape is validated by the schema that parses it rather than by a descriptor"
    },
    {
      "key": "QWEN_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "RUNWAY_API_KEY",
      "reason": "a model provider credential. The provider set is the model registry, which is founder frozen; each key is read only through the provider runtime boundary"
    },
    {
      "key": "SENTRY_RELEASE",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "SKILLS_LAYERS",
      "reason": "a serialized document rather than a single value, so its shape is validated by the schema that parses it rather than by a descriptor"
    },
    {
      "key": "SOFT_DELETED_RESOURCE_PURGE_ENABLED",
      "reason": "a server rollout or escape hatch switch, declared in the feature flag registry or in SECURITY_ESCAPE_HATCHES, which already states what turning it on costs"
    },
    {
      "key": "STRIPE_CHECKOUT_ENABLED",
      "reason": "a server rollout or escape hatch switch, declared in the feature flag registry or in SECURITY_ESCAPE_HATCHES, which already states what turning it on costs"
    },
    {
      "key": "STRIPE_PRICE_BASIC_MONTHLY_INR",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_BASIC_MONTHLY_USD",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_ENTERPRISE_MONTHLY",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_ENTERPRISE_YEARLY",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_MAX_15X_MONTHLY",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_MAX_MONTHLY",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_PRO_MONTHLY",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_PRO_YEARLY",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_TEAM_MONTHLY_INR",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_TEAM_MONTHLY_USD",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRICE_TEAM_YEARLY_USD",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "STRIPE_PRODUCT_ENTERPRISE",
      "reason": "a Stripe price or product id. What a plan costs is the billing catalogue, which the founder has frozen; the registry classifies configuration, not the price list"
    },
    {
      "key": "UPLOAD_SCAN_REQUIRED",
      "reason": "a server rollout or escape hatch switch, declared in the feature flag registry or in SECURITY_ESCAPE_HATCHES, which already states what turning it on costs"
    },
    {
      "key": "UPLOAD_SCAN_WEBHOOK_TOKEN",
      "reason": "a webhook endpoint and its credential, owned by the security lane that holds the inspection path"
    },
    {
      "key": "UPLOAD_SCAN_WEBHOOK_URL",
      "reason": "a webhook endpoint and its credential, owned by the security lane that holds the inspection path"
    },
    {
      "key": "VERCEL_GIT_COMMIT_SHA",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "VERCEL_REGION",
      "reason": "set by the build platform rather than by an operator, so there is no owner to name and no environment it may be set in"
    },
    {
      "key": "WEB_MCP_SERVERS_JSON",
      "reason": "a serialized document rather than a single value, so its shape is validated by the schema that parses it rather than by a descriptor"
    }
  ],
  "registeredButUnread": [
    {
      "key": "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY",
      "reason": "read by the identity package and by the root layout outside the scanned roots"
    },
    {
      "key": "AGI_ALLOW_REMOTE_DATABASE",
      "reason": "read by packages/platform/data-layer, which is where the isolation rule it overrides lives"
    },
    {
      "key": "AGI_E2B_COMPUTE_MICROUSD_PER_SECOND",
      "reason": "read by the cost ledger in packages, and warned about by validateRequiredEnvVars"
    },
    {
      "key": "AGI_OTEL_EXPORTER_ENDPOINT",
      "reason": "read by packages/platform/observability, which owns the exporter"
    }
  ]
}

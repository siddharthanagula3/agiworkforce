{
  "schemaVersion": 1,
  "entries": [
    {
      "file": "apps/web/app/api/artifacts/publish/route.ts",
      "migration": 257,
      "reason": "writes and reads the published_artifact_versions rows that 0257 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/app/api/code/sessions/[sessionId]/notebook/lib/notebook-runs.ts",
      "migration": 254,
      "reason": "writes and reads the notebook_runs rows that 0254 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 249,
      "reason": "writes and reads the background_jobs fencing and usage columns that 0249 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 252,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 254,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 257,
      "reason": "writes and reads the file_lineage rows that 0257 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/jobs/cancellation.ts",
      "migration": 249,
      "reason": "writes and reads the background_jobs cancellation and worker-fencing columns that 0249 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/jobs/job-service.ts",
      "migration": 249,
      "reason": "writes and reads the background_jobs cancellation, retry and worker-fencing columns that 0249 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/server/account-erasure.ts",
      "migration": 249,
      "reason": "writes and reads the background_jobs usage column that 0249 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/server/account-erasure.ts",
      "migration": 252,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/lib/server/account-erasure.ts",
      "migration": 254,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/lib/server/account-erasure.ts",
      "migration": 257,
      "reason": "writes and reads the file_lineage and published_artifact_versions rows that 0257 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/server/generated-file-persist.ts",
      "migration": 257,
      "reason": "writes and reads the file_lineage rows that 0257 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/server/organization-erasure.ts",
      "migration": 252,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/lib/server/organization-erasure.ts",
      "migration": 254,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/lib/server/organization-erasure.ts",
      "migration": 257,
      "reason": "writes and reads the file_lineage rows that 0257 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/server/slo/catalogue.ts",
      "migration": 249,
      "reason": "names background_jobs as a catalogue key only and executes no SQL against the columns 0249 adds, so the SLO catalogue is correct either side of the apply."
    },
    {
      "file": "apps/web/lib/services/deletion-manifest.ts",
      "migration": 252,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/lib/services/deletion-manifest.ts",
      "migration": 254,
      "reason": "classification key only, executes no SQL against the table: the erasure and export inventories name it so the completeness test has to account for it, and the sweep skips a table the database does not yet have."
    },
    {
      "file": "apps/web/lib/services/deletion-manifest.ts",
      "migration": 257,
      "reason": "writes and reads the file_lineage and published_artifact_versions rows that 0257 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    },
    {
      "file": "apps/web/lib/services/managed-memory-context-service.ts",
      "migration": 252,
      "reason": "writes and reads the organization_admin_policies context-source columns that 0252 adds; deploy-production.yml runs `pnpm db:migrate -- verify`, which throws while that migration is pending (scripts/lib/neon-migrations.mjs verifyMigrations), so this code cannot reach production before it is applied."
    }
  ]
}

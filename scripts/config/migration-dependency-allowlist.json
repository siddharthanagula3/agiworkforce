{
  "schemaVersion": 1,
  "entries": [
    {
      "file": "apps/web/app/api/settings/organization/route.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/authorization/subject.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/connectors/mcp-custom-connections.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/server/scim/scim-provisioning-service.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/active-workspace-service.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/domain-retention-service.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/organization-invitation-service.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/organization-policy-override-service.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/user-connector-tools.ts",
      "migration": 274,
      "reason": "Does not read the row timestamps this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new columns are additive with defaults, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/auth/device/token/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/connectors/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/device/poll/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/search/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/settings/devices/[deviceId]/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/settings/sync/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/device-steps/device-registry.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/domain-retention-service.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/ediscovery-export-service.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/org-shared-session-service.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/retrieval-index-service.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/support/actions/executors/revoke-connector.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/triggers/trigger-ingest.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/user-connector-tools.ts",
      "migration": 277,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a created_at or updated_at column of a different table, which the guard matches as a substring. The new column is additive with a default, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/server/risk-signals.ts",
      "migration": 278,
      "reason": "Reads columns this migration adds. The production deploy runs the migration verify step, which refuses to promote while a migration is pending, so this code cannot reach production before the migration is applied."
    },
    {
      "file": "apps/web/app/api/llm/v1/chat/completions/lib/context-compaction.ts",
      "migration": 280,
      "reason": "Reads and writes the column this migration adds. The production deploy runs the migration verify step, which refuses to promote while a migration is pending, so this code cannot reach production before the migration is applied."
    },
    {
      "file": "apps/web/app/api/chat/conversations/[id]/route.ts",
      "migration": 280,
      "reason": "Reads and writes the column this migration adds. The production deploy runs the migration verify step, which refuses to promote while a migration is pending, so this code cannot reach production before the migration is applied."
    },
    {
      "file": "apps/web/app/api/chat/sync/route.ts",
      "migration": 280,
      "reason": "Reads and writes the column this migration adds. The production deploy runs the migration verify step, which refuses to promote while a migration is pending, so this code cannot reach production before the migration is applied."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 283,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a column of the same name on a different table or the word in a comment, which the guard matches as a substring. The new column is additive, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/app/api/user/export/route.ts",
      "migration": 284,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a column of the same name on a different table or the word in a comment, which the guard matches as a substring. The new column is additive, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/server/account-erasure.ts",
      "migration": 284,
      "reason": "Does not read the column this migration adds: the file names the table and, separately, a column of the same name on a different table or the word in a comment, which the guard matches as a substring. The new column is additive, so this code runs unchanged before and after the migration."
    },
    {
      "file": "apps/web/lib/services/managed-memory-context-service.ts",
      "migration": 284,
      "reason": "Reads and writes the column this migration adds. The production deploy runs the migration verify step, which refuses to promote while a migration is pending, so this code cannot reach production before the migration is applied."
    }
  ]
}

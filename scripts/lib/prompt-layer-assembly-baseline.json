{
  "schemaVersion": 1,
  "unresolved": {
    "apps/web/app/api/chat/conversations/[id]/messages/lib/generate-follow-ups.ts": "one fixed prompt for an internal follow-up call that carries no user, project or workspace instruction layer",
    "apps/web/app/api/chat/conversations/[id]/messages/lib/generate-title.ts": "one fixed prompt for an internal title call that carries no user, project or workspace instruction layer",
    "apps/web/app/api/llm/v1/chat/completions/lib/context-compaction.ts": "summarises an existing transcript with a fixed prompt; the summary re-enters a request the processor has already resolved",
    "apps/web/app/api/llm/v1/chat/completions/lib/context-window.ts": "replaces an oversized system message in place and contributes no layer of its own",
    "apps/web/app/api/llm/v1/chat/completions/lib/research-loop.ts": "adds the file sources block to a request the processor resolved before the loop started",
    "apps/web/features/chat/pages/WebChatPage.tsx": "client composer; what it sends reaches the server as request messages, and the server resolves the layers",
    "apps/web/lib/hooks/useChatStream.ts": "client stream; the style instruction it sends reaches the server as a request system message, and the server resolves the layers",
    "apps/web/lib/server/scanned-document-text.ts": "an optical character recognition call with a fixed prompt and no user instruction layer",
    "apps/web/lib/services/managed-memory-context-service.ts": "renders the recalled memory block; the request processor tags its layer at the call site",
    "apps/web/lib/services/model-memory-extraction.ts": "a fact extraction call whose prompt comes from the manifest and carries no user instruction layer",
    "apps/web/lib/services/scheduled-agent-executor.ts": "runs a saved agent whose system prompt is the agent definition the account stored",
    "apps/web/lib/support/agent/answer/model-route.ts": "the support agent answers from its own manifest prompt and reads no account instruction layer",
    "packages/ai/providers/ollama/src/translate.ts": "a provider adapter putting an already assembled request onto the wire",
    "packages/platform/local-llm/src/multimodal.ts": "an on-device helper call with a caller-supplied prompt and no account instruction layer",
    "packages/platform/local-llm/src/tier2.ts": "an on-device helper call with a caller-supplied prompt and no account instruction layer"
  }
}

{
  "name": "@agiworkforce/apply-patch",
  "version": "0.0.1",
  "description": "Applies patches through a privileged filesystem mutation path with explicit bounds.",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}

{
  "name": "@agiworkforce/trust-boundaries",
  "version": "0.0.1",
  "description": "Classifies AGI-owned Managed Cloud hosts. Pure classification; performs no network access.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}

{
  "name": "@agiworkforce/compliance",
  "version": "0.0.1",
  "description": "Provider disclosure and jurisdiction policy used by the published legal set.",
  "private": true,
  "license": "PROPRIETARY",
  "type": "module",
  "exports": "./src/index.ts",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {
    "@agiworkforce/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}

{
  "$comment": "Which API routes do not reach the account and tenant gate, and why. scripts/check-route-account-gate.mjs resolves every route's authentication through the import graph and fails on an entry that no longer matches, on an undeclared route that skips the gate, and on any route that authenticates a user without it.",
  "public": {
    "apps/web/app/api/agents/execute/route.ts": "a retired managed-execution endpoint that answers every caller the same way",
    "apps/web/app/api/completion/route.ts": "a retired managed-execution endpoint that answers every caller the same way",
    "apps/web/app/api/connectors/directory/icon/route.ts": "the connector directory is the same catalogue for every caller",
    "apps/web/app/api/connectors/directory/route.ts": "the connector directory is the same catalogue for every caller",
    "apps/web/app/api/csrf/route.ts": "mints a CSRF token for an anonymous session before any account exists",
    "apps/web/app/api/download/checksums/route.ts": "published release artefacts are the same for every caller",
    "apps/web/app/api/download/route.ts": "published release artefacts are the same for every caller",
    "apps/web/app/api/health/route.ts": "a liveness probe that reads no account data",
    "apps/web/app/api/help/search/route.ts": "the help centre index is public content",
    "apps/web/app/api/mcp-apps/sandbox/route.ts": "serves the static sandbox document and reads no account data",
    "apps/web/app/api/mission/route.ts": "static product copy served to every caller",
    "apps/web/app/api/models/route.ts": "the model catalogue is the same for every caller",
    "apps/web/app/api/plugins/[id]/route.ts": "the plugin directory is public catalogue content",
    "apps/web/app/api/plugins/route.ts": "the plugin directory is public catalogue content",
    "apps/web/app/api/pricing/localized/route.ts": "list pricing localised by request region, with no account data",
    "apps/web/app/api/releases/[target]/[version]/route.ts": "published release metadata is the same for every caller",
    "apps/web/app/api/releases/cli/latest/route.ts": "published release metadata is the same for every caller",
    "apps/web/app/api/releases/desktop-cloud/latest/route.ts": "published release metadata is the same for every caller",
    "apps/web/app/api/releases/latest/[platform]/route.ts": "published release metadata is the same for every caller",
    "apps/web/app/api/support/handoff/availability/route.ts": "reports whether live support is staffed right now",
    "apps/web/app/api/v1/providers/[providerId]/stream/route.ts": "a retired managed-execution endpoint that answers every caller the same way",
    "apps/web/app/api/version/route.ts": "the build version string, identical for every caller"
  },
  "machine": {
    "apps/web/app/api/code/sessions/[sessionId]/provider-proxy/[...path]/route.ts": "a sandbox-scoped provider proxy token minted for one code session is the principal",
    "apps/web/app/api/cron/drain-audit-streams/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/drain-background-jobs/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/enforce-billing-collection/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/enforce-billing-retention/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/enforce-domain-retention/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/enforce-workspace-retention/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/evaluate-model-rollout/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/evaluate-slo-burn/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/expire-memories/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/expire-organization-invitations/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/expire-support-handoffs/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/health-probe/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/index-retrieval-documents/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/page-security-anomalies/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/purge-deleted-accounts/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/purge-deleted-media/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/purge-deleted-organizations/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/purge-security-audit-logs/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/purge-soft-deleted-resources/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/purge-temporary-chats/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reap-agent-runs/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reap-code-turns/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reclaim-sandboxes/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reconcile-billed-plans/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reconcile-credits/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reconcile-provider-costs/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/recover-reservations/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/refresh-connector-directory/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/refresh-plugin-directory/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/replicate-object-backups/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/report-enterprise-usage/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/reset-credits/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/roll-up-product-metrics/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/cron/run-schedules/route.ts": "the platform scheduler authenticates with the cron secret and no tenant is on the request",
    "apps/web/app/api/github/webhook/route.ts": "the GitHub app webhook signature over the raw request body is the principal",
    "apps/web/app/api/media/video/openrouter-webhook/route.ts": "the provider callback signature is the principal for an asynchronous media job",
    "apps/web/app/api/mobile/iap/apple-notifications/route.ts": "an App Store server notification signed by Apple is the principal",
    "apps/web/app/api/mobile/iap/google-notifications/route.ts": "a Google Play Pub/Sub push identity token is the principal",
    "apps/web/app/api/scim/v2/Groups/[groupId]/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/scim/v2/Groups/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/scim/v2/ResourceTypes/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/scim/v2/Schemas/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/scim/v2/ServiceProviderConfig/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/scim/v2/Users/[userId]/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/scim/v2/Users/route.ts": "a workspace SCIM bearer token is the principal, and it refuses a locked-down workspace at authentication",
    "apps/web/app/api/stripe-webhook/route.ts": "the Stripe signature over the raw request body is the principal",
    "apps/web/app/api/webhooks/connectors/[triggerId]/route.ts": "the connector trigger signing secret is the principal; the workspace comes from the trigger row",
    "apps/web/app/api/webhooks/gmail/route.ts": "a Google Pub/Sub push identity token is the principal",
    "apps/web/app/api/webhooks/google-calendar/route.ts": "the calendar channel token is the principal; the workspace comes from the trigger row",
    "apps/web/app/api/webhooks/slack/route.ts": "the Slack request signature is the principal; the workspace comes from the trigger row"
  },
  "sessionLifecycle": {
    "apps/web/app/api/auth/logout/route.ts": "revokes the caller's own device session; gating it would strand the live tokens of exactly the accounts a suspension wants cut off",
    "apps/web/app/api/auth/set-token/route.ts": "exchanges a verified provider token for the session cookie every gated route then reads",
    "apps/web/app/api/auth/device/token/route.ts": "exchanges an approved device code for a token that every later request is gated on",
    "apps/web/app/api/auth/device/refresh/route.ts": "rotates a device refresh token; the token it mints is gated on every use",
    "apps/web/app/api/device/poll/route.ts": "the device code is the credential here, and the token it returns is gated on every use"
  }
}

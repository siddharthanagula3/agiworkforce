{
  "name": "@agiworkforce/guardian-core",
  "version": "0.0.1",
  "description": "Guardian findings pipeline: policy evaluation, fingerprinting, dedupe, ranking, and verification, independent of any delivery surface.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./fingerprint": "./src/fingerprint.ts",
    "./config": "./src/config.ts",
    "./verify": "./src/verify.ts",
    "./dedupe": "./src/dedupe.ts",
    "./rank": "./src/rank.ts",
    "./policy": "./src/policy.ts",
    "./adapters": "./src/adapters/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}

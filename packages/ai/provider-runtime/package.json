{
  "name": "@agiworkforce/provider-runtime",
  "version": "0.0.1",
  "description": "One streaming client for every provider: egress, retry, failover, fallback and watchdog.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./decisions": "./src/typesafe-decisions.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {
    "@agiworkforce/types": "workspace:*",
    "@agiworkforce/agent-core": "workspace:*",
    "@typesafe-ai/sdk": "0.6.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}

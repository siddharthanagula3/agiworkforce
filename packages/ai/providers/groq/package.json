{
  "name": "@agiworkforce/providers-groq",
  "version": "0.0.1",
  "description": "Groq API adapter behind @agiworkforce/provider-protocol.",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:live": "AGIWORKFORCE_LIVE_TEST=1 vitest run --passWithNoTests",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {
    "@agiworkforce/providers-openai": "workspace:*",
    "@agiworkforce/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}

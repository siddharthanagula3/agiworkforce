{
  "name": "@agiworkforce/observability",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "ErrorReportingAdapter port, a scrub step, and a Sentry envelope adapter shared by browser-adjacent surfaces",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}

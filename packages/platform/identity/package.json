{
  "name": "@agiworkforce/identity",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "IdentityProvider port with a Clerk adapter, a browser-safe session-cookie entry and a testing adapter. The host app supplies the provider SDK from its own dependencies; see the note in src/adapters/clerk.ts for why this package must not declare it.",
  "exports": {
    ".": "./src/index.ts",
    "./browser": "./src/session-cookie.ts",
    "./testing": "./src/testing.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}

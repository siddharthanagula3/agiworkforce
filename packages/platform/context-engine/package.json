{
  "name": "@agiworkforce/context-engine",
  "version": "0.0.1",
  "description": "The one engine every context source is resolved through: per-source permission and policy checks, per-source token budgets, cross-source dedup, staleness, and a replayable per-turn manifest.",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --cache --cache-strategy content --cache-location .cache/eslint --max-warnings=0"
  },
  "dependencies": {
    "@agiworkforce/context": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}

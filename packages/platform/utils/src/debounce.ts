/**
 * Debounce / Throttle Utilities (re-exports)
 *
 * This module re-exports debounce and throttle from `async.ts` for convenience.
 * The implementations live in `async.ts` alongside other async helpers.
 *
 * @module debounce
 * @packageDocumentation
 */

export { debounce, throttle } from './async';

# Retired as already resolved

328 items were found in the source audits, checked, and judged already resolved — recorded here rather than deleted, so a future reader can see what was retired and why instead of rediscovering it. Nothing here needs work unless you disagree with the call.

- CRIT-017 'No CI secret scanner existed' — dropped as a standalone row; resolved (scripts/check-secrets.mjs added and blocking in ci.yml) but folded into SEC-01, which carries the proven residual gaps (no Supabase pattern, history not scanned) and its own residual (release bundles unscanned, REL-004/REL-010).
- known-flaws BOUNDARY-01 'apps/packages/services import-boundary rule' — resolved and CI-guarded; no source contradicts.
- known-flaws BYOK-HANDOFF-CONSENT-01 '/continue-with-byok leaked privacy mode without consent' — resolved; the consent flow is now called on /byok, /fork-byok and /continue-with-byok. The remaining consent gap on a different path is tracked separately as SEC-61.
- known-flaws AUDIT-IMMUT-01 'security_audit_logs freely mutable by app_rls' — resolved; migration 0043 applied to prod after branch rehearsal, app_rls verified INSERT+SELECT-only. The doc-accuracy half (SECURITY.md immutability claim) is carried in SEC-54.
- known-flaws EXEC-POLICY-DUP-01 'duplicate exec-policy engine' — resolved by deletion; CLI evaluation now uses the agiworkforce-execpolicy crate uniformly. The live defect in the surviving crate is SEC-22.
- known-flaws SSRF-DNS-01 'dormant proxy crate with fail-open DNS check' — resolved by deletion after zero-consumer proof; its explicit residual ('broader absence of OS-level egress enforcement') is folded into SEC-05.
- known-flaws SVC-GATEWAY-MANAGED-GATE-INVERTED-01 'gateway managed-compute kill-switch had inverted semantics vs web' — resolved 2026-07-15, aligned byte-identical to web's semantics.
- known-flaws WEB-APIKEY-CSRF-BLOCK-01 'issued sk-prefixed API keys blocked by CSRF check' — resolved; isBearerTokenValid now cryptographically verifies sk-prefixed bearers. A different sk-token defect (RLS rejection masked as a 503) is live and tracked as SEC-44.
- known-flaws WEB-AUTH-BEARER-COOKIE-PRINCIPAL-DIVERGENCE-01 'Bearer header and cookie could resolve to different principals' — resolved 2026-07-16; a present Bearer header is now authoritative in getClerkAuthUser.
- known-flaws WEB-CONVERSATIONS-PROJECTID-NO-OWNERSHIP-01 'project-scoped conversation creation lacked ownership check' — resolved 2026-07-18.
- known-flaws CLI-HOOK-01 'PreToolUse hook decisions must be honored by every exec path' — resolved and guarded.
- known-flaws CLI-PERM-01 'cached command permissions must evaluate whole command before program fallbacks' — resolved and guarded; the surviving newline-chaining defect in the same file is SEC-23.
- known-flaws CLI-DAEMON-AUTO-APPROVAL-01 'daemon triggers set unattended auto-approval on a pseudo model ID' — resolved 2026-07-14.
- known-flaws DESKTOP-TERMINAL-SANDBOX-NOT-APPLIED-TO-AGENT-EXEC-01 'agent-invoked terminal_execute bypassed manual-terminal sandbox preferences' — resolved 2026-07-15; the agent path now resolves the same TerminalSandboxPreferences and allowed directories.
- ExecutionPlan #5 'three rustls-webpki TLS advisories suppressed with a false justification' — resolved 2026-08-09 (70fb7ce90); the false 'pinned by Tauri transitive deps' note was removed and the advisory step made blocking. The broader advisory-policy debt is SEC-36.
- ExecutionPlan #6 'db_query SQL table allowlist bypassed by whitespace/comment tokenization' — resolved 2026-08-09 (9e40b17a8); sql_identifier_tokens confirmed present and used at all documented call sites.
- ExecutionPlan #7 'desktop project/memory sync not scoped to the account' — resolved 2026-08-09 (3cda52588).
- ExecutionPlan #8 'extension memory/tab-group commands inherit permissive default policy' — resolved 2026-08-09 (f1276c88d, armed in e76a93011). The one deliberately-retained residual is SEC-62.
- ExecutionPlan #9 'extension storage key retyped in six places; 8 dead shortcut actions' — resolved 2026-08-09 (bfce749b3).
- ExecutionPlan #10 'Safe/Plan mode gate unreachable; approve-and-remember permanently defeats it' — resolved 2026-08-09 (a86d150f7); is_tool_permitted_for_mode confirmed called before approval at tool_confirmation.rs:1582.
- ExecutionPlan #11 'TS secret scanner misses five patterns the Rust CLI catches' — resolved 2026-08-09 (f9a04c858) for the redaction pattern set. The sinks that do not apply that set are tracked as SEC-48.
- ExecutionPlan #12 'VS Code extension sends DB passwords in the git diff to the model' — resolved 2026-08-09 (64195ee0f).
- ExecutionPlan #13 'CLI AGI_API_URL bypasses SSRF allowlist and leaks bearer token' — resolved 2026-08-09 (2182c07be).
- ExecutionPlan #14 'desktop SSRF guard uses string prefixes, misses CGNAT/0.0.0.0/8/multicast' — resolved 2026-08-09 (80e8048e8); the audit's decimal-IP claim was a false positive (the url crate canonicalizes first).
- ExecutionPlan #15 'VS Code gateway validator permits plaintext http://localhost' — resolved 2026-08-09 (74690353b).
- ExecutionPlan #16 'CLI rules file is a persistent prompt-injection channel' — resolved 2026-08-09 (d16a0df18); agent writes to rules and commands are now denied. The absence of a general envelope is SEC-29.
- ExecutionPlan #17 'Local mode leaks prompts to managed cloud via image/video generation' — resolved 2026-08-09 (00afb5349).
- ExecutionPlan #18 'desktop web-search results reach the model with no injection fence' — resolved 2026-08-09 (272fc24bd) for that one channel; the missing cross-surface envelope is SEC-29.
- ExecutionPlan #35/#36 'web logout leaves tokens in storage' and 'apiFetch sends ciphertext as the bearer token' — resolved 2026-08-09 (e5d0727b9, 2ac7e148a). The at-rest plaintext-storage half is SEC-49.
- ExecutionPlan #45 'desktop IPC allowlist bypassed by tauri-mock; stale in both directions' — resolved 2026-08-09 (be38f2cf4).
- ExecutionPlan #59 'capability toggles fail open; dead feature-flag key' — resolved 2026-08-09 (ac20a2962); is_enabled no longer unwrap_or(true) and sync failures no longer report success.
- ExecutionPlan #60 'provider host allowlists: three hand-typed copies, one functionally short' — resolved 2026-08-09 (ac20a2962). The unresolved deduplication half (#61) is folded into SEC-05.
- ExecutionPlan #88 'ciphertext envelopes carry no key id or version' — the envelope half resolved 2026-08-09 (664df8b69, key_version now travels inside the envelope, verified); the KMS/escrow/rotation-runbook residual is carried by SEC-40.
- ExecutionPlan #98 'shared UI hand-parses desktop private localStorage to pick a trust label' — resolved 2026-08-09 (c6dc19e52).
- ExecutionPlan Chrome-audit 'EXT-OPTIONS-LOGOUT-NOOP: options page Log Out never signed the user out' — resolved; clearAuthToken()/signOutClerk() are now called.
- ExecutionPlan 'E2B vendor name leaked to end users via tool description, error text and raw SDK tracebacks' — resolved; redactSandboxVendor() scrubs hostnames, sandbox ids and the env var name on every path out of routeExecutionTool while logs keep raw text.
- AuditRemediationLedger 'Phase-9 runtime acceptance matrix (9D) blocked on an unpushed argon2 fix' — the blocker itself is resolved: ExecutionPlan #1 records the argon2 tracing fix landing 2026-08-09 (d4cc8e8e5) with outputFileTracingIncludes verified present. The remaining 9D-not-run item is infra/CI scope, not security.
- gap-audit §8 'Web Sentry PII scrubbing' — reported resolved (delegates to @sentry/nextjs with centralized scrubbing, default-disabled without a production DSN). The one uncontradicted remaining sub-path, beforeSend not scrubbing exception messages, is carried in SEC-48.
- gap-audit §8 'Execution-sidecar approvals — approval view is imported and rendered in the mounted desktop sidecar' — resolved; no source contradicts.
- ui-gaps GAP-002 'Desktop requires task-scoped consent before local tools access new folders' — resolved; path extraction, canonical resolution, protected-path rejection and explicit consent before dispatch are in tool_executor/mod.rs.
- ui-gaps GAP-008 'Full-access sandbox selection requires confirmation and complete risk disclosure' — resolved; AgentExecutionSettings intercepts all three unsandboxed transitions with a cancelable danger dialog.
- ui-gaps GAP-011 'Bypass permissions mode enabled with no consent modal or risk copy' — resolved; agentModeConsent.ts is the sole agent.mode write boundary and persists versioned consent only while granted.
- ui-gaps GAP-124 'Max reasoning plus Bypass Permissions requires compound-risk consent' — resolved; agentModeConsent.ts owns both the base and compound-risk boundaries.
- ui-gaps GAP-101 'Live native tool approvals support Return to approve and Escape to deny' — resolved; McpToolConfirmationPrompt installs a modal-scoped key handler.
- ui-gaps GAP-123 'Chrome uses approved sites and per-action approval instead of inert category or full-CDP controls' — recorded wontfix by product decision; the disclosure gap it leaves is carried in SEC-63 rather than re-raised here.
- ui-gaps GAP-105 'MFA gate' and GAP-115 'passkey and multi-device controls' — recorded wontfix pending account-service contracts that publish verified MFA state; not defects in current scope.
- WEB-CHAT-CHECKOUT-01 (known-flaws) — resolved and uncontradicted: in-chat upgrade CTAs route to real Stripe checkout, and when STRIPE_CHECKOUT_ENABLED is off the route returns an honest 'not available yet' error rather than faking success.
- CLOUD-01 (known-flaws) — resolved/superseded by the 2026-06-27 public-alpha decision; AGI_MANAGED_COMPUTE_PRIVATE_BETA is now only an incident kill-switch. The 'metering/fraud/refund/retention must keep pace' obligation it carried is captured in BILL-33 and BILL-35.
- GATEWAY-METERING-IDEMPOTENCY-01 (known-flaws) — resolved: managed billing double-charge risk fixed in code and migration 2026-07-15, requiring a stable client operation key and RLS-bound reservation before provider execution; migrations 0060-0066 confirmed applied to production by a later entry.
- SVC-MANAGED-USAGE-0056-DEPLOY-SEQ-01 (known-flaws) — resolved: migrations 0043 and 0052-0059 applied to production Neon in order after a green branch rehearsal.
- ExecutionPlan #20 (gateway path enforces no usage caps) — resolved 2026-08-09 (715077ba3); caps now passed to reserve_managed_usage_request.
- ExecutionPlan #21 (client disconnect mid-stream settles as failed and bills zero) — resolved 2026-08-09 (71872ffdc); abandoned managed streams now settle the output the client actually received.
- ExecutionPlan #22 (Cloud Code agent turns bill a flat 25c regardless of usage) — resolved 2026-08-09 (94046227f). The separate approval-state-machine defect in the same module is an ai-routing item, not billing.
- ExecutionPlan #23 (every E2B sandbox second free because the compute rate env var ships unset) — resolved 2026-08-09 (715077ba3); now fails loud in production instead of metering zero.
- ExecutionPlan #25 (Team seat purchase never reconciles licensed_seats) — resolved 2026-08-09 (9f36c2d1a). The residual policy work is folded into BILL-21.
- ExecutionPlan #26 (no refund path, refunds did not revoke plan, unregistered Price 500s renewals) — resolved 2026-08-09 (3b5c5f43a). Residual verification is folded into BILL-19 and BILL-25.
- ExecutionPlan #27 (rate limits flat across 122 configs) — recorded resolved, but the gateway leg is contradicted by known-flaws and is retained as BILL-52.
- ExecutionPlan #28 (scheduled-task tier quotas exceed total cron capacity ~10x) — resolved 2026-08-09 (d061dccc3); the remaining cron-cadence half is an infra/hosting item, not a quota-contract defect.
- ExecutionPlan #33 (cache pricing diverged between desktop and web; surcharges triplicated) — resolved 2026-08-09 (4023f46f9). The measurement gap it exposed is retained as BILL-12.
- ExecutionPlan #30 (account erasure materially incomplete on web — 34-entry list omitting many user-scoped tables) — resolved 2026-08-09 (04c8aa9c3); the erasure list is now derived and covers 66 tables. The desktop equivalent is still open as DPDP-06.
- ExecutionPlan #31 (delete-account route treated any DB error as 'columns missing' and hard-deleted, then falsely claimed no data removed) — resolved 2026-08-09 (3a9d5c271).
- ExecutionPlan #86 (no record that any user accepted the Terms of Service, making the arbitration clause, class-action waiver and liability cap unenforceable) — resolved 2026-08-09 (7aa633875); clickwrap and a terms-acceptance record now exist, pinned by terms-acceptance-migration.test.ts.
- ExecutionPlan #87 (restored PITR data not re-erased; no suppression/tombstone list, contradicting the published DPA) — resolved 2026-08-09 (7aa633875). The broader propagation gap is retained as DPDP-37.
- ExecutionPlan #92 (undisclosed subprocessor Expo and stale store listings advertising 'Hobby — $5/mo') — resolved 2026-08-09 (664df8b69). The remaining store privacy-declaration mismatch is retained as DPDP-19.
- ExecutionPlan #85 (EU AI Act disclosure wired on only one surface, backed by erasable device-local storage) — recorded resolved 2026-08-09 but contradicted by the 2026-08-14 removal of the web disclosure; retained as DPDP-02 rather than dropped.
- WEB-TIER-NAMING-HOBBY-STALE-01 (known-flaws) — verified fixed: grep for 'Hobby' across apps/web/app/privacy/page.tsx, terms/page.tsx and refund-policy/page.tsx returns no match, so the retired tier name no longer leaks into legal copy. The models/route.ts tier-taxonomy divergence it also mentioned is covered by BILL-14.
- FoundersAssistance #27 (six ACTIVE live-mode Stripe prices contradicting published pricing) — resolved: all six archived and confirmed, zero ACTIVE live prices remain, env vars repointed at verified test-mode IDs. The un-revoked Prices-Write permission it left behind is folded into BILL-02.
- FoundersAssistance #28d (top-ups hardcoded to USD regardless of the subscriber's regional currency) — fixed 2026-08-14: the route now reads the live subscription currency and refuses non-USD subscribers, failing closed if Stripe is unreachable. The undecided INR top-up unit price is folded into BILL-41.
- GAP-014 (Restore purchases reports every terminal outcome and offers retry) — resolved; useIapPurchaseFlow returns restored/none/failed/account-changed after store reconciliation.
- GAP-015 (Mobile prevents cross-platform duplicate subscription purchases) — resolved; plan.subscription_source is defined in the contract and emitted from persisted billing state. The unresolved ownership-precedence policy is retained as BILL-28.
- GAP-013 (Mobile change-email handoff), GAP-030 (inline Cloud capability preferences), GAP-042 (account security state), GAP-110 (web bulk chat and link management), GAP-118 (archived chats manager), GAP-033 (account Cloud data export before deletion) — all resolved per audit/ui-gaps.md with cited implementing files.
- GAP-019, GAP-024, GAP-047, GAP-113, GAP-117 (mobile/web plugin installation and unified directory declined until an account-bound marketplace lifecycle exists) — wontfix product decisions on the entitlement surface; the underlying entitlement dependency is retained as BILL-54.
- GAP-115 (passkey and multi-device controls explicitly unavailable pending account contracts) and GAP-120 (trusted-contact escalation declined) — wontfix decisions already disclosed honestly in the shipped UI, with boundary tests pinning the copy.
- GAP-259 ('Improve the model for everyone' and 'Location' toggles intentionally removed as dead controls) — resolved by deliberate removal; the residual question of whether the notice still describes them is covered by DPDP-01 and DPDP-14.
- DPDP_PROGRESS O-14 (apply 0113/0114 to a branch DB and verify RLS) — resolved: applied through production with 26/26 behavioural checks.
- DPDP_PROGRESS §7.1 (consent_records documented as append-only while a default-privileges grant from 0037 left both new tables mutable) — resolved by migration 0116 with a REVOKE plus a BEFORE UPDATE OR DELETE trigger, proven behaviourally including an adversarial re-grant test.
- DPDP_PROGRESS §7.2 second-pass legal-copy fixes (false CI-blocks-on-Semgrep claim, false CodeQL claim, overstated Actions SHA-pinning, stale '34 user-scoped tables' figure, wrong frame-src 'none' CSP claim, two more false 'no transactional email system' claims in /dpa, unperformable waitlist email promise) — all resolved with the corrected text now live.
- DPDP_PROGRESS §7.3 third-pass fixes (/mobile/legal false HealthKit declaration, falsely named crash-monitoring and analytics providers, FAQ custom-endpoint capability claim, FAQ training-answer trust-boundary language, /help BYOK card exclusions, FAQ EU/UK residency and Enterprise SSO/SCIM state) — resolved, each verified by hand and corrected.
- DPDP_PROGRESS §7.2's 57 cut audit proposals (including the Apple App Store recipient mislabel, an API route-coverage figure and a flat US-only data-transfer statement) — recorded as wrong or already fixed; listed here so they are not silently re-raised.
- DPDP_PROGRESS accessibility contrast/focus/screen-reader rows — the evidence refuted the finding (a11y-report.json shows zero violations); the real defect was breadth, and the rows are now scoped with their limits published.
- PRIVACY-01 (Local-to-BYOK explicit-fork product lock) — resolved and CI-guarded; the trust-boundary enforcement defects it protects against (media.rs raw reqwest bypass, computer-use BYOK consent) have their primary home in the security slice.
- CAP-024 (scored moderation thresholds) — verification found text-classifier.ts already implements weighted rules with BLOCK/FLAG scoring and reporting.ts logs every block and flag, contradicting its Open status; the per-organization threshold half is folded into DPDP-31.
- Model-identity badge does not reflect server-side model substitution (phase4 PP-02, tagged billing) — dropped from this slice: the defect is a client failing to read the fallback/original_model the server already emits, whose primary home is ai-routing. Noted here because credit-driven fallback is one of its triggers.
- Generated videos are never persisted and CSP blocks video playback (phase4 PP-19, tagged billing) — dropped from this slice: the defects are media persistence and a missing CSP media-src directive, primary home web/media. The billing-visible consequence (a paid generation lost) is noted in BILL-46.
- API keys minted with 'Run inference' scope rejected and surfaced as a fake 'billing*unavailable' 503 (phase4 PP-29) — dropped from this slice: root cause is getUserScopedDb rejecting sk_live*/sk*test* bearers because they carry no signed sub claim for RLS, primary home security/API auth. The misleading billing error string is a downstream symptom worth correcting there.
- CAP-016 (provider batch tier) and CAP-021 (provider flex service tier) — dropped from this slice: both are routing-contract and provider-mapping work whose pricing arm is downstream; primary home ai-routing. The entitlement-clamping sibling CAP-020 is retained as BILL-53.
- CAP-009 (org plugin governance), CAP-010 (org skill policies), CAP-019 (org browse-domain policy) and CAP-036 (extension access enforcement) — dropped from this slice: gap-audit GAP-P0-010 groups them as stored-policy-not-enforced-at-execution-boundaries, whose primary home is security. The entitlement portions are retained as BILL-53 and BILL-54.
- CAP-052 artifact runtime bridge security gate (T1, T3, T5, T6, T8, RT-2, RT-3, RT-5b, RT-5c and the overall NO-GO) — dropped from this slice: primary home security. The billing preconditions that must be resolved before it ships are retained as BILL-57, and the fail-open limiter as BILL-58.
- O-19b (legacy plaintext TOTP secrets and a weak TOTP key derivation) and the remainder of O-19's untriaged security inventory (XOR stream cipher, desktop machine-key PBKDF2, api-gateway CORS defaults, missing sslmode enforcement, Clerk JWT authorizedParties, non-constant-time cron secret compare, Pino redaction, share tokens in error logs, Sentry exception scrubbing, plaintext localStorage bearers) — dropped from this slice: primary home security. ACCOUNT_STATUS_FAIL_OPEN is retained as DPDP-35 because it is the suspended-account entitlement gate.
- GAP-P0-007 (scheduled tasks degraded to one daily batch of at most ten runs) — dropped from this slice: primary home infra/backend. Its quota-contract consequence was already closed as ExecutionPlan #28.
- BASE-003 finding 6 (mobile paywall test asserts the stale 'Upgrade to Max' tier label) — dropped as a test-maintenance item whose primary home is mobile/testing; the underlying plan-identity coupling it illustrates is retained as BILL-14.
- Vercel Hobby plan constraints, publishing credentials, key escrow, backup/restore policy, alerting vendor, repository visibility and account custody (ExecutionPlan founder items 1-6, 8, 11, 12, 14) — dropped from this slice: primary home infra/ops and security.
- test.csv 'test item' (domain ui) — a test-harness artifact, not a real finding; it carries no evidence, source anchor, or code reference.
- WEB-CHAT-CHECKOUT-01 (in-chat upgrade CTAs route to real Stripe checkout, honest gating when disabled) — marked resolved/LOW in known-flaws.md with no contradicting source.
- WEB-APPSHELL-MOBILE-SIDEBAR-01 (persistent desktop sidebar unusable at narrow viewports) — fixed 2026-07-15 with matchMedia-gated modal drawer, backdrop, Escape, focus return, aria-expanded; no source contradicts.
- WEB-CONNECTOR-FLOW-DEADENDS-01 (GitHub connector flow had 4 dead ends) — fixed 2026-07-11 per known-flaws.md.
- WEB-ROUTE-PROD-SCHEMA-MISMATCH-01 (/api/chat/folders route and stack deleted as dead/schema-mismatched) — fixed-by-deletion 2026-07-16.
- WEB-STAR-ARCHIVE-NONPERSIST-01 (Star/Archive were client-only) — fixed 2026-07-17, migration 0059 applied to prod.
- WEB-PROJECT-DETAIL-CHATLIST-UNPOPULATED-01 (project detail read a never-populated conversationIds field) — fixed 2026-07-18.
- WEB-CONVERSATIONS-PROJECTID-NO-OWNERSHIP-01 (project-scoped conversation creation lacked ownership check) — fixed 2026-07-18.
- WEB-PROJECT-UPLOAD-DUPLICATION-01 (two drifted project-source upload views) — consolidated 2026-07-18.
- WEB-CORE-LEGACY-DEADCODE-LAYER-01 (44-file dead apps/web/core subgraph) — deleted across two passes 2026-07-16/17.
- WEB-SYNC-PAGE-STALE-WAITLIST-01 (sync page showed a stale waitlist CTA for a live feature) — fixed 2026-07-11.
- WEB-IMAGE-CHAT-PERSISTENCE-01 (composer 'Create image' never persisted the chat record) — fixed 2026-07-11.
- WEB-MEMORY-INJECTION-PRODUCTION-PATH-VERIFIED-01 and WEB-PROJECT-KNOWLEDGE-GROUNDING-VERIFIED-01 — both recorded as audit false positives; the production paths are wired.
- UI-TAILWIND-SOURCE-01 (Tailwind v4 @source globs missed shared UI packages) — resolved in known-flaws.md.
- UI-MODEL-GATING-01 (dead appMode-gated ModelPopover superseded by live ModelSelector) — resolved; production path confirmed through the correctly gated selector.
- UI-WEBSEARCH-TOGGLE-01 (composer web-search toggle was a dead control) — resolved 2026-07-15, and subsequently superseded by the deliberate removal of the manual toggle in favour of ambient search (the residual indicator gap is kept as UI-04).
- UI-AGENTMODE-DEFAULT-01 (Agent Mode defaulted to auto-edit rather than ask-first) — resolved 2026-07-15.
- DESKTOP-WEB-UI-PRIMITIVES-DUPLICATED-01 (apps/web/components/ui duplication) — a later 2026-07-15 narrative entry confirms apps/web/components/ui no longer exists and ~128 files consume @agiworkforce/ui directly.
- EXT-DUPLICATE-MODULE-FORKS-01 (six duplicate dead module forks in the Chrome extension) — resolved; confirmed pure dead code via grep across src and **tests**.
- EXT-OPTIONS-LOGOUT-NOOP (options page Log Out never signed the user out) — resolved 2026-07-21.
- EXT-POLICY-PRIVILEGED-MSG-WEB-REACHABLE (privileged tab/cookie/chat messages reachable from any allowlisted page) — resolved 2026-07-21 with a regression test; the retained REPLAY_SHORTCUT decision is a security-slice item.
- Chrome extension message-policy and storage-key findings (ExecutionPlan #8, #9, #49) — resolved 2026-08-09 (f1276c88d, e76a93011, bfce749b3, 9f640a392).
- Extension onboarding slash-command finder used a separate built-in list (HARD-016) — recorded as fixed in the ExecutionPlan 'Closed — do not re-report' table; side_panel.ts now points at Workflows.
- Extension chat sessions were device-local only, never synced to the account — resolved; the extension now mirrors eligible Managed Cloud conversations through the shared client with fail-closed provenance checks.
- VSCODE-CYCLEMODE-KEYBIND-DEAD-01 (Shift+Tab Cycle Agent Mode keybinding dead) — resolved.
- VSCODE-MODEL-DEFAULT-MISMATCH-01 (fresh-install setting/config fallback/Quick Pick/webview unified onto a single 'auto' alias) — resolved 2026-07-25.
- VS Code webview action-status used an undefined error-color token — resolved; the webview now uses the canonical danger token.
- Web streaming re-render performance (ExecutionPlan #69: markdown reparsed per token, transcript rebuilt per chunk, 7.3s profile) — resolved 2026-08-09 (ac20a2962) via React.memo, useMemo on preprocessMath, and stable plugin arrays.
- Web time-to-first-token sequential round trips (ExecutionPlan #67) — resolved 2026-08-09.
- Web i18n adoption gaps on device-auth, billing toasts and WebChatPage (ExecutionPlan #73) — resolved 2026-08-09 (ac20a2962).
- Chrome extension had no i18n infrastructure at all (ExecutionPlan #76) — resolved 2026-08-09 (664df8b69); VS Code i18n infrastructure (#77) resolved 2026-08-09 (49d509f47).
- Greeting shouted the profile name in ALL CAPS, and the sidebar showed a truncated all-caps name — both resolved via normalizeGreetingName and the centralized normalizeDisplayName/resolveAccountDisplayName/accountInitial helpers.
- Video generation in-flight state was an invisible blank card (label hidden behind motion-safe:opacity-0) — resolved; retained as evidence under UI-15 for the sweep it motivates.
- Map card geocoded to the wrong continent; image and video composer mode dropped back to text on every send; every assistant reply labelled 'Unavailable model' under Auto routing; 'E2B' vendor name leaked to end users; 'Plugins' menu row rendered as visually disabled; Projects page used the marketing palette; Library card Delete floated in the grid gutter; an empty assistant turn looked identical to a lost response; file/image attachment upload broken by a clipped menu row, missing R2 CORS, and a stretched chip — all resolved in the 2026-08-11..14 demo-readiness cycle recorded in ExecutionPlan.md.
- GAP-109/110/111/114/118/121 (Web Tasks per-task detail, Data Controls bulk management, device-auth consent identity, Reduce sensitive content enforcement, archived-chats manager, Voice settings honesty) — all recorded as resolved with named implementing files in audit/ui-gaps.md.
- GAP-011/012/124/125/126/127/129/130/132/136/138 (VS Code bypass-permissions consent, branded settings editor, compound-risk consent, plan-owner identity, browse-the-web context source, first-use disclosures, four-step onboarding, agent config discovery, custom instructions, capability availability descriptor) — all recorded as resolved.
- GAP-112/113/115/116/117/119/120/122/123/128/131/133/134/135/137 (web and extension rows) — explicit product decisions marked wontfix in audit/ui-gaps.md with recorded rationale; the ones that still carry a live capability gap were folded into WEB-31, EXT-05 and EXT-12 rather than dropped.
- CAP-002 (CarPlay/Android Auto), CAP-026 (unprompted proactive suggestions), CAP-047 (in-chat commerce and checkout) — recorded as Not Planned founder decisions in audit/capability-gaps.csv, not defects.
- CAP-024 moderation thresholds — the ledger row reads Open, but source verification found text-classifier.ts already implements weighted BLOCK/FLAG scoring (not toggle-only regex) and reporting.ts logs every block and flag; the residual per-organization scoping belongs to the security/compliance slice.
- Web logout leaving auth/refresh tokens in storage, apiFetch sending ciphertext as bearer, and persisted-store key collisions (ExecutionPlan #34, #35, #36) — resolved 2026-08-09 (e5d0727b9, 2ac7e148a, migrations 0099/0100); primarily security-slice items in any case.
- BASE-006 (stale google*batch*\* wiring allowlist entries) — resolved and independently re-verified: grep -c google_batch apps/desktop/wiring-allowlist.json returns 0. No source contradicts.
- BASE-007 (desktop wiring guard not verified green from a clean checkout) — resolved: node apps/desktop/scripts/check-wiring.mjs exits 0 with 1272 registrations, 1268 frontend calls, 1270 Rust command definitions, 4 orphan + 65 reachability reviewed exemptions.
- BASE-003 finding 6 (mobile paywall test asserts a stale tier label) — resolved and verified in this pass: apps/mobile/**tests**/paywall-bottom-sheet.test.tsx:170 now asserts 'Upgrade to Max 5x', matching the canonical catalog label.
- MATCH-011 (Desktop system prompt names nonexistent memory_add) — resolved and verified: grep for memory_add in apps/desktop/src-tauri/src/core/agent/prompt_engineer.rs returns no matches; ExecutionPlan item #51 records the fix (ab1a77e79).
- HARD-014 / DESKTOP-LOCAL-PROVIDER-TRUST-CLASSIFICATION-DRIFT-01 (local provider IDs hardcoded to 'ollama') — resolved at the contracts layer and verified: LOCAL_PROVIDER_IDS in packages/contracts/types/src/model-catalog.ts:1081 is now derived from REGISTRY_HARNESSES by trustModes.includes('local'), so LM Studio, llama.cpp and vLLM are classified correctly. The separate claim of ~6 residual provider==='ollama' comparisons in production code was not re-confirmed and would be a small follow-up, not a standing defect.
- DESKTOP-EMBEDDED-INFERENCE-DEAD-SCAFFOLDING-01 (llama-cpp-2 dependency) — resolved as a confirmed non-issue: the crate sits behind an unused local-llm Cargo feature with zero imports; no embedded inference ships.
- DESKTOP-WINDOW-STATE-INVISIBLE-LAUNCH-01 — resolved: originally logged Critical, retracted after the original repro could not be reproduced via an authentic path.
- DESKTOP-BYOK-STALE-TESTKEY-TRUSTBOUNDARY-01 — resolved: retired OpenAI models appearing in Local mode traced to stale QA test data, not a trust-boundary leak.
- DESKTOP-RUST-ORPHAN-01 (23 unreachable Desktop Tauri .rs files) — resolved via pnpm check:module-reachability; superseded by DESK-05, which covers the remaining TypeScript-side orphans.
- CLI-TUI-ORPHAN-01 (~370 orphan .rs files, 108K LOC in the CLI TUI) — resolved; the one residual dead file, subagent_v2.rs, is tracked separately as CLI-06.
- LOCAL-CHAT-NOINVOKE-01 (Tauri desktop never invoked the local Ollama model on send) — resolved, confirmed via Ollama server logs showing no request.
- DESKTOP-CHAT-SILENT-FAIL-01 (first Local-mode send silently failed, 3 compounding bugs) — resolved, reproduced end to end via a real native session.
- DESKTOP-SETTINGS-BILLING-RENDER-LOOP-01 (Settings→Billing crashed the Settings modal 100% of attempts) — resolved via live-interaction WDIO QA.
- DESKTOP-TERMINAL-SANDBOX-NOT-APPLIED-TO-AGENT-EXEC-01 — resolved: the agent path now resolves the same TerminalSandboxPreferences and allowed directories as the manual terminal.
- DESKTOP-MCP-DOTFILE-CONFIG-FAKE-SUCCESS-01 (two parallel MCP config UIs, one a fake-success dead end) — resolved 2026-07-03.
- DESKTOP-PROJECT-SCOPING-UNWIRED-01 (project-to-chat scoping dead at three stacked seams) — resolved 2026-07-17, all three seams landed and unit-tested. PLAN.md still lists 'desktop project scoping after W7' in its resume queue, which reads as a stale line rather than a genuine contradiction.
- DESKTOP-PLANSMODAL-WAITLIST-STALE-CTA-01 — resolved 2026-07-16; paid CTAs now open the real web pricing/Stripe checkout page.
- DESKTOP-PLAN-TIER-DISPLAY-STALE-01 and DESKTOP-PLAN-TIER-DUPLICATE-OWNER-01 — both resolved 2026-07-15; planTier removed from appModeStore (store version 3 sanitizes legacy state) and the pricing modal now reads the backend-populated plan.
- DESKTOP-V3-COMPOSER-DEADCODE-01, DESKTOP-SIDEBAR-SEARCHMODALCMDK-DEAD-CODE-01, DESKTOP-VOICE-DICTATION-STORE-MISMATCH-01 — all resolved by deletion or consolidation with zero-importer proof.
- DESKTOP-SONNER-TOASTER-UNMOUNTED-01, DESKTOP-SONNER-TOAST-LIGHT-THEME-ONLY-01, DESKTOP-SIDEBAR-COLLAPSE-NOT-PERSISTED-01, DESKTOP-MARKDOWN-NO-HEADING-01, DESKTOP-CLOUDROLLBACK-01, DESKTOP-TOOLTIMELINE-01, DESKTOP-WDIO-COMPOSER-MOUNT-FLAKY-01, DESKTOP-WDIO-FIXED-PORT-CONTENTION-01, DESKTOP-CHAT-CONVO-ACTIONS-PERSIST-01 — all resolved UI and test-infra defects with no contradicting source.
- DESKTOP-SCHEDULER-WEEKLY-DECAY-CRON-01 (invalid 6-field cron string meant memory_weekly_decay never registered since ship) — resolved 2026-07-15.
- DESKTOP-OLLAMA-TOOL-INJECTION-TIMEOUT-01 (111 tools serialized into every send to a non-tool-native local model) — resolved 2026-07-11.
- DESKTOP-RUST-ECONOMY-ROUTING-DRIFT-2026-07-10, DESKTOP-RUST-THINKING-DISABLE-TEST-2026-07-10 and DESKTOP-ECONOMY-ROUTING-01 — resolved via SSOT derivation, test update and regression restore.
- DESKTOP-BYOK-OPENROUTER-VERIFY-01 and DESKTOP-BYOK-PROVIDER-UI-COVERAGE-01 — resolved (the latter as a corrected overclaim; BYOK provider coverage is partial by design).
- DESKTOP-MEMORY-DECAY-BRIDGE-HARDCODED-01 — resolved 2026-08-05: stateBridge.ts bridgeMemoryStore now calls getDecayConfig() instead of a hardcoded false, with a regression test.
- DESKTOP-EFFORT-TESTS-CATALOG-DRIFT and the v76 realtime_metrics migration guard (the 4 cargo test --workspace --lib failures) — resolved 2026-08-01 per known-flaws.md: the v76 CREATE INDEX gained a table_exists guard and the effort-catalog tests were updated to follow the catalog by founder decision. PLAN.md's '12 stale-model-ID routing_logic_tests reds' line appears partially stale as a result.
- check:llm-operability RED on three missing mobile feature READMEs (archived-chats, reflect, team) — resolved same day by commit 528ba8bc3; a fresh run exits 0 (34/34).
- MOBILE-JEST-REGRESSION-2026-07-10 — resolved 2026-07-11; full mobile jest suite green (174 suites / 1744 tests).
- MOBILE-CONNECTORS-ROUTE-THEATER-01 (fake hardcoded 11-item catalog on all 3 mobile chat entry points) — resolved 2026-07-11. The missing regression guard is retained as MOB-14.
- MOBILE-PRESSABLE-CSSINTEROP-FLEXDIR-01 (NativeWind flex-direction rendering bug, 6 further live instances) — resolved 2026-07-11 via a repo-wide sweep.
- MOBILE-IOS-PREBUILD-DRIFT-01 — resolved 2026-07-16; the dead tracked root ios/ project was deleted after exhaustive proof it had no build script, EAS config or CI reference.
- MOBILE-GUARDRAILS-01 (mobile no-hex lint guardrails) — resolved; the flagged literals are official connector brand colors.
- MOBILE-PROVIDER-SWITCH-GATE-DIVERGENCE-01 (mobile granted mid-thread provider switching to tiers other surfaces deny) — resolved 2026-08-05 by delegating to the canonical canSwitchProviderInThread.
- Mobile bottom-sheet library rendered nothing under RN 0.86.2/Fabric — resolved by upgrading @gorhom/bottom-sheet to 5.2.14. The unaudited sheet contents this unblocked are retained as MOB-20.
- Mobile image aspect ratio never sent to the route; generated media rendered above its own prompt (timestamp tie-break); every reply read aloud in typed chats (auto-TTS mis-gated on the microphone preference); raw FetchRequestCanceledException tracebacks shown to users — all four resolved in the 2026-08-13 media-generation pass.
- CLI-HOOK-01, CLI-PERM-01, CLI-DAEMON-AUTO-APPROVAL-01, BYOK-HANDOFF-CONSENT-01 and EXEC-POLICY-DUP-01 — all resolved CLI security defects, now guarded. The residual exec_policy.rs rename is retained as CLI-16.
- UI-CLI-PALETTE-ARG-01, UI-CLI-STATUSBAR-MODE-01, UI-CLI-PICKER-LOCAL-01, UI-CLI-EFFORT-REASONING-01, UI-CLI-OVERLAY-DOUBLEBORDER-01 and UI-CLI-TOOLDROP-SILENT-01 — all resolved CLI TUI defects.
- CLI slash-panel overlays (/title, /skills-toggle, /memories, /diff-review, statusline) — resolved; the root-cause overlay-Submit-drop was fixed the same day and all five overlays were subsequently built as real features with regression tests ('ALL 7 CLI HIGH RESOLVED').
- ExecutionPlan items #6, #7, #13, #16, #17, #40, #41, #44, #45, #46, #48, #51, #53, #55, #56, #57, #58, #59 — desktop, CLI and mobile defects all recorded DONE with commit hashes and no contradicting source (db_query allowlist tokenization, account-scoped project/memory sync, CLI AGI_API_URL SSRF, CLI rules-file injection channel, Local-mode media egress, unemitted Tauri events, AgentMode vocabularies, IPC allowlist/tauri-mock bypass, code_search guard, max-iterations clamp, prompt memory tool, shortcut sets, hardcoded model IDs, max-token defaults, timeout constants wiring, tier vocabularies, capability fail-open).
- GAP-001 through GAP-138 with status 'resolved' (desktop and mobile) — roughly 70 UI and capability gaps recorded as shipped with named implementing files: mobile Skills catalog, task-scoped folder consent, recorder HUD, Connections pairing, Cowork Dispatch lifecycle, archived-chats sidebar, full-access sandbox confirmation, desktop memory controls, mobile email handoff, restore purchases, duplicate-subscription prevention, Chats history, continuity explainer, global search, Library documents and search, manual pairing code, scheduled-task templates, Capabilities preferences, memory controls, data export, language selector, notification categories, parental controls, permissions, connector detail screen, Safety controls, account security, terminal dock, empty-chat quick actions, composer context controls, recorder narration, UI scale, reduce motion, memory policy controls, MCP workspace, send behaviour, notification scopes, shortcut conflict guidance, native approval keyboard handling, empty-capture recovery, settings search index and pairing instructions. None is contradicted by another source.
- CAP-024 (scored moderation thresholds) — the ledger marks it Open/Wire but code verification found text-classifier.ts already implements weighted rules with BLOCK_THRESHOLD/FLAG_THRESHOLD scoring and reporting.ts logs every block and flag. Dropped from this slice as apparently built at platform level; the per-organization threshold half belongs to the compliance/enterprise slice.
- CAP-002 (CarPlay and Android Auto) and CAP-005 (first-party inference kernels) — explicit Not Planned product decisions with no code implication; recorded here so they are not re-raised as mobile or desktop gaps.
- CAP-016 (provider batch tier) — the actionable half is removing preview-only Google batch code; the allowlist arm is already clean (0 google_batch entries) and the doc arm was fixed by deleting examples/google-batch-api.ts. The residual is one inert metadata line at packages/ai/model-registry/catalog/harnesses.json:118-124 naming 'adapter':'desktop-google-batch' for a deleted module, mirrored into 3 generated registries — a one-line catalog cleanup, not a standing defect.
- CAP-022 (meeting transcription) and CAP-051 (design workspace whiteboard mount) — net-new capability decisions rather than defects; their unmounted-code aspect is covered by DESK-05. CAP-032, CAP-038 and CAP-042 are folded into DESK-05; CAP-029 and CAP-043 into DESK-39; CAP-049 into DESK-51. Recorded here so the individual CAP ids are not lost.
- docs/agent-context/bug-finding-guide.md — contributed zero items; it is a pure process and workflow guide (search patterns, workflow steps) with no findings, TODOs or open items of its own.
- BASE-001 (no baseline record existed) — resolved; baseline recorded 2026-08-09. Its live residue (two divergent baselines, 8 unverified commits) is preserved in INFRA-47.
- BASE-002 (commands.json missing Rust/security gates) — resolved; rustClippy, rustFmtCheck, rustDependencyPolicy, rustDependencyAdvisories, secretScan, dependencyAuditJs, rustTest and rustTestSingle added and verified.
- BASE-005 (evidence-directory convention) — resolved as NOT APPLICABLE; docs/current/agent-and-repo-operability.md already names audit/ and reports/, enforced by check-repo-organization.mjs and check-report-retention.mjs.
- BASE-006 (stale google*batch*\* wiring allowlist entries) — resolved as NOT APPLICABLE; grep -c google_batch on apps/desktop/wiring-allowlist.json returns 0. The separate catalog-metadata residue is kept as AI-03.
- BASE-007 (desktop wiring guard not verified from clean checkout) — resolved; check-wiring.mjs exits 0 with 1272 registrations, 1268 frontend calls, 1270 Rust command definitions.
- BASE-009 (no script failed release completion on open ledger tasks) — resolved; scripts/check-audit-progress.mjs added and wired into launch-readiness-check.sh step [9/9], verified present.
- CRIT-017 (no CI secret scanner) — resolved; scripts/check-secrets.mjs plus secret-scan-allowlist.json added and blocking in ci.yml, verified present, 8366 files scanned. Its residue (release bundles not scanned as artifacts) is covered by INFRA-16's signing/checksum requirement.
- MATCH-001 (origin_surface contract accepts a value the DB rejects) — resolved by ExecutionPlan #37; the Zod schema and SQL CHECK constraint were reconciled. The general class is preserved as TEST-05.
- MATCH-004/005/006/007/009 (canonical route paths retyped across surfaces) — resolved by ExecutionPlan #47, which consolidated 13 non-test literal re-typings of the managed-cloud chat path and the mobile shadowed constant. The missing route-literal guard that would prevent recurrence is kept as INFRA-36.
- MATCH-011 (desktop system prompt names nonexistent memory_add tool) — resolved by ExecutionPlan #51; the prompt now names the real registry tools.
- MATCH-012 (z-index token scale unused) — resolved by ExecutionPlan #99; design-tokens.ts zIndex now has a matching --z-\* block and the eight overlay components consume it. UI slice in any case.
- HARD-007 (desktop 30-second timeout duplication) — largely resolved by ExecutionPlan #57, which wired constants/timeouts.ts (previously zero importers) into the four modules that execute them. Residual literals fold into INFRA-36.
- HARD-009 / HARD-010 / HARD-012 / HARD-015 / HARD-016 (300ms debounce, 10-second shadow constants, page sizes, keyboard-shortcut arrays, extension slash-command list) — resolved by ExecutionPlan #65, #53, #45 and the extension onboarding fix. The missing magic-number and duplicate-symbol guards are preserved as INFRA-36 and the pagination-contract concern as INFRA-26.
- HARD-017 (mobile connector-catalog hardcoding regression guard) — resolved; MOBILE-CONNECTORS-ROUTE-THEATER-01 was fixed 2026-07-11 and ExecutionPlan's closed-findings table confirms no hardcoded catalog remains.
- HARD-018 (no provider/internal-endpoint literal guard) — resolved; scripts/check-no-hardcoded-endpoints.mjs verified present with tests. Its scan-root gap is preserved in AI-01 and AI-02.
- MATCH-008 (half the routing config in vercel.json inert) — resolved by commit 438e154d4; the regression it created (five host-scoped routes emptied) was caught by check:ci-guardrails and fixed in 8af15d594. The stale guard owner is kept as INFRA-11 and the still-broken host serving as INFRA-12.
- SCALE-GROW-004 (opaque cursor pagination) — resolved; zero OFFSET-based occurrences remain under apps/web/app/api.
- MODELS-CURATION-DRIFT-01 — resolved (fixed, recurred once, re-fixed). The recurrence risk it demonstrates is carried in AI-02.
- WEB-PROVIDER-DRIFT-01 — resolved 2026-07-15; all TS leaf-adapter construction now dispatches through @agiworkforce/providers-factory.
- GOOGLE-SSE-CRLF-FRAMING-01 — resolved 2026-07-10; parseGeminiStream split only on double-LF so entire responses fell into the trailing buffer.
- TOOLLOOP-ANTHROPIC-THINKING-CONTINUITY-01 — resolved; extended-thinking continuity restored when the tool loop migrated to packages/ai/providers/anthropic.
- MCP-APPROVAL-RESUME — resolved 2026-07-09; the v1 tool loop's fail-closed awaiting_approval with no resume mechanism was fixed. The distinct Cloud Code approval gap is kept as AI-29.
- MODEL-COMPACT-OPENAI-TIER-UNVERIFIED-01 — resolved 2026-07-14; catalog identity and tier corrected after live verification.
- DESKTOP-ECONOMY-ROUTING-01 — resolved; the taskRouting regression from a catalog cleanup commit was restored.
- DESKTOP-RUST-ECONOMY-ROUTING-DRIFT-2026-07-10 and DESKTOP-RUST-THINKING-DISABLE-TEST-2026-07-10 — resolved 2026-07-10 via SSOT derivation and a stale-test update.
- DESKTOP-EFFORT-TESTS-CATALOG-DRIFT and the v59/v76 realtime_metrics migration-test failures — resolved per known-flaws' 2026-08-01 verification-battery section (table_exists guard added; effort tests updated per founder decision to follow the catalog). PLAN.md's line describing 12 stale-model-ID reds is stale.
- ExecutionPlan #63 (second uneligible routing engine and drifted Rust Auto-router) — recorded resolved 2026-08-09. The remaining canonical-resolver question survives in AI-08 and AI-10.
- ExecutionPlan #54 (image generation calls three nonexistent model IDs) — REVERTED rather than resolved, because every symbol the fix added was unreachable; the underlying work is folded into AI-01's provider-registry dependency.
- ExecutionPlan #55 and #56 (hardcoded model IDs and max-token defaults across 10-11 desktop/CLI files) — resolved 2026-08-09 (d16a0df18). The recurrence guard gap is kept as AI-02.
- ExecutionPlan #57 (desktop timeouts.ts with zero importers) — resolved; see HARD-007 note above.
- ExecutionPlan #58 (duplicated tier/plan vocabularies) — resolved 2026-08-09; billing-slice adjacent in any case.
- ExecutionPlan #60 (provider host allowlists: three hand-typed copies, one functionally short) — resolved 2026-08-09; egress-policy.ts was missing five hosts present in the canonical list.
- ExecutionPlan #67/#68/#69/#70 (TTFT sequential round trips, RLS 6-round-trip adapter, per-token markdown reparse, cloud-sync sequence and unindexed ILIKE search) — resolved 2026-08-09. The systemic verification they do not provide is kept as INFRA-27.
- ExecutionPlan #71 (scheduled tasks executing at most 10x/day with no requeue) — recorded as already fixed by the wave-2 rewrite, but GAP-P0-007's later verification of vercel.json and WAVE_CLAIM_LIMIT contradicts it, so the item is retained as INFRA-33 rather than dropped.
- ExecutionPlan #72-#78 (i18n adoption across shared UI, web, desktop, mobile, Chrome, VS Code, CLI, plus the key-parity and mobile hex guardrails) — recorded resolved 2026-08-09, but contradicted by known-flaws and DPDP O-15, so retained as unclear in DOCS-19.
- ExecutionPlan #79 (GDPR e2e suite skipping itself green: 15 tests, 38 skip calls, 6 tautological guards) — resolved 2026-08-09. The class is preserved as TEST-03 and the skipped-test inventory as TEST-01.
- ExecutionPlan #80 (signup → checkout → entitlement had zero E2E coverage) — resolved 2026-08-09; preserved as context under TEST-02.
- ExecutionPlan #82 (nothing can page a human) — code half resolved via the health-probe cron and runbook; the unresolved vendor half is retained as INFRA-24.
- ExecutionPlan #83 (no AI output quality evals) — recorded resolved with tools/evals and evals.yml, both verified present, but contradicted by PLAN.md's 'no eval corpus exists', so retained as unclear in AI-11.
- ExecutionPlan #88 (ciphertext envelopes carry no key id or version) — resolved 2026-08-09; envelope.ts now documents key_version travelling inside the envelope. The remaining KMS and escrow decision is a security-slice/founder item.
- ExecutionPlan #90 (provider suspension not failover-eligible) — resolved 2026-08-09; the rotator now covers credential failures rather than only connection, server, overload, capacity, timeout and rate-limit errors.
- ExecutionPlan #95 (no down-migrations across 98 migrations) — resolved 2026-08-09; verified apps/web/db/neon/down/ exists with paired .down.sql files.
- ExecutionPlan #99 (z-index scale defined and never used) — resolved; see MATCH-012 note.
- OBSERVABILITY-WEB-SENTRY-01 (apps/web Sentry client a pure no-op stub) — resolved; both ExecutionPlan and gap-audit §8 record that apps/web/shared/lib/sentry.ts now delegates to @sentry/nextjs with centralized PII scrubbing and default-disabled behaviour when no production DSN is configured.
- NETLIFY-PROXY-01, CI-TIER-SCRIPTS-01, CI-INSTEP-REDS-01, CI-RUST-AUDIT-01, DX-NODE-01, NEON-01, CI-GUARD-01, WORKSPACE-SCRIPT-01, MAP-COMPLETE-01, AGENT-DOC-01, ORG-ROOT-01, ORG-TOOL-01, ORG-GENERATED-01, DOC-DRIFT-01, DX-README-01, CODEOWNERS-01, DOC-CLAIM-01, BOUNDARY-01 — all resolved/guarded rows in the known-flaws table with no source contradicting them; each names a gate that now exists and passes.
- WEB-CLOUD-AGENT-RUNS-MIGRATION-UNAPPLIED-01 and WEB-DEPLOY-SEQ-MIGRATION-AHEAD-OF-CODE-01 — resolved 2026-07-21 (migrations 0060-0066 applied and matching code deployed). The recurring class is retained as INFRA-23.
- E2B-NO-ORPHAN-SWEEPER — resolved; lib/e2b/reclaim.ts plus a daily authenticated cron now reap paused sandboxes. The narrower transient-reconnect orphan is retained as INFRA-45.
- CAP-005 (first-party inference kernels) — Not Planned by explicit product decision; provider and local-runtime adapters remain the intended execution seam. Recorded here so it is not re-raised as a gap.
- tiers-openapi-seats-minimum-stale and the two positive-control entries (checkout MIN_PURCHASABLE_SEATS import, context_monitor.rs DEFAULT_CONTEXT_WINDOW fallback) from ExecutionPlan's 'Closed — do not re-report' table — resolved or non-defects, listed so they are not re-flagged.
- 'Overall ledger completion output was AUDIT_REMEDIATION_INCOMPLETE' — dropped as a status readout rather than a defect; its two named blockers are carried as TEST-01 and INFRA-03.
- docs/agent-context/bug-finding-guide.md — contributed zero items; it is a process guide (workflow steps, ripgrep patterns, a 'do not trust blindly' list) containing no findings, TODOs or open items.
- 'A headline audit deliverable still asserts a stale, refuted security claim (voice_inject_text BROKEN, injects into password fields)' (AuditCompleteness.md §4.1) — source itself marks it resolved: the claim was refuted in the same round by VOICE-MEDIA-012 (system_dictation_available() is hardcoded false, the injectText chain has zero callers), and it is a correction to a retiring audit file. The real residual defect is carried forward as SEC-82.
- 'Custom MCP import supports URL-based add only, not raw-JSON-config import' (agentic-modes-gap-14) — FIXES-APPLIED.md reports raw-JSON MCP config import shipped, Zod-validated, feeding the existing inspect→add pipeline so it inherits the server-side security boundary.
- 'Custom connector and Plugin removal bypassed the confirmation dialog that catalog connectors correctly use' (CPS-03) — FIXES-APPLIED.md reports both routed through the shared useConfirm() primitive; the remaining un-confirmed disconnect in the modal's ConnectorsPanel is an integrations-domain item, not a security one.
- 'No per-model tier-access matrix on the marketing pricing page' (G1/mqp-01) — FIXES-APPLIED.md reports the matrix now derived from tierAllowedModels in the canonical registry rather than hand-typed.
- 'Pricing comparison table has no training-data-use disclosure row' (G7/mqp-19) — FIXES-APPLIED.md reports an unconditional 'No' training-data-use row added, matching the existing policy in privacy/page.tsx.
- 'EU-AI-Act prohibited-practices list covered only 2 of 5 points and only on the mobile legal page' (legal-trust-01) — FIXES-APPLIED.md reports completion from 2 of 5 to 5 of 5 and relocation onto the canonical /acceptable-use page.
- 'Automated high-stakes decision-making clause omitted medical' (legal-trust-02) — FIXES-APPLIED.md reports medical added to the enumerated domain list.
- 'Sandbox retention disclosed vaguely (reclaimed once unreachable) versus the enforced 24-hour SANDBOX_MAX_AGE_MS' (legal-trust-06) — FIXES-APPLIED.md reports privacy copy now states 'within 24 hours', matching the enforced constant; the separate tier-differentiated-retention ambition remains covered by DPDP-16/DPDP-37.
- 'Tool-access-mode setting was fully dead — defined, never read or set' (settings-21-gap) — resolved by deletion: FIXES-APPLIED.md records toolAccessMode removed as dead code with zero readers and zero writers, rather than wired to a real control.
- 'Voice settings page is real and honest but has no nav entry' (settings-27-gap / SETTINGS-001) — FIXES-APPLIED.md reports a nav entry added via VoiceSection.tsx; a UI/settings-IA item in any case.
- 'Max 15x subscriber shown as Free with a $7 downgrade offered as Upgrade' (QA-004, P0) — audit/manual-qa-2026-08-15.md marks it FIXED and verified (billing-policy.ts returns false when unauthenticated, WebChatPage falls back to 'free' only when billingPolicyReady, UpgradePlanDialog's '= free' default removed; UpgradePlanDialog.test.tsx 4/4 and 89/89 across 9 billing-policy consumer suites). The unswept-consumer follow-up is carried forward as BILL-71.
- wire-or-cut.md resolved security/crypto cuts — dead SecurityManager-era surfaces already removed and verified: renderer-exposed Stripe command set (19 phantom billing commands + uncalled billing*initialize), shared subscription/email-credential/voice-cancellation command wrappers, native cloud CRUD placeholders returning ERR_CLOUD_NOT_IMPLEMENTED, legacy ai*\* and custom-updater command layers, 52 remaining orphan Tauri registrations, the uncalled Desktop sandbox-profiles module, and the phantom inbound Slack-app runtime with its credential fields. Nothing in wave 2 contradicts these cuts.
- wire-or-cut.md resolved security controls — verified shipped and not contradicted: unverified outer-isolation sandbox profile removed (it had let the CLI skip its own OS sandbox with no caller ever establishing an enclosing container); model-authored Desktop code networking converged on core/agi/SandboxManager with network forced off and launch refused when OS-level enforcement is missing; managed API keys now require explicit scopes with a default-deny on keys lacking a public-API scope; product crash/error reporting gated on production checks, explicit consent, PII scrubbing and Desktop Local/BYOK rejection; dangerous-tool approvals now await a real decision instead of returning a failed ToolResult before the user sees the prompt.
- wire-or-cut.md resolved privacy/compliance items — verified and not contradicted: per-project memory scope dropdown removed (a single-option select with no onChange, no state and no persistence, implying scoping that exists nowhere); sensitive-data memory exclusions enforced in persistManagedAutoMemoryFacts before any candidate reaches user_memories; project-knowledge duplicate detection now compares the stored checksum; per-project export endpoint added; conversation printing fixed so a virtualized transcript no longer prints a partial document that looks complete.
- wire-or-cut.md resolved fake-capability removals in the billing/entitlement area — verified: CLI /background and the placeholder task lifecycle removed (six deferred tools that only created empty output files and accepted caller-authored status transitions), desktop unread dock badge command removed (it only logged and returned success), and the bundled marketplace success stories removed (60 arrays containing 63 fabricated customer statistics such as 'reduced bugs in production by 60%' and 'Saved $100K', none sourced from telemetry).
- 'apps/desktop/archive/ (204 files) confirmed correctly isolated dead code' (DEAD-CODE-022) — source marks it resolved: excluded from tsconfig include and Vitest with zero imports from src/, and cited as the correct template for isolating confirmed-dead trees.
- CLR-01 / models-reasoning-quotas G2 — model-picker deprecation warning: RESOLVED per FIXES-APPLIED.md ('deprecation_date … now surfaces an advance warning, read from the catalog').
- CLR-02 / MEDIA-TITLE-03 / shell-nav-ia-gap-05 — conversation titles were permanent raw truncations: RESOLVED (LLM-generated two-stage titles, renameConversation got its first caller, a latent 405 PATCH bug fixed). Residual title race filed as WEB-85.
- CLR-04 — shared code-block copy button hover-gated and unreachable on touch: RESOLVED per FIXES-APPLIED.md (opacity-0 default removed). Note web's forked MessageBubble may still carry the old behaviour — tracked under UI-28.
- CLR-05 — 'Edit' prefilled the bottom composer instead of inline editing, and EditableMessage.tsx had zero importers: RESOLVED (fixes/01-web-chat-shell.json lists CLR-05 as fixed; EditableMessage wired into MessageBubble's user-message render path).
- search-deep-research G4 — no Reports gallery UI over the already-RLS-scoped /api/research/reports: RESOLVED per FIXES-APPLIED.md.
- search-deep-research G10 — report citation list missing favicons: RESOLVED per FIXES-APPLIED.md.
- orch-gap-01 (artifacts-10) — artifacts gallery reachable from nowhere and rendered in marketing chrome: RESOLVED (Artifacts rail entry added plus a new /chat/artifacts route mounting the same GalleryClient inside WebAppShell; /gallery keeps marketing chrome and SEO).
- orch-gap-02 (shell-03) — primary nav rail defined twice by hand, Tasks unreachable from /chat, isActive hardcoded: RESOLVED (both shells now consume apps/web/shared/components/layout/app-nav-items.ts; verified live as an identical 8-item rail with exactly one active entry).
- orch-gap-03 / SEARCH-RESEARCH-001 / search-deep-research G2 — Deep Research silently degraded to a single-turn fallback on Anthropic: RESOLVED (the provider !== anthropic exclusion removed after its premise was found stale; verified live). Note AI-13 still covers the free-trial fallback, stripped connector tools and uncitable project files.
- settings-27-gap / duplication settings-and-nav §2c — /settings/voice had real content but no nav entry: RESOLVED via a new VoiceSection.tsx injected through WEB_SETTINGS_NAV_GROUPS (deliberately web-only so Desktop Cloud does not get an empty Voice tab). The still-miswired collapsed-sidebar gear is filed as WEB-37; the same pattern for /settings/byok and /settings/sync is filed as WEB-50.
- shell-nav-ia-gap-01 (shell-07) — destructive actions used native window.confirm() while a styled AlertDialog existed: RESOLVED (delete conversation, delete project, delete message, archive all chats, permanently delete every chat, delete archived chats and plugin removal now route through the shared useConfirm(); WebAppShell.handleProjectDelete had no confirmation at all and was fixed). Residual connector-disconnect gap filed as UI-74.
- shell-nav-ia-gap-03 (shell-01) — composer placeholder never reacted to the Chat↔AGI Work axis: placeholder half RESOLVED; tier-visibility half explicitly declined as a prior product decision (AUDIT-FIX CMP-14). Remaining chrome-level ask folded into WEB-19.
- settings-21-gap — toolAccessMode setting defined with zero readers and zero writers: RESOLVED by deletion (DELETED_DEAD_CODE), not by wiring a dropdown. Recorded in UI-60's evidence; CPS-17's separate missing lazy-vs-eager context-load control is an integrations-slice item.
- CPS-03 — custom connector and plugin removal bypassed the confirmation dialog catalog connectors use: RESOLVED via the shared useConfirm() rollout. The un-confirmed ConnectorsPanel disconnect that survived it is filed as UI-74.
- CPS-08 (connectors-01) — mobile Skills screen unreachable from any nav: RESOLVED (Skills row restored to DrawerContent's PRIMARY_ITEMS; recorded as collateral damage from commit 1e858a7f1, not a decision). Same defect as EXTENSIBILITY-001 / SHELL-NAV-IA-003 / GAP-001.
- agentic-modes-gap-14 (agentic-20) — custom MCP import supported URL-only, no raw-JSON config: RESOLVED (Zod-validated raw-JSON import feeding the existing inspect→add security pipeline). Note it was added only to ConnectorsPage, the copy signed-in users never see — that drift is filed as WEB-53.
- MEDIA-VIDEO-01 — attachments staged before switching to image/video mode were silently discarded on send (a genuine data-loss bug): RESOLVED per FIXES-APPLIED.md. The remaining image-to-video source_image contract gap (VOICE-MEDIA-010) is a media-slice item.
- MEDIA-PLACEHOLDER-10 — image/video generation placeholders not pre-sized to the requested aspect ratio: RESOLVED, including a second-order height-cap bug the fix caught, verified by measuring DOM boxes before and after real generations.
- QA-001 — artifact panel toolbar controls overlapped (198px measured) because viewport breakpoints were used inside a fixed-width panel: RESOLVED via @container queries + overflow-hidden + min-w-0. The recommended repo-wide sweep for the same mistake is filed as WEB-99.
- QA-002 — interactive checklist card lost all tick progress on refresh: RESOLVED via conversation-id + content-hash localStorage persistence. The key-collision limit is filed as WEB-98.
- QA-004 — a Max 15x subscriber was shown as Free with a $7 downgrade offered as 'Upgrade': RESOLVED (billing-policy.ts now returns false early when unauthenticated; WebChatPage and UpgradePlanDialog gated on readiness; 89/89 consumer tests green). The unswept pattern is filed as WEB-62.
- duplication chat-shells[4] / library-artifacts-gallery[0] — GalleryClient mounted twice (public /gallery + in-app /chat/artifacts): DELIBERATE and correct (same component, chrome prop, robots noindex, canonical to /gallery, regression test asserting it is not a fork). Recorded as the template pattern for other surfaces.
- duplication library-artifacts-gallery[1] — Artifacts gallery and Library modelled as two disconnected products: DELIBERATE per app-nav-items.ts:101-112 (Claude-parity Artifacts vs ChatGPT-Library parity). The concrete defects underneath it are filed as WEB-34, WEB-35 and WEB-67.
- duplication library-artifacts-gallery[2] — in-chat docked ArtifactsPanel vs cross-conversation gallery: DELIBERATE; both filter off the same useArtifactsStore and render through the same ArtifactPreview, only the scope differs.
- duplication library-artifacts-gallery[3] — peripheral artifact-adjacent surfaces (marketing page, token-gated /shared-artifact/[token], dev QA harness, PublishedArtifactsSection revoke-list): DELIBERATE, correctly scoped distinct purposes. The stale qa-artifacts 'delete after QA' comment is filed as WEB-58.
- duplication components[3] — public + in-app Gallery page restated in the components axis: DELIBERATE, no action.
- duplication components[7] — Library built as a shared core plus thin platform adapters over one LibraryTransport interface: DELIBERATE and done correctly; recorded as the template the chat shell, artifact panel and project gallery duplications should converge onto.
- duplication extension-surfaces[3] — /features/plugins and /plugins both existing: DELIBERATE at the route-existence level (matches the site's /features/X marketing family). The narrative contradiction is filed as WEB-56.
- duplication extension-surfaces[4] — redirect-only alias routes into /apps, /connectors, /skills: DELIBERATE thin aliases, explicitly excluded from sitemap.ts. The dead ?tab=agents param is filed as WEB-57.
- duplication settings-and-nav §1a — nav-item array duplicated between WebChatPage and WebAppShell: RESOLVED same-day via app-nav-items.ts (APP_NAV_DESTINATIONS + buildAppNavItems, isActive derived from pathname). Recorded so the fix is not silently lost when the source is retired.
- wire-or-cut 2026-08-06 late web items — in-conversation search: RESOLVED (MessageSearch was complete but unexported/unmounted/unbound, and browser Cmd+F silently omitted the middle of virtualized transcripts; ChatMessageList now owns Cmd+F). Per-match highlighting filed as WEB-108.
- wire-or-cut 2026-08-06 late web items — composer budget indicator never rendered for a single user (BudgetTrackerDisplay guarded behind !inline while the only production mount always passes inline): RESOLVED.
- wire-or-cut 2026-08-06 — restore deleted conversations: RESOLVED (restore endpoint + Settings > Privacy 'Recently deleted'), deliberately making no retention promise since no purge job exists. The absent purge job is a compliance-slice item (DPDP-16/DPDP-37).
- wire-or-cut 2026-08-06 — print a conversation: RESOLVED (print action + stylesheet); the prior failure mode was that browser Ctrl+P silently omitted the middle of a virtualized transcript, producing a partial printout that looked complete.
- wire-or-cut 2026-08-06 — Web appearance settings: dead second theme control (AppearanceSettings.tsx, zero importers) CUT, and defaultModel/defaultModelTier/advancedMode/advancedModelId/chatFontSize deleted as store fields with zero readers and zero writers. Mounting them would have shipped cosmetically-wired inert controls.
- wire-or-cut 2026-08-06 — default model, reasoning effort, text size, code wrapping and shortcut settings: RESOLVED (default model and effort now write model-store.ts/thinking-store.ts, the stores the composer actually reads; text size and code wrapping ship with real stylesheet hooks, closing the earlier 'font-size preference has no CSS hook' item).
- wire-or-cut 2026-08-06 — web credit alert modal, advanced-mode toggle and connector card: all CUT as dead code (CreditAlertModal required fields no API returns; AdvancedModeToggle drove store fields with no reader; ConnectorCard had zero importers and a third copy of formatRelativeTime).
- wire-or-cut 2026-08-06 — per-message token/cost display: RESOLVED (TokenUsageDisplay mounted with real data from persisted messages.input_tokens/output_tokens, closing the earlier always-empty-widget risk).
- wire-or-cut 2026-08-06 — 'voice' preference stored an OpenAI TTS voice name that web read-aloud (browser SpeechSynthesis) could never use: RESOLVED, replaced with a device-local ReadAloudVoiceRow.
- wire-or-cut 2026-08-06 — project knowledge duplicate detection, aggregate storage quota, file version history, and .ipynb upload support: all RESOLVED. Related still-open register items WEB-17 and WEB-25 should be re-scoped against these.
- DEAD-CODE-022 — apps/desktop/archive/ (204 files): confirmed correctly isolated dead code, excluded from tsconfig and Vitest with zero imports from src/; recorded as the correct template for isolating confirmed-dead trees.
- parity-implementation-matrix vague 'Partial' rows for empty chat input, plus menu, file symbol/attachments, model dropdown, mic/voice, send/stop, recent chats, account menu, message bubbles, streaming states, message actions, feedback, artifact side panel, canvas/document editing, persistent artifact storage, web/desktop/mobile chat and artifacts columns: dropped as status-tracker rows with no specific defect. Every concrete defect they gesture at is carried by a named register entry (UI-01..UI-24, WEB-01..WEB-32) or by the new items above; re-deriving them as separate gaps would inflate the register with unactionable rows.
- AuditCompleteness.md §3.3, §3.1, §3.5, §4.1, §4.2 and GAP-P0-003; red-test-suites.md §1/§2; the breach-runbook and incident-response gap tables; and the CLI/enterprise/testing/docs coverage findings: out of this slice (testing, infra/ci, compliance and docs agents own them).
- DEAD-CODE-022 — apps/desktop/archive/ (204 files) confirmed correctly isolated dead code, excluded from tsconfig and Vitest with zero imports from src/; source marks it resolved and recommends only periodic CI confirmation.
- AuditCompleteness.md §4.1 — CurrentProductInventory.md:251's stale 'voice_inject_text is BROKEN, injects into password fields' claim; source marks it resolved (corrected by VOICE-MEDIA-012 to DEAD/P3), the document is being retired, and the residual code issue is captured as new item SEC-70.
- AuditCompleteness.md §4.2 — 'Chat is genuinely shared' framing stated without its primary-vs-secondary qualifier in CrossPlatformArchitectureAudit.md:196 and CurrentProductInventory.md:279; an internal inconsistency between audit deliverables that are themselves being retired, with no code defect behind it.
- CLR-04 — shared code-block copy button was hover-gated (unreachable on touch) in packages/ui/unified-chat MarkdownContent.tsx:52, which affected desktop via the shared package; FIXES-APPLIED.md confirms it is now always visible.
- CPS-03 — custom connector and Plugin removal bypassed the confirmation dialog; FIXES-APPLIED.md confirms both are now routed through the shared useConfirm() primitive.
- settings-21-gap — toolAccessMode/setToolAccessMode in the shared unified-chat settings store; resolved by deletion (outcome DELETED_DEAD_CODE) per FIXES-APPLIED.md. The six remaining dead field/setter pairs in the same store are carried forward as new item UI-31.
- wire-or-cut.md's ~60 'resolved' cut/wire decisions covering desktop, CLI, mobile and the extensions — dead planning-preview, LLM-council, image-gallery, workflow-marketplace (incl. 63 fabricated success-story statistics), renderer Stripe, legacy updater, legacy AI project/code, checkpoint-command and MCP-Tasks surfaces cut; execution sidecar, dangerous-tool approvals, Deep Research report/history, empty-chat scope, settings search, QR pairing, high-contrast support, desktop memory export, temporary chats and browser prompt-injection fencing wired. Recorded resolved in source with no contradicting evidence; the residual open threads they left behind are filed as DESK-86, DESK-100, DESK-101, DESK-102, SEC-72, SEC-73, CLI-20 and CONN-14.
- parity-implementation-matrix.md — 'Desktop developer sessions (CodeWorkspace) no longer missing from the shell'; CodeWorkspace is lazy-mounted in DesktopShellV3.tsx (Local-only, since 2026-08-04). The remaining remote projection is carried as new item DESK-99.
- frontend-experience-contract.md §13 correction (2026-08-09) — CLI voice previously mismarked Absent; apps/cli/src/voice.rs (cpal capture, Whisper API/local binary, Local-mode egress gate) is reached from both the REPL and the TUI via /voice, so the capability is present.
- frontend-experience-contract.md §14 P0 item 4 — Chrome Quick mode routing through the admitted auto-economy profile; completed 2026-07-16, travels with both side-panel send paths without mutating the saved model selection.
- parity-implementation-matrix.md generic 'Partial' rows for Desktop chat mode, Desktop sidebar, Desktop app controls and Desktop local compute host — no specific defect stated; each is already represented by existing register entries (DESK-33/34/35, DESK-44, DESK-53, DESK-07/08).
- parity-implementation-matrix.md generic 'Partial' rows for Mobile local-first onboarding, Mobile chat, BYOK handoff, approvals/continuity and heavy generation — no specific defect stated; covered by existing MOB-08, MOB-12, MOB-19, MOB-34 and BILL-44.
- parity-implementation-matrix.md generic 'Partial' rows for Chrome page-context capture, browser actions, workflow recording, native bridge and permissions/settings — no specific defect stated; covered by existing EXT-02, EXT-06, SEC-62, SEC-63 and DPDP-18.
- EXTENSIBILITY-001 / SHELL-NAV-IA-003 / CPS-08 (mobile Skills screen unreachable, prior art GAP-001) — RESOLVED: FIXES-APPLIED.md fixes/09-mobile-nav.json records the Skills row restored to DrawerContent.tsx's PRIMARY_ITEMS after the audit established its removal by commit 1e858a7f1 was collateral damage, not a decision. Note the drawer test asserting the row's absence must be inverted in the same change.
- agentic-modes-gap-14 (custom MCP import supports URL-add only, no raw-JSON config) — RESOLVED per FIXES-APPLIED.md fixes/11-connectors.json: raw-JSON MCP config import added, Zod-validated, feeding the existing inspect→add pipeline so it inherits the real server-side security boundary. CAVEAT preserved in CONN-27: the duplication audit found the feature was added exclusively to ConnectorsPage.tsx, the copy signed-in users essentially never see.
- CPS-03 (custom connector and plugin removal bypassed the confirmation dialog) — RESOLVED per FIXES-APPLIED.md fixes/11-connectors.json: destructive actions now routed through the shared useConfirm() primitive. Residual (the live modal's ConnectorsPanel disconnect is still unconfirmed, and the dialog was hand-copied three times) is carried forward as CONN-28.
- DEAD-CODE-022 (apps/desktop/archive/, 204 files) — RESOLVED / no action: confirmed correctly isolated dead code, excluded from tsconfig's include list and Vitest with an explicit 'Superseded... unreachable from main.tsx' comment and zero imports from src/. Recorded by the audit as the correct template for isolating confirmed-dead trees; only ongoing task is periodically confirming CI keeps archive/ excluded and unimported.
- AuditCompleteness.md §4.1 (CurrentProductInventory.md:251 asserts voice_inject_text is BROKEN and injects into password fields) — RESOLVED as a self-correction: a later, more rigorous pass in the same round (VOICE-MEDIA-012, P3) showed the claim is false — system_dictation_available() is hardcoded false so Global-source dictation is refused at admission, and the injectText/voiceInjectText action chain has zero callers. The underlying P3 hardening item (gate voice_inject_text itself behind system_dictation_available()) is desktop/voice, outside this slice.
- search-deep-research G2 / orchestrator orch-gap-03 (Deep Research silently takes the single-turn path on Anthropic, the default model) — RESOLVED per FIXES-APPLIED.md: the provider !== 'anthropic' exclusion in route.ts was removed after its premise was found stale, verified live on the Gemini Pro-tier catalog model the router selected producing a real plan list, elapsed timer, round counter and cited report. Recorded as adds_evidence on AI-13, whose free-trial-user half remains open.
- search-deep-research G4 (no Reports gallery UI despite the backend already supporting an unfiltered list) — RESOLVED per FIXES-APPLIED.md: a Reports gallery was added on the already-existing, already-RLS-scoped /api/research/reports endpoint that previously had no UI caller.
- search-deep-research G10 (report citation list missing favicons the sibling Sources tab already renders) — RESOLVED per FIXES-APPLIED.md.
- settings-12-gap / models-quotas G4 (per-task credit-debit ledger exists in credit_transactions but never reaches a settings screen) — RESOLVED per FIXES-APPLIED.md: credit-history ledger surfaced from the real credit_transactions table.
- settings-21-gap (toolAccessMode setting fully dead — defined, never read or set) — RESOLVED by DELETION per FIXES-APPLIED.md, not by wiring. Recorded in UI-27 as the accepted precedent for the remaining dead field/setter pairs, and in CONN-26 as the reason no such control exists in any form today.
- settings-27-gap / SETTINGS-001 nav half (voice settings page real and honest but absent from SETTINGS_NAV_GROUPS_WEB) — RESOLVED per FIXES-APPLIED.md via a deliberately web-only VoiceSection.tsx injection in WebSettingsModal.tsx, avoiding a no-content Voice tab in Desktop Cloud's shared settings array. Cited as the fix template for the still-open /settings/byok and /settings/sync gap (recorded against UI-11).
- shell-nav-ia-gap-01 (destructive-action confirmation inconsistently wired; the highest-frequency deletes used native window.confirm) — RESOLVED per FIXES-APPLIED.md: delete conversation, delete project, delete message, archive-all-chats, permanently-delete-every-chat, delete archived chats and plugin removal all routed through the shared useConfirm(); a genuine no-confirmation bug in WebAppShell.handleProjectDelete was found and fixed en route.
- shell-nav-ia-gap-03 placeholder half (composer Chat↔AGI Work toggle never changed the placeholder) — RESOLVED per FIXES-APPLIED.md. The tier-visibility half (canUseAgiWork hides the axis from free/basic tiers) was explicitly declined as a deliberate prior product decision (AUDIT-FIX CMP-14, prevents a control that hard-fails with agi_work_plan_required), not treated as a defect.
- shell-nav-ia-gap-05 / CLR-02 / agentic-modes-gap-06 / MEDIA-TITLE-03 (conversation titles were a permanent raw 50-60 char prompt truncation) — RESOLVED per FIXES-APPLIED.md: LLM-generated two-stage, fire-and-forget, sanitized titles, guarded so a user rename is never clobbered; renameConversation() got its first caller and a latent 405 PATCH bug was fixed en route; image/video conversations no longer bypass auto-titling. Known residual (an AUTO_TITLE_PLACEHOLDERS effect in WebChatPage.tsx races and re-truncates the generated title, mitigated server-side) is a web/UI item outside this slice.
- orchestrator orch-gap-01 (artifacts gallery complete and working but reachable from nowhere, rendering in marketing chrome) — RESOLVED per FIXES-APPLIED.md: an Artifacts rail entry plus a new /chat/artifacts route mounting the same GalleryClient inside WebAppShell; /gallery stays public with its SEO surface intact.
- orchestrator orch-gap-02 (the primary nav rail was defined twice by hand and Tasks was unreachable from /chat, the default screen) — RESOLVED per FIXES-APPLIED.md: both WebChatPage and WebAppShell now consume shared/components/layout/app-nav-items.ts with isActive derived from pathname; verified live as an identical 8-item rail on every route with exactly one active entry.
- agentic-modes-gap-09 (no execution-environment picker for local vs cloud vs remote-paired within one session) — NO ACTION by design: consistent with CLAUDE.md's explicit Local/BYOK/Managed Cloud trust-boundary separation. The audit's own recommendation is to NOT merge trust boundaries into one picker; if ever closed it must be an explicit consent-gated remote-pairing flow, not a unified dropdown.
- memory-06-gap (no recording/transcript memory corpus independent of chat memory) — NO ACTION recommended: the competitor feature this was measured against is itself flagged UNVERIFIED by the source research, and grep found no recording/transcript memory concept beyond dictation (speech-to-text input) and TTS read-aloud, neither of which stores or recalls transcripts.
- memory-10-gap (no memory-scope disclosure for a Live/full-duplex voice surface) — NO ACTION: the prerequisite surface does not exist. apps/mobile/src/features/voice/ is dictation input into standard text chat, not a voice-first conversational surface. Revisit only if such a surface ships, at which point the disclosure copy ships with it.
- duplication chat-shells[4] / library-artifacts-gallery[0] / components[3] (GalleryClient mounted twice at public /gallery and in-app /chat/artifacts) — NO ACTION: DELIBERATE and correct. Both routes render the identical component with a chrome prop, robots noindex plus canonical='/gallery' are set on the in-app mount, and a regression test asserts it is 'not a forked copy of the gallery'. Recorded by the audit as the template pattern the chat shell (UI-25), artifact panel (DESK-85) and project gallery (DESK-84) duplications should converge onto.
- duplication library-artifacts-gallery[1] (Artifacts gallery and Library modelled as two disconnected products) — NO ACTION on the split itself: app-nav-items.ts:101-112 documents it as a deliberate product decision (Claude-parity Artifacts vs ChatGPT-Library-parity Library). The concrete defects underneath it are carried forward as WEB-50.
- duplication components[7] (Library = shared core plus thin platform adapters) — NO ACTION: DELIBERATE and done correctly; web's and desktop's LibraryView adapters each carry a doc comment explaining they supply only what differs on their platform and delegate to the single shared LibraryView via a shared LibraryTransport interface. Cited by the audit as the reference pattern.
- duplication extension-surfaces[3] (/features/plugins and /plugins both existing) and [4] (five redirect-only alias routes into /apps, /connectors, /skills) — NO ACTION on route structure: DELIBERATE, matching the site's consistent /features/X marketing-page family pattern; sitemap.ts explicitly lists the redirect-only routes as intentionally excluded. The narrative contradiction between the two plugins pages is carried forward as DOCS-25.
- duplication settings-and-nav[0] resolved half (nav-item array duplication between WebChatPage and WebAppShell) — RESOLVED same-day via app-nav-items.ts; recorded here so the fact that this class of drift already occurred once is not lost when the source is retired. Prior drift: '/chat rendered 6 entries and /chat/library rendered 7, Tasks was missing from chat, isActive hardcoded wrong.'
- docs/adr/wire-or-cut.md — approximately 100 CUT and WIRED entries (desktop planning preview, LLM council, image gallery/presets, subscription and email/voice wrappers, renderer Stripe command set, native cloud CRUD placeholders, legacy ai\_\* and updater command layers, orphan debug/background/GitHub/doctor wrappers, one-off schema scripts, CLI /background and placeholder task lifecycle, unread dock badge, phantom Slack runtime, unverified outer-isolation sandbox profile, general multi-backend web cache, team approval escalation router, MCP Tasks RPC, orphaned a11y utility UI, workflow marketplace with 63 fabricated statistics, legacy research fork, AGI checkpoint command layer, visual editor, experimental surface, duplicate computer-use views, reasoning accordion, credit alert modal, advanced-mode toggle, connector card, data-retention setting, project memory-scope dropdown, dead voice preference, and the wired-in Deep Research report/history, execution sidecar, dangerous-tool approvals, temporary chats, browser prompt-injection scanning, sales tax, style sync, pre-send secret warning, in-conversation search, composer budget indicator, share-link lifetime, artifact version restore, per-message usage, copyright and model-licence pages, response reporting, conversation printing, project knowledge dedup/quota/versioning, notebook uploads, project export and duplication, camera capture, chat defaults, shortcut consolidation, memory exclusions, project templates, json_object enforcement, embeddings endpoint, mobile push delivery, notification email channel and schedule-completion notifications) — RESOLVED, no action. Recorded in bulk so the retirement of wire-or-cut.md is not read as losing open work; every open or partially-open item from that ledger has been extracted individually above.
- docs/agent-context/risk-map.json AUDIT-IMMUT-01 (audit log immutability) — RESOLVED: 0043_audit_log_immutability.sql revokes UPDATE,DELETE on public.security_audit_logs from app_rls and converts cleanup_old_security_logs() and delete_user_data(text) to SECURITY DEFINER so 90-day retention and GDPR erasure still purge; applied to prod Neon after a disposable-branch rehearsal with app_rls verified INSERT+SELECT-only. Explicit instruction preserved: do NOT re-derive the REVOKE, it exists. The two residues (the blanket GRANT re-grant footgun at 0037_rls_user_isolation.sql:81, and delete_user_data(text) having EXECUTE still open to public) are security/compliance items outside this slice.
- docs/current/frontend-experience-contract.md §13 correction note — two rows were previously mismarked and are RESOLVED: CLI voice is present, not absent (apps/cli/src/voice.rs with cpal capture, Whisper API/local binary and a Local-mode egress gate, reached from both the REPL and TUI via /voice), and Desktop developer sessions are mounted (CodeWorkspace lazy-mounted in DesktopShellV3.tsx, Local-only, since 2026-08-04). What remains missing for the latter is the remote projection, carried forward as AI-55.
- Chrome Quick mode routing through the admitted auto-economy profile (frontend-experience-contract.md §14 P0 item 4) — RESOLVED 2026-07-16: Chrome Quick now travels with both side-panel send paths and routes the turn through the admitted auto-economy profile without mutating the saved model selection.

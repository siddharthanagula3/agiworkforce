# Wire-or-Cut Decisions

Status: Active

Owners: Product integration and the owning surface

This ledger records decisions for code that implements or calls a product
capability but has no production entry point. The default is to cut dead code.
Each exception must identify a real runtime selection mechanism or a bounded
follow-up ticket.

## 2026-07-29: Desktop IPC Baseline

| Capability                                            | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Desktop planning preview                              | Cut      | `apps/desktop/src/api/planning.ts`, both planning preview components, and `planningStore.ts` were unmounted and invoked nonexistent `agent_preview_*` commands. The live research-plan experience remains in the web chat.                                                                                               |
| Desktop LLM council                                   | Cut      | `apps/desktop/src/api/council.ts` and `councilStore.ts` were referenced only by archived UI and invoked nonexistent `llm_council_query` / `llm_council_get_status` commands.                                                                                                                                             |
| Desktop image gallery/style presets                   | Cut      | The gallery, preset picker, and gallery store had no production import path; their generation call targeted a command that is not part of the native IPC contract. Supported image generation remains on reachable product paths.                                                                                        |
| Desktop slash `execute-plan` handler                  | Cut      | The handler was reachable only from the abandoned planning store and had no mounted chat entry point. Shared slash-command reconciliation is tracked separately in Ticket 1D.                                                                                                                                            |
| Shared subscription wrappers                          | Cut      | Five `desktop-command-client` subscription functions had no consumers and no matching registered native commands. Canonical checkout, plan management, and cancellation remain server-authoritative in the web billing boundary.                                                                                         |
| Shared email credential-migration wrappers            | Cut      | The two wrappers had no consumers and no matching registered native commands. The live database/keychain credential path is unchanged.                                                                                                                                                                                   |
| Shared voice cancellation/interrupt wrappers          | Cut      | The two wrappers had no consumers and no matching registered native commands. Existing registered voice playback and cancellation commands remain available through their real callers.                                                                                                                                  |
| Renderer-exposed Stripe command set                   | Cut      | Nineteen Tauri command functions had no frontend callers, `billing_initialize` had no startup caller, and both feature branches exposed a phantom API. Local renderer Stripe ownership was removed; web/API billing remains canonical, while the internal `BillingState` used by agent entitlement checks is retained.   |
| Native cloud CRUD and transfer commands               | Cut      | Six `cloud_*` commands were registered fail-closed placeholders that could only return `ERR_CLOUD_NOT_IMPLEMENTED`. The two transfer commands called those placeholders and their TS wrappers had no consumers. Managed Cloud persistence remains on the live shared Web API client boundary in `services/cloudChat.ts`. |
| Legacy native AI project/code commands                | Cut      | Eight uncalled `ai_*` commands duplicated the mounted agent, workspace, and guarded tool paths while keeping project context only in memory. Their two otherwise-unused managed states and web-development mocks were removed with the native module.                                                                    |
| Legacy custom updater command layer                   | Cut      | Five uncalled commands and their unused TS contract duplicated the mounted updater experience. Desktop updates continue through the signed Tauri updater plugin, and version reads continue through `@tauri-apps/api/app`.                                                                                               |
| Orphan native debug/background/GitHub/doctor wrappers | Cut      | Twenty-two uncalled commands owned isolated managed state or renderer mocks but no product entry point. Mounted background agents and Git workflows remain; shared diagnostic checks remain available as Rust internals, while startup recovery retains its separate sanitized diagnostic export.                        |
| Runtime-selected local model list commands            | Keep     | `llm_list_llamacpp_models`, `llm_list_lmstudio_models`, and `llm_list_vllm_models` are selected by provider name at runtime and are explicitly documented in `apps/desktop/wiring-allowlist.json`.                                                                                                                       |
| Remaining temporary Desktop IPC exceptions            | Cut      | The final 52 registrations had no production command-name occurrence. Their native functions remain internal where Rust callers still use them, but they are no longer exported through Tauri; obsolete renderer mocks were removed. No temporary Ticket 1C exception remains.                                           |

The enforcement policy and reviewed exception format are enforced by
`pnpm check:module-reachability` against `apps/desktop/wiring-allowlist.json`.

## 2026-07-30: Enterprise and Gateway Database Boundaries

| Capability                                                | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Enterprise configuration and support schema               | Wire     | Migration `0076_enterprise_control_plane_tables.sql` owns SSO/directory configuration, organization policy, audit, usage-ledger, and support-case relations with RLS. Existing Web/admin and gateway routes retain their bounded configuration/support responsibilities.                                                                                                                                                                                                                   |
| First-party SSO sign-in and SCIM provisioning runtime     | Defer    | Configuration storage is real, but authentication/provisioning enforcement is a Phase 3 enterprise product decision. The Admin readiness UI states this boundary explicitly and does not claim that configuration alone enables SSO or SCIM. The unconsumed directory-sync webhook that returned HTTP 501 after signature verification was removed, along with its unused secret contract; no externally addressable provisioning endpoint is advertised until the runtime is implemented. |
| Gateway chat, pairing, and mobile approval relations      | Wire     | Migration `0077_gateway_compatibility_tables.sql` owns the five pre-existing gateway relation names. Reachable routes now use `getUserScopedClient`; the former privileged compatibility allowlist was deleted.                                                                                                                                                                                                                                                                            |
| One-off schema application scripts and Docker init mounts | Cut      | `pnpm db:migrate` is the sole schema authority. It records exact checksums in `schema_migrations`; CI proves clean apply, no-op reapply, drift-free verify, route/relation ownership, and a two-tenant RLS probe.                                                                                                                                                                                                                                                                          |

## 2026-07-30: CLI Placeholder Surface

| Capability                         | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Current-turn backgrounding command | Cut      | `/background` and `/bg` were advertised in the shared registry and both CLI frontends but only returned an unavailable message. They were removed from the registry, TUI, classic shared handler, tests, and command documentation. Subagent batches remain foreground-only until a durable detached-run contract exists.                                                                                                                                                         |
| Placeholder task lifecycle         | Cut      | The six deferred task-registry tools only created empty output files and accepted caller-authored status transitions; no process supplied stdout, exit status, polling, or termination. The tools, aliases, dispatch, in-memory records, `/tasks` command, renderer, help entry, and snapshots were removed. The real foreground `task` subagent tool and approval-gated synchronous shell tools remain. No background process lifecycle is advertised until one actually exists. |

## 2026-07-30: Desktop Placeholder Surface

| Capability                 | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                  |
| -------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Unread dock/app-icon badge | Cut      | The registered native command only logged its input and returned success, while its shared client wrapper had no callers. Both sides of the IPC contract were removed. The real system tray, window controls, and mounted notification delivery paths remain; no product surface advertises unread badging. |

## 2026-07-30: Mobile Notification Actions

| Capability                   | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                          |
| ---------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Agent-approval review action | Wire     | Background approval alerts now register and use the Expo-safe `agent_approvals` category with an authenticated Review action. The response listener accepts only the declared action or a normal notification tap, preserves the sign-in gate, and opens the existing in-app review surface. Direct lock-screen approval/denial remains prohibited. |

## 2026-07-30: Slack Connector Boundary

| Capability                        | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                     |
| --------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phantom inbound Slack-app runtime | Cut      | Uncalled Socket Mode, slash-command, Block Kit, message-update, reaction, file-upload, user/channel lookup, and live-history helpers were removed with their app-token and signing-secret credential fields. Retained Slack support is outbound bot-token messaging plus the separate MCP OAuth connector; no inbound Slack app is advertised. |

## 2026-07-30: Managed API Key Scopes

| Capability          | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API key scope grant | Wire     | New keys require one or more of `models:read`, `inference:write`, and `usage:read`; the reachable Account settings picker submits and displays those grants. The shared Web auth boundary default-denies API keys on routes without an explicit public-API scope and rejects missing grants. Legacy empty-scope rows retain only the three public-API grants, never general account-route access. |

## 2026-07-30: Sandbox Boundary

| Capability                         | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Unverified outer-isolation profile | Cut      | No production caller establishes or verifies an enclosing container, while the CLI previously treated the profile as permission to run without its own OS sandbox. The runtime and Rust/TypeScript protocol variants were removed. Old configuration text now falls back to workspace-write, removed serialized shapes are rejected, and an unrepresentable legacy split-permission projection errors instead of inventing enforcement. Real managed Seatbelt and bubblewrap paths remain. |

## 2026-07-31: Desktop Code-Egress Boundary

| Capability                             | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Model-authored Desktop code networking | Wire     | The direct command, AGI `CodeExecutor`, and chat `code_execute` now converge on `core/agi/SandboxManager`. Model-owned launches force network access off; macOS applies a Seatbelt network deny profile, Linux applies Bubblewrap `--unshare-net`, and missing/unsupported enforcement refuses the launch. The direct user command alone may request network access through its existing high-risk confirmation. |
| Uncalled Desktop sandbox profiles      | Cut      | `sys/security/sandbox.rs` exposed uninvoked Seatbelt/Bubblewrap/AppContainer profile metadata and all-false feature gates. No runtime caller existed, so the module was deleted instead of retaining a second stub-only sandbox authority.                                                                                                                                                                       |

## 2026-07-30: Team Billing Boundary

| Capability                        | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Licensed-seat billing and pricing | Cut      | Team remains a real sales-assisted entitlement and administration tier, but the repository has no organization-linked licensed quantity, Stripe quantity reconciliation, or member-lifecycle enforcement. Exact seat prices, Team Stripe Price env/config paths, and stale Web billing types were removed; public and upgrade surfaces now say Custom and contracted capacity. The unmounted Desktop SQLite charge estimator and its IPC/client/UI surface were also removed. Its historical table and activity decoders remain read-compatible so existing local data is not destructively rewritten. |

## 2026-07-30: Web Cache Boundary

| Capability                      | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| General multi-backend Web cache | Cut      | The standalone memory/localStorage/IndexedDB cache and its React hook had no live app, test, barrel, or package-entry consumer. Importing the hook would also have constructed all three default caches and cleanup timers, but no shipped bundle reached it. Both files were removed. The separate offline retry queue remains the authoritative localStorage-backed Web persistence path. |

## 2026-07-30: Gateway Approval Routing Boundary

| Capability                      | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Team approval escalation router | Cut      | The in-memory roster and owner/admin/member escalation algorithm had no route, notification, WebSocket, persistence, or package consumer. Its process-local Map could not provide durable or multi-replica-safe ownership, so the module and self-test were removed. The real owner-scoped gateway approval request/resolve and Mobile notification paths remain Neon-backed and do not advertise team escalation. |

## 2026-07-30: MCP Tasks Extension Boundary

| Capability                | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MCP asynchronous task RPC | Cut      | The session-level Tasks methods and wire types were not exposed by the public MCP client, Tauri command layer, renderer, or transport capability handling. No shipped request could call or observe the task lifecycle, so the private methods, types, constants, capability field, and serialization-only tests were removed. Standard MCP tools/resources/prompts and the existing elicitation behavior remain. |

## 2026-07-30: Accessibility Runtime Boundary

| Capability                   | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Orphaned Web a11y utility UI | Cut      | The duplicate standalone utility and unmounted settings panel had no live consumer; the panel's setters were literal no-ops. Both files were removed while the separately used accessible controls and announcement hooks remain.                                                                                                                                                                                                                                                                                                        |
| System high-contrast support | Wire     | Web and Desktop shipped styles honor `prefers-contrast: more` and Windows `forced-colors: active`, with stronger semantic tokens plus OS colors for controls, links, focus, selection, selected states, and gradient-text fallbacks. Mobile reads Android high-text-contrast and iOS darker-system-colors signals, listens for live changes, and applies static high-contrast light/dark palettes across the shared theme while suppressing custom accents that could weaken contrast. Source and behavior tests pin all three surfaces. |

## 2026-07-30: Crash-reporting Runtime Boundary

| Capability                       | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Desktop native crash-dump upload | Cut      | The optional Rust Sentry feature was compiled out of every release, depended on a runtime shell DSN that packaged GUI apps do not reliably inherit, and initialized before renderer-owned consent/trust-boundary state was available. The separate hand-rolled Sentry HTTP sender also bypassed that authority by accepting automatically queued renderer errors. Both native egress paths were removed. Native panics and submitted diagnostics remain in the redacted local log; a future native uploader requires a typed runtime consent bridge, Managed-mode enforcement, packaged configuration, symbol upload, and end-to-end release verification. |
| Product crash/error reporting    | Wire     | Web and Desktop renderer Sentry paths remain real and fail closed: production/configuration checks, explicit consent, PII scrubbing, and Desktop Local/BYOK rejection precede egress. Mobile, Chrome, VS Code, and Desktop native diagnostics are explicitly local-only and do not bundle dormant upload SDKs. This is the supported surface matrix rather than a promise of universal remote collection.                                                                                                                                                                                                                                                  |

## 2026-07-30: Web Code-Execution Result Boundary

| Capability                           | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Provider-managed execution result UI | Wire     | MessageBubble now mounts the existing CodeExecutionBlock for live and persisted provider-managed execution state, including progress, stdout, stderr, exit status, and inline plots. E2B executions retain their canonical tool-timeline and generated-file/artifact presentation rather than duplicating output through this provider-result path. |

## 2026-07-30: Desktop Agent-Task Planning Boundary

| Capability                        | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                      |
| --------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local dynamic workflow planner UI | Wire     | Desktop Local Tasks now mounts the existing AgentTaskPanel. Its auto/sequential/parallel/swarm creator and live monitor reach the native AGI goal commands; swarm execution flows through AgiCore and SwarmOrchestrator into TaskDecomposer's validated dependency graph. Managed Cloud keeps its separate durable account-owned Tasks surface. |

## 2026-07-30: Desktop MCP Bundle Registry Boundary

| Capability                    | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MCP bundle browse and install | Wire     | The mounted Connectors settings owner now exposes MCPWorkspace's Bundle Registry tab. MCPBundleBrowser reaches the native registry/install/update/remove commands through mcpbStore, preserves progress and failure results, and routes completed installs to Configuration. Non-installable entries are manual-only; remote entries cannot self-attest verification; unverified installs require explicit confirmation; installed server configs start disabled. |

## 2026-07-30: Desktop Empty-Chat Entry Points

| Capability                    | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Empty-chat scope and starters | Wire     | DesktopShellV3 supplies EmptyChat with the authoritative folder label/picker and the mode-aware schedule navigator. Scoped greetings name only a real selected workspace; chat starters write editable conversation drafts; recurring-work navigation opens the existing Local or Managed Cloud schedule owner. Shared category chips remain filtered by active runtime capability rather than advertising unavailable modes. |

## 2026-07-30: Desktop Settings Search Boundary

| Capability                         | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Individual-setting search and jump | Wire     | The mounted Desktop settings owner uses a static index of real controls, filters it to currently visible tabs, and groups exact destinations beneath their owning section. Selecting a result opens the authoritative tab and announces the destination; anchored controls are then scrolled into view, focused, and temporarily highlighted. Search results never create settings or advertise unsupported capabilities. |

## 2026-07-30: Desktop Companion Pairing Target Boundary

| Capability                       | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phone QR and manual-code actions | Wire     | The real Desktop-to-Mobile path now names the mounted Mobile destination, supports refresh, high-resolution enlarge, and raw-code copy, and explains the Mobile manual-entry fallback. Production pairing remains authenticated and server-issued; the deterministic UI-development fixture is non-connectable and exists only in the explicit local preview runtime. |
| Computer-to-computer pairing tab | Cut      | The current store and signaling flow own one short-lived mobile companion session with desktop and mobile roles. There is no independently authorized computer peer, target selection, routing, persistent device identity, or revocation lifecycle. A Computer tab would advertise a nonexistent transport, so it remains absent until those contracts exist.        |

## 2026-07-30: Release Accessibility Gates

| Capability                    | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Automated WCAG 2.1 A/AA audit | Wire     | Desktop registers its rendered cloud-sign-in axe audit as a Playwright project and runs it in the path-scoped blocking E2E job. Web builds and starts the production app, audits five public routes in both light and dark schemes, fails closed on navigation failure, an incomplete run, or any axe violation, and uploads the JSON report. CI guardrails pin both workflow calls and the Desktop project mapping so neither audit can silently become orphaned again. |

## 2026-07-30: Web Conversation Branching Boundary

| Capability                                       | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Persisted selected-message conversation branches | Wire     | The live Web transcript mounts the shared BranchNavigator and creates owner-scoped managed-cloud branches through `/api/chat/conversations/[id]/branches`. Creation is CSRF-protected, rate-limited, idempotent, bounded, and transactional: it validates the owned fork point, copies the persisted transcript through that message, and records source/target message ids under forced RLS. Mobile remains separately tracked because it still performs only a whole-thread copy. |

## 2026-07-30: Desktop Workspace Index Boundary

| Capability                             | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Orphaned regex symbol/dependency index | Cut      | Seven registered `workspace_*` commands, two incompatible renderer wrappers, and browser mocks had no runtime caller. The backend recognized only a few Rust/TypeScript declarations, never populated exports, kept unresolved import strings as dependency targets, and treated symbol-name substrings as references. The vertical slice was removed; reachable grep/glob search and the editor LSP path remain unchanged. |

## 2026-07-30: Enterprise Local Policy Runtime Boundary

| Capability                                                       | Decision  | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MDM policy, managed overrides, model restrictions, defaults push | Cut claim | None of these product capabilities has a loader, distribution/subscription path, precedence resolver, or runtime enforcement consumer. The RLS-backed gateway policy route is read-only. The Rust and TypeScript signed-policy implementations remain only as the cross-language contract fixtures accepted by `docs/decisions/2026-07-30-enterprise-local-verifier-retention.md`; inventory records the four absent product capabilities as Missing rather than treating verifier source as an Unwired runtime. |

## 2026-07-30: VS Code Checkpoint Boundary

| Capability                         | Decision  | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Developer-session checkpoint UI    | Cut claim | The authoritative app-server host advertises checkpoints as unavailable and exposes no checkpoint/restore RPC; the extension's command-parity contract forbids checkpoint, worktree, and rewind controls. The protocol field remains validated as part of the complete capability response, while inventory records the absent UI/runtime as Missing.                                 |
| Orphaned patch-batch snapshot undo | Cut       | Batch apply, snapshot storage, and batch undo were exported but had no caller in the extension. They were unrelated to app-server checkpoints and could not justify a checkpoint UI. The dead mutation/restore surface was removed; the mounted `DiffDecorationProvider` review/accept flow, original-context comparison, patch logs, and VS Code's native edit undo behavior remain. |

## 2026-07-30: Beta Feature Preference Boundary

| Capability                         | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| User-controlled beta feature flags | Cut      | Desktop's local rollout/override service and read-only IPC chain had no production consumer, and Web's `beta_features` response field was hardcoded true with no runtime reader. Both phantom paths were removed. Real authenticated entitlement and deployment capabilities remain server-owned (`advanced_model_access`, `code_execution`, and `generic_web_search`) and continue to gate the shipped controls that consume them. |

## 2026-07-30: MCP Apps Host Boundary

| Capability             | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Embedded MCP Apps host | Cut      | Two unreachable Desktop prototypes accepted arbitrary HTML/URLs through a private message format or generated an iframe from a custom component schema. Neither was connected to a production result path or implemented the MCP Apps host contract: tool `_meta.ui.resourceUri`, `ui://` resource retrieval with the registered MIME type, resource CSP/permissions, and the JSON-RPC App bridge. Both prototypes were removed. Reachable MCP server configuration, credential, health, catalog, tool-browsing, and tool-execution paths remain; embedded Apps are Missing until the standard host contract exists end to end. |

## 2026-07-30: Daily-Log Memory Reorganization Boundary

| Capability                          | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Daily-log memory reorganization job | Cut      | The exposed `compact` step only counted logs and always returned zero created memories. A separate command returned an extraction prompt without invoking an LLM, promotion trusted caller-supplied memory objects, and archive was an independent mutation. No scheduler, model-policy gate, transaction, mounted UI, or production caller made those endpoints a job. The false lifecycle and compaction dashboard claims were removed. Daily-log capture/export/cleanup and saved-memory CRUD/search remain, as does the separate live conversation-context compactor in the local turn host. |

## 2026-08-05: Workflow Marketplace Boundary

| Capability                          | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Desktop workflow marketplace        | Cut      | A 5,200-line publish/browse/social/templates subsystem with no user path: `stores/marketplaceStore.ts` was the sole importer of `api/marketplace.ts` and nothing imported the store, and every one of the 36 exported functions and 5 exported types had zero consumers. The Rust half (`sys/commands/marketplace.rs` plus the entire `features/workflows/` module tree) was reachable only from that dead chain. A third, independently dead copy lived in `packages/client/desktop-command-client/src/marketplace.ts`, also with no consumers. All three were removed along with the 36 registrations, their `ALLOWED_GENERIC_COMMANDS` entries, and their renderer mocks. The wired `skillMarketplaceStore` skills/plugins catalog is a separate system and is unaffected. |
| Bundled marketplace success stories | Cut      | The template catalog shipped 60 `success_stories` arrays containing 63 fabricated performance statistics presented as customer outcomes ("reduced bugs in production by 60%", "Closed $2M in pipeline", "Saved $100K"), plus `sample_results` claims of the same kind. None were sourced from telemetry. They were invented product copy and were removed with the module rather than scrubbed in place.                                                                                                                                                                                                                                                                                                                                                                      |

Note on enforcement: these 36 commands passed `check-wiring.mjs` before this cut
because `lib/tauri-mock.ts` and `utils/ipc.ts`, both reachable from
`main.tsx`, named them, which the reachability walk counted as callers. A mock
or an allowlist naming a command can mask it from the orphan check. Prefer
removing the mock entry in the same change that removes a command.

## 2026-08-06 to Wave 2 Orphan Sweep

| Capability                             | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Legacy research fork                   | Cut      | `ResearchPanel.tsx` declared a parallel type set, called `invoke('research_start')` directly instead of going through `stores/researchStore`, and shadowed `ResearchReport`'s name. `SourceCard`, `ResearchProgressPanel`, and `ResearchSourceCard` were unfeedable: the backend emits `research:source_added`/`research:finding_added` only when `request.task_id` is set (`sys/commands/research.rs:296-322`) and the standalone Deep Research page never sets it. The mounted page now renders `ResearchReport` and `ResearchHistory`.                                                                                                       |
| AGI checkpoint command layer           | Cut      | Fourteen commands with no reachable caller. Three separate dead TS clients (`hooks/useCheckpoints.ts`, `api/agi_checkpoint.ts`, `api/autonomousCheckpoint.ts`) plus five wrappers in `desktop-command-client/src/agent.ts`. Five of the fourteen appeared "called" only because `lib/tauri-mock.ts` named them. The live checkpoint path is `stores/codingCheckpointStore.ts` → `coding_checkpoint_*`, which is untouched. **Follow-up (not done here):** `core/agi/checkpoint_store.rs` and `checkpoint_manager.rs` now have no consumer beyond a doc comment and a re-export; they were left in place rather than silently widening this cut. |
| Desktop visual editor and live preview | Cut      | `features/editing/{VisualEditor,LivePreview}.tsx` had zero importers including their barrel. The mounted Canvas feature owns diff UI and never reused them. Neither contained an `invoke()`, so no Rust went dead. `EnhancedDiffViewer`, `FileTreeWithChanges`, `ChangeSummary`, and `ConflictResolver` remain exported.                                                                                                                                                                                                                                                                                                                        |
| Desktop experimental surface           | Cut      | The whole `features/experimental/` directory (6 components) was unreferenced. `KnowledgeBaseViewer` was its only `invoke()` caller.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Duplicate computer-use views           | Cut      | `features/computer-use/{ScreenPreview,ActionLog}.tsx` were subsumed by `ExecutionSidecarScreenView` (which merges computer-use and browser screenshots with source priority) and the type-agnostic `ExecutionSidecarApprovals`.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Web reasoning accordion                | Cut      | `ReasoningAccordion.tsx` was an orphaned duplicate of the live `ThinkingBlock`, carrying its own test suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Web credit alert modal                 | Cut      | `CreditAlertModal.tsx` was imported only by a test. Three reachable warnings already cover the case (`ChatComposerNew.tsx` trial banner, `Usage.tsx` approaching-limit line, and `UsageWarningBanner` above the composer), and the modal required `remainingCents`/`allocatedCents` that no API returns. The no-top-ups/contact-sales product rule it asserted stays covered by `UpgradePlanDialog.test.tsx` and `marketing-copy-regression.test.ts`.                                                                                                                                                                                           |
| Web advanced-mode toggle               | Cut      | `AdvancedModeToggle.tsx` had no importer outside its own test; the `advancedMode`/`advancedModelId` store fields it wrote have no reader. Live model selection is `shared/stores/model-store.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Web connector card                     | Cut      | `features/connectors/components/ConnectorCard.tsx` had zero importers and carried a third copy of `formatRelativeTime` (the live ones are in `ConnectorsPage.tsx` and `shared/utils/format.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                |

The desktop reachability allowlist ratcheted from 90 entries to 68 across this
sweep. Every removal was forced by the check itself, which fails a stale entry.

## 2026-08-06 to Wave 2 Wire Decisions (Desktop)

| Capability                       | Decision               | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Deep Research report and history | Wire                   | `DeepResearchPage` showed only a summary and key findings; the report body, its citations, copy, and PDF export existed in `ResearchReport.tsx` taking exactly the store's `ResearchResponse` and had no importer. Both it and `ResearchHistory` (search, delete-one, clear-all, detail dialog) are now mounted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Execution sidecar                | Wire                   | `ExecutionSidecar` takes no props and self-gates on `isOpen`, but `open()` had NO caller anywhere, so a running agent produced no visual feedback at all. Mounted in `DesktopShellV3` and given an auto-open in `useExecutionSidecarContext` driven by pending approvals, active computer use, browser streaming, or a running tool stream, respecting the store's existing `userClosedThisSession` flag.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Dangerous-tool approvals         | Wire                   | `tool_executor/mod.rs` emitted `approval:request` fire-and-forget and then returned a failed `ToolResult` immediately: the call was already refused before the user saw the prompt, and answering it hit "Approval not pending" because nothing had registered with `ApprovalController`. It now calls `ApprovalController::request_approval` (managed Tauri state, reachable from the app handle it already holds), awaits the decision with a 300s timeout, and proceeds to execute on approval. Fails closed when the controller is unmanaged. Rendering is owned by `ExecutionSidecarApprovals`; `McpToolConfirmationPrompt` keeps `mcp_tool` so one decision never shows two Approve buttons.                                                                                                                                                                                                                                                                |
| Desktop project RAG embeddings   | **Demoted, not wired** | The plan scored this a one-line constructor swap (`RAGEngine::new` -> `with_embeddings`). It is not. (1) `ProjectManager`, the struct the plan named, has zero production consumers; only its own test constructs it. (2) The real call sites are `sys/commands/knowledge.rs:194,231`, but `projectSearchKnowledge`/`projectAddKnowledgeFile` have no consumer either, so the whole chain is unreachable and wiring it would improve nothing a user can reach. (3) Worse, it would introduce a silent bug: the hash fallback is **384-dim** and the configured embedding route is **768-dim**, and the fallback is decided per call, so an Ollama outage between indexing and querying yields dimension-mismatched vectors and `cosine_similarity` returns 0 for every chunk, i.e. search silently returns nothing. Any real implementation must persist the embedding model/dimension per chunk and compare like with like. Recorded here rather than half-done. |

Follow-up surfaced by the above: `core/agi/checkpoint_store.rs` and
`checkpoint_manager.rs` now have no consumer beyond a doc comment and a
re-export, and `features/projects/manager.rs` (`ProjectManager`) is constructed
only by its own test. Both were left in place rather than widening the sweep.

## 2026-08-06 to Wave 2 Web Lane

| Capability                     | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Settings Help section          | Wire     | Settings had no `help` nav key, so `/help`, `/status`, `/changelog`, `/docs`, `/support`, and `/legal` all shipped but were unreachable from inside the product. New `HelpSection` links all six; every destination is an existing route.                                                                                                                                                  |
| Conversation-level fork        | Wire     | The branch API, its hook, and per-message branching were already live end to end (`use-conversation-branches.ts` mounted at `WebChatPage.tsx:908`); only the conversation-level entry point was missing. `ConversationTitleMenu` gains "Duplicate as branch", branching from the last message.                                                                                             |
| Web appearance settings        | Cut      | `AppearanceSettings.tsx` had zero importers and drove `web-settings-store.theme`, which nothing else reads or writes, the live theme control is `GeneralSection` via `next-themes`. Mounting it would have shipped a SECOND, disconnected theme control. Its `chatFontSize` had no CSS hook at all, so the font-size buttons changed nothing outside the component's own preview swatches. |
| Dead web-settings store fields | Cut      | `defaultModel`, `defaultModelTier`, `advancedMode`, `advancedModelId`, `chatFontSize` and their setters had zero readers AND zero writers outside the store. Live model selection is `shared/stores/model-store.ts`, consumed at `ChatComposerNew.tsx:615-620`. A settings row writing these would have been cosmetically wired and functionally inert.                                    |

**Deferred, recorded rather than half-done:** a real font-size preference needs
a CSS hook that does not exist (no variable, no class application), that is a
feature build, not a wiring task. `TokenUsageDisplay` likewise stays deferred:
no producer sets `WebChatMessageMetadata.cost`/`tokensUsed`, so mounting it
would render an always-empty widget. Wiring provider usage through
`useChatStream`'s completion path is the prerequisite.

## 2026-08-06 to Wave 3 (partials completed)

| Capability                      | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sales tax / VAT / GST           | Wire     | `api/checkout/route.ts` is the ONLY Checkout-Session creation site and collected no tax, while `/terms` asserted tax obligations to the customer. Now sets `automatic_tax`, `tax_id_collection` (so EU/UK B2B reverse-charge works), and the `customer_update.address` Stripe requires when automatic tax runs against an existing customer. Regression test added.                                                                                                     |
| Styles and response length sync | Wire     | `style-store` was `localStorage`-only, so a custom writing style did not follow the user to another device and was destroyed by clearing site data. Now dual-writes to the existing `user_settings` JSONB namespace (`response-style`) via `savePreferenceNamespace`, debounced, errors swallowed; localStorage stays as an offline-first cache. No migration needed.                                                                                                   |
| Pre-send secret warning         | Wire     | `lib/security/secret-patterns` was wired only into the support-handoff transcript, so pasting an API key straight into chat produced no signal. `ChatComposerNew.handleSubmit` now runs the synchronous non-throwing `containsSecrets` on the assembled outgoing text and surfaces a one-shot warning through the composer's existing `localNotice` banner. Deliberately non-blocking: refusing to send someone's own message is a worse failure than warning about it. |
| Desktop memory export           | Wire     | `memoryStore.exportAll` existed but its only UI was `MemoryManager` behind `showImportExport`, whose sole caller `MemoryBrowserModal` was never mounted, so device memories could not be exported at all. The live Memory settings tab now offers a JSON export in Local scope.                                                                                                                                                                                         |
| Desktop cloud Help              | Wire     | Adding `help` to `SETTINGS_NAV_GROUPS_WEB` was caught by the desktop `capability honesty` test, which asserts every nav item renders content. The desktop cloud shell is the same Managed Cloud plane, so it gets the same destinations, opened externally via the existing `openExternalUrl` guard.                                                                                                                                                                    |

## 2026-08-06 to Wave 2 late web items

| Capability                     | Decision           | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------ | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| In-conversation search         | Wire               | `MessageSearch` was complete but unexported, unmounted, and unbound; the browser's own Cmd+F ran against a VIRTUALIZED transcript and could only match rows currently in the DOM. `ChatMessageList` now owns Cmd+F, computes matches across all groups, and scrolls to each via the `listApiRef.scrollToRow` it already used for autoscroll. Per-match text highlighting inside the bubble is NOT included, it needs a new prop threaded through the markdown renderer and is tracked separately.         |
| Composer budget indicator      | Wire               | `BudgetTrackerDisplay` was a padded card, `ComposerFooter`'s inline mode is a one-line flex row, so it was guarded behind `!inline`, and the only production mount always passes `inline`. It never rendered for a single user. Added a `compact` pill variant and rendered it in the inline row, with a test that locks in the variant that actually ships.                                                                                                                                              |
| Per-tool connector permissions | Wire (GitHub only) | The disconnect copy already told users "the per-tool permissions you saved are deleted" while no UI existed to save any. Mounted for GitHub, whose `CONNECTOR_TOOLS` entries are real wire names mirroring `GITHUB_TOOL_DEFS`. Every other connector's list is capability prose with no backing tool, and real tools are discovered at runtime by `catalogToConnectorToolDefs`, a static config cannot mirror them, so the general case stays deferred behind a live per-connector tool-catalog endpoint. |

## 2026-08-06: Standing gates

The wire-or-cut default only holds if something mechanical enforces it. Three
gates were added or extended so the UNWIRED and stale-claim classes cannot
silently regrow.

| Gate                                    | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `check-module-reachability`, TypeScript | **New, enforcing.** The check covered Rust only, which is exactly why the UNWIRED class accumulated almost entirely in TypeScript. It now walks the desktop renderer's real import graph from `main.tsx`, reusing the existing `scripts/lib/module-graph.mjs` walker rather than adding a second one. First run found **276 unreachable modules**, seeded as a ratcheting `knownUnreachable` baseline: a NEW orphan fails, and a baseline entry that stops reproducing fails as stale, so wiring or cutting a module forces its line out in the same commit. Both directions verified by deliberately reintroducing each violation. **The 276 are debt to drain, not approvals.** |
| `check-agent-context`, evidence paths   | **New, enforcing.** `workspaceUnits[].evidence` had no existence guard, so the model-registry unit cited a deleted test file for weeks with nothing failing. Matches only concrete paths (slash-bearing, no spaces, no glob characters) because `evidence` legitimately also holds shell commands and globs, a false positive there would push authors to delete real evidence to get green.                                                                                                                                                                                                                                                                                      |
| `reference-integrity`                   | **Was already failing** before this work (9 undeclared references). Now green, with the ratcheting debt list driven from 228 down to 224.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `knip`                                  | **Configured, deliberately NOT a gate.** First run reports 760 unused files, which is mostly entry-point config tuning (React `lazy()`, Next.js route co-location, adapter-driven panels), not real debt. See `knip.README.md` for the count and the three steps to make it enforcing. A required gate failing with hundreds of untriaged findings gets skipped, and a skipped gate looks like coverage that is not there.                                                                                                                                                                                                                                                        |

## 2026-08-06 to Wave 3 (second batch)

| Capability                       | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Desktop temporary chats          | Wire     | `is_temporary` was declared on the wire type, honored by the create route, and purged by the retention cron, but desktop passed a hardcoded `false` at both construction sites, so the capability existed everywhere except a control that could turn it on. Threaded through `ensureCloudConversation`, added a `temporaryChat` preference, and put a toggle in the composer's host controls (Managed Cloud only). The preference is read at CREATION time so toggling it later cannot retroactively change an existing conversation's retention. The settings store is reached by lazy import: a static one drags the whole settings graph into `CloudRuntime` and breaks consumers that mock only its own collaborators. |
| Browser content prompt-injection | Wire     | `PromptInjectionDetector` existed but was applied only to the computer-use screenshot/OCR path; every `browser_get_*` command handed raw page text to the model with no marker and no scan. `browser_get_text`, `browser_get_content`, `browser_get_dom_snapshot`, and `browser_query_all` now wrap results in an explicit untrusted-data fence and prefix a warning on a pattern match, mirroring what the Chrome extension already did in `cdpDriver.ts`. Content is never silently dropped: a false positive must not blind the agent to a legitimate page.                                                                                                                                                              |

## 2026-08-06 to Wave 2/3 final items

| Capability                     | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Edit existing spreadsheet      | Wire     | `ExcelEditor` reads the source workbook through calamine and rewrites it with edits applied, so existing sheets and cells survive. Exposed as `document_edit_excel` across the command layer, the agent tool catalog, the chat tool dispatcher, and `tool_guard` (RiskLevel::High, always requires approval, validates BOTH the source and destination paths). Allowlisted as agent-selected: the LLM tool executor calls the Rust function directly, so there is no renderer invoke by design.                                                                                                                                                                                                                                                                                                                                                                                                          |
| Edit existing Word document    | **Cut**  | `WordEditor::edit_document` CANNOT parse an existing .docx, docx_rs is write-only, and the function's own `tracing::warn!` says the source "content is not preserved". It builds a new document from the edits alone. Exposing it as "edit your document" would silently destroy the user's file, so it is deliberately NOT registered. The 2.7 plan item assumed both editors were equivalent; they are not. Delete the module or replace docx_rs before revisiting.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Computer-use confirmation wait | Wire     | Superseded 2026-09-05. The pause is no longer owned by the loop's own `is_paused` bool and no longer fails closed. A step the safety layer flags now parks on the channel every other desktop tool confirmation parks on (`ToolConfirmationState::await_confirmation`), rendered on the voice consent dialog through the shared `ToolApprovalRequest` rather than the tool dialog, so one decision keeps one pair of answers. Approval resumes the same step through the same driver, a denial ends the task as `confirmation_denied`, and an unanswered request expires at `CONFIRMATION_TIMEOUT_SECS` (120s) as `confirmation_timed_out`, never as a run. The grant rule is the channel's: `NEVER_REMEMBERABLE` tools always ask, every other one may be remembered for the session only. `ComputerUseTask::require_confirmation`, which nothing ever set, is deleted rather than left as a dead gate. |
| Share-link lifetime            | Wire     | Expiry was hardcoded to 7 days with no caller control. `POST /api/share` now accepts a bounded `expires_in_days` (1, 7, or 30; default 7). Bounded by an enum rather than a free number so a caller cannot mint a link that effectively never expires.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Web artifact version restore   | Wire     | Version browsing was read-only on web while desktop already had rollback. `restoreArtifactVersion` re-upserts the chosen version's content through the shared engine, which APPENDS it as the new latest, versions are content-keyed, so history is preserved rather than rewound. Goes through the shared store directly because its artifact type is wider than web's `ArtifactInput` and narrowing would drop kinds like `component`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

**Remaining deferred, with the blocking reason:** per-message cost and latency
(3.2) needs provider usage plumbed through `useChatStream`'s stream-completion
path into `MessageMetadata` before any widget can render it, no producer sets
`cost`/`tokensUsed` today, so mounting a display first would ship an
always-empty control. That plumbing is a feature, not a wire.

## 2026-08-06: Interactive cards, slice 2

| Capability              | Decision  | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `clarify.v1` renderer   | Wire      | The decoder, persistence, and fallback shipped in slice 1 with a deliberately empty registry, so every clarify card rendered as a flat block of preformatted text with its options invisible. `features/chat/components/messages/cards/ClarifyCard.tsx` renders the questions and options as real UI, keyed by stable `questionId`/`optionId` (labels are server-resolved and never travel inbound), with read-only rendering for the `answered`, `dismissed`, and `expired` states and no masked input for `isSecret`. |
| `itinerary.v1` renderer | Not built | Still in the kind allowlist with no renderer and no producer, so it continues to render the authored fallback. Registering an entry with no producer would be untested code pretending to be shipped, the same reason slice 1 shipped the registry empty.                                                                                                                                                                                                                                                               |

**Remaining for a complete clarify loop:** the card renders read-only today
because `InteractiveCardBlock` passes `ctx: { canRespond: false }` and no server
endpoint accepts an `InteractiveCardResponsePayload`. Answering requires a route
that resolves option ids to labels server-side and satisfies the
exact-set-equality invariant in `claimCloudAgentApprovalCheckpoint`, plus a
producer tool (`ask_clarifying_questions`) that suspends the turn. Until that
lands the card is strictly better than the fallback, the user can read the
questions and options, but cannot be answered in place.

## 2026-08-06: Per-message usage

| Capability             | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Per-message token/cost | Wire     | Sourced from the PERSISTED `messages.input_tokens`/`output_tokens` row via `toChatMessage`, not from a new stream frame. A terminal `x_usage` SSE frame was built first and reverted: it broke `stream-transform.byte-parity.test.ts`, and the break exposed a real accounting divergence between the two response builders (see `known-flaws.md` WEB-STREAM-USAGE-PARITY-01). The persisted row carries the settled numbers, survives reload, and touches no wire contract. `TokenUsageDisplay` is mounted with real data. |

## 2026-08-06: Trust, legal, and safety surfaces

| Capability             | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Copyright / IP notices | Wire     | `/copyright` shipped and linked from the footer and the legal index. `CONTACT_SUBJECTS.ipComplaint` already existed as a routing subject with no page pointing at it, while the product hosts user uploads and publishes artifacts at shareable URLs, so there was no stated notice-and-takedown route.                                                                                                                                                                 |
| Model licences         | Wire     | `/model-licenses` is GENERATED from `packages/contracts/types/src/models.json`, the same registry the router and pricing read, so it cannot drift from what actually runs. Models whose `license` is absent render as "not recorded in the registry" rather than a guessed licence, naming one we cannot prove would be a false statement on a legal page.                                                                                                              |
| Report a response      | Wire     | The durable sink (`public.content_reports`, migration 0093) and its handler already existed but ONLY at `/api/mobile/content-report`, so web could not report a harmful answer at all, its only routes out were a general feedback link and a refusal APPEAL, which is the opposite complaint. Added `/api/content-report` as an ALIAS re-exporting the same handler (one schema, one rate limit, one idempotency key) plus a per-message action on assistant messages. |

## 2026-08-06: Conversation printing

| Capability           | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Print a conversation | Wire     | No print action and no print stylesheet existed. The browser's own Ctrl+P was unusable for two reasons, the second serious: it printed the app chrome, and the transcript is VIRTUALIZED, only the rows currently in the DOM exist, so a long conversation printed as a few pages with the middle silently missing. A partial printout that LOOKS complete is the real failure. `lib/print-conversation.ts` scopes the document via `data-print-scope`, expands the list before printing, and clears the scope on `afterprint` AND on a timer, because `afterprint` does not fire in every browser and a stuck attribute would hide the sidebar on screen. Print styles force ink-on-paper (a printed dark theme is unreadable and wastes toner), avoid breaking messages across pages, and expand link hrefs. |

## 2026-08-06: Project knowledge duplicate detection

| Capability          | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Duplicate detection | Wire     | `checksum_sha256` was computed by the client, stored on the row, and passed to the extractor, but never COMPARED, so re-uploading the same file created a second unrelated row: it consumed one of the project's 20 file slots, doubled the text stuffed into the model's context, and made the file list confusing. Now probed before extraction (so a duplicate never pays for that work) and scoped to non-deleted rows (so re-adding a file the user deliberately removed still works). The conflict is thrown OUTSIDE the try so the schema-not-ready branch can never swallow it. Two existing assertions were rewritten from query-COUNT to intent ("nothing was inserted"), because a count assertion breaks every time the route gains a legitimate read. |

## 2026-08-06: Project knowledge storage quota

| Capability              | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Aggregate storage quota | Wire     | A per-file byte cap and a 20-files-per-PROJECT count cap existed, but nothing bounded TOTAL bytes, so a user held unbounded storage simply by spreading large files across projects. `knowledgeStorageBytes` joins the plan catalog (free 100 MB, basic 1 GB, pro 10 GB, team 25 GB; Max uncapped, Enterprise negotiated, local-only/BYOK uncapped because they store nothing on the platform), enforced in the knowledge-file upload path by summing across ALL of the user's projects, a per-project quota would have the same hole it is fixing. |

## 2026-08-06: Project knowledge file version history

| Capability           | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| File version history | Wire     | Migration `0098_project_knowledge_versions.sql` adds `version`, `supersedes_id`, `superseded_at`. Re-uploading a CORRECTED file previously created an unrelated row: the old version stayed active, so the model saw both the stale and the corrected text, the 20-file budget was consumed twice, and nothing said which was current. Same name + different checksum is now an edit that supersedes; same name + same checksum is the duplicate case rejected earlier. `superseded_at` is deliberately NOT `deleted_at`, conflating "replaced" with "user deleted it" would make restoring a version impossible. The previous version is retired only AFTER the replacement row exists, so a failed insert can never leave a project with no active copy. Every "currently in effect" query was scoped: the list, the 20-file count, the duplicate probe, the storage-quota sum, and, most importantly, `project-context-service`, which is what actually feeds the model. |

Note on the migration-ordering guard: `shared-project-knowledge-read-only-migration.test.ts`
asserted that 0090 is the highest-numbered migration touching
`project_knowledge_files` at all, while its own stated rationale is narrower.
"a later migration reinstating a FOR ALL policy would silently undo this". It
was tightened to assert exactly that, so schema evolution is possible without
either renumbering behind an applied migration or deleting a security guard.
Verified in both directions: a probe migration creating a `for all` policy on
that table still fails it.

## 2026-08-06: Jupyter notebook uploads

| Capability       | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.ipynb` uploads | Wire     | Notebooks could not be uploaded at all. Added `application/x-ipynb+json` to the attachment allowlist with filename inference, and, the part that matters, a dedicated extractor rather than routing them through the plain-text path. A `.ipynb` is JSON, so the text path would have "worked" while feeding the model the notebook's ENTIRE serialized form: base64 PNG outputs, execution counts, kernel metadata, per-cell ids. On a notebook with a few plots that is megabytes of noise that crowds out the analysis and burns the project's context budget. The extractor keeps markdown prose, source, TEXT outputs, and error output (frequently the most informative cell), preserves cell order because a notebook's meaning is sequential, and drops image blobs. |

## 2026-08-06: Project export

| Capability     | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Project export | Wire     | `GET /api/projects/[id]/export` returns a versioned, self-contained JSON snapshot as a download. Only an account-wide GDPR export existed, which is the wrong granularity for moving one project, archiving finished work, or handing a client their material. Deliberately OWNER-ONLY, narrower than `GET` on the project itself: that route also serves org-shared readers, but an export is a bulk extraction of every knowledge file's text, and sharing grants reading in place rather than exfiltration. Includes extracted text (a manifest of filenames the recipient cannot read is not an export), excludes superseded and deleted rows (the export mirrors the project as it stands, not its edit history), and degrades to project-without-files on a pre-migration deployment rather than failing. |

## 2026-08-06: Project duplication

| Capability        | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Duplicate project | Wire     | `POST /api/projects/[id]/duplicate`. Branching a project previously meant recreating its instructions by hand and re-uploading every knowledge file, which is also how the same bytes end up stored twice. Copies settings, instructions, and knowledge files BY REFERENCE to the same storage object (already content-addressed by checksum; re-uploading would double storage for identical content). Does NOT copy conversations: a conversation records something that happened rather than configuration, so copying would fabricate history the user never had, and it is the part of a duplicate nobody can undo by hand. The insert routes through the SAME `assert_user_resource_limit` guard as create, a duplicate that bypassed the project quota would be a trivial way around a paid limit. Owner-only, matching export. Files are copied after the project exists so a quota rejection leaves nothing behind, and a failed file copy still returns the usable project rather than rolling back a successful create. |

## 2026-08-06: Controls that persisted nothing anyone read

Three controls shipped in production that a user could see, and in two cases
operate, while nothing downstream consumed the value. Each is CUT rather than
implemented, because in every case building the missing half would have meant
inventing a policy nobody approved.

| Capability               | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Data retention (days)    | Cut      | `Settings → System` rendered a 1-365 day input that validated and persisted, while `retention_period` had ZERO consumers anywhere in `apps/web/app/api`. Nothing purged anything. A user who set 30 days believed their conversations were being deleted; they were not, which makes this a false privacy claim rather than a missing feature, strictly worse than having no control. Implementing it instead would have meant silently deleting user data on a retention schedule nobody signed off, the same reasoning `0095_published_artifacts.sql:11-20` already records for artifact expiry. The zod schema entry is retained so values stored by the old control still validate; only the input is gone. Re-add when a purge path AND an approved policy both exist. |
| Per-project memory scope | Cut      | `ProjectSettingsDialog` rendered a `<select>` with exactly one option ("Default"), no `onChange`, no state, and no persistence, a dropdown the user could open but never change. It implied per-project memory scoping that exists nowhere: `user_memories` (migration `0010_memory.sql`) has no project column, and `managed-memory-context-service.ts` selects purely by user. The explanatory sentence beneath it is KEPT because it is true and is what a user opening the dialog needs to know: memory is account-wide in both directions.                                                                                                                                                                                                                             |
| `voice` preference       | Cut      | The server-synced `general` preference namespace stored `voice: 'nova'`, an OpenAI TTS voice name, and loaded it into `GeneralSection` state on every mount, where nothing rendered or read it. Web read-aloud runs entirely on the browser's `SpeechSynthesis` engine, which has never heard of that voice set. Replaced by a REAL control (`ReadAloudVoiceRow`) built on `speechSynthesis.getVoices()`, deliberately stored device-locally rather than in the synced namespace: installed voices are a property of the OS and browser, not the account, so a synced `voiceURI` resolves to nothing on the next machine and reads as the setting being ignored. Any `voice` key still stored on an account is ignored.                                                     |

## 2026-08-06: Connector expiry visibility

| Capability                     | Decision | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Expired-connector notification | Wire     | `GET /api/connectors` has always returned `needsReauthorization` per row, and `use-connectors.ts:250` has always computed the set, but only `ConnectorsPage` read it. A connector's OAuth grant could expire and the only way to find out was to open that one settings page and scroll to the right row; everywhere else the connector simply stopped working with no explanation. `SettingsModal` gained a surface-owned `navBadges` prop (the shared package renders the badge but never decides what warrants one, so no web policy leaks into the shell Desktop also renders) and `WebSettingsModal` feeds it from the field it was already fetching and discarding. The badge carries a full sentence for assistive tech and the tooltip, a bare "2" says nothing about what needs attention. |

## 2026-08-06: Conversation recovery, camera capture, chat defaults

| Capability                                                           | Decision      | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Restore deleted conversations                                        | Wire          | `DELETE /api/chat/conversations/[id]` only sets `deleted_at`, and there is no conversation equivalent of `cron/purge-deleted-media`, nothing ever purges those rows. Deleted conversations were therefore the worst of both: permanently unreachable (every read filtered `deleted_at is null`) while their messages sat in the table indefinitely. Added a `deleted=only` list filter, `POST /api/chat/conversations/[id]/restore`, and a Settings → Privacy → "Recently deleted" section. Restore deliberately does NOT touch `archived`/`pinned`/`starred` or bump `updated_at`: an archived-then-deleted conversation returns to being archived, and an old conversation returns to its place in history rather than jumping to the top as if it had new activity. The copy makes NO retention promise, because no purge job exists to enforce one, that would repeat the unenforced-retention claim removed from Settings → System the same day. |
| Duplicate conversation                                               | Already ships | The conversation-level "Duplicate as branch" item in `ConversationTitleMenu` forks from the LAST message, and `forkConversation` copies every message up to and including the fork point into a new conversation, which is a complete duplicate. A separate "duplicate" path would be a second implementation of the same behaviour.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Camera capture                                                       | Wire          | `CameraCaptureDialog` in the composer's attachment menu. Deliberately NOT gated on the desktop-only `canTakeScreenshot` capability: `getUserMedia` works in every supported browser, and the camera is the one attachment source a laptop and a phone both have. The preview is required rather than decorative, unlike the screenshot path, where the browser's own picker shows what will be captured, `getUserMedia` returns a live stream with no UI, so grabbing a frame directly would turn the camera light on and attach a photo the user never saw. The stream is stopped on unmount, on cancel, and when it resolves after the dialog has already closed. The preview is mirrored; the SAVED frame is not, because flipping it would reverse any text in shot.                                                                                                                                                                              |
| Default model, reasoning effort, text size, code wrapping, shortcuts | Wire          | Settings → General. Default model writes `model-store.ts` and reasoning effort writes `thinking-store.ts`, the stores the composer actually reads, rather than the dead `web-settings-store.defaultModel`/`defaultModelTier` fields a previous attempt targeted. Text size and code wrapping ship WITH their stylesheet hook (`html[data-chat-text-size]`, `html[data-code-block-wrap]` in `globals.css`, stamped by `AppearancePreferences`); the earlier `chatFontSize` field was cut precisely because no CSS rule existed behind it.                                                                                                                                                                                                                                                                                                                                                                                                              |
| Keyboard shortcut documentation                                      | Wire          | Three parallel lists existed: the `handleKeyDown` if-chain (what fires), a `shortcuts` array returned from `useKeyboardShortcuts` that no caller read, and a four-entry array in `WebChatPage` that WAS the one the dialog rendered. They had drifted, Escape, Cmd+Shift+C and Cmd+Shift+R all worked and none of them appeared in the shortcuts dialog. Consolidated onto `KEYBOARD_SHORTCUT_DOCS`, now rendered by both the dialog and Settings, with a test pinning docs against bindings in both directions.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Sensitive-data memory exclusions                                     | Wire          | Enforced in `persistManagedAutoMemoryFacts` on the WRITE path, before any candidate reaches `user_memories`, filtering in the UI would leave the fact stored and merely hidden, telling the user it was excluded while it sat in the database and kept being fed to the model. Plain case-insensitive substrings, not regexes: a user-supplied regex is a footgun (`.*` silently excludes everything) and a ReDoS surface on a per-turn server path. Deliberately NOT added to `CLOUD_SAFE_SETTINGS_NAMESPACES`: Desktop's local memory is a different store with a different write path where this filter does not run, so syncing the list would put the terms on a surface that does not honour them. The UI states explicitly that it governs new memories only.                                                                                                                                                                                  |

## 2026-08-06: Project templates

| Capability        | Decision             | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Project templates | Wire (built-in only) | `features/projects/data/project-templates.ts` + a picker in `CreateProjectDialog`. Deliberately NO user-defined template CRUD: the product already has that feature under another name, `POST /api/projects/[id]/duplicate` copies a project's instructions, settings, and knowledge files while leaving conversations behind, which is exactly what "save this project as a template" would do. A second subsystem storing name + instructions would be a parallel implementation with its own table and its own drift. What duplicate CANNOT serve is the empty state, a first project, where the user faces a blank Instructions box and no idea what belongs in it, and that is the gap these fill. Templates only pre-fill fields the create form already sends, so a template can never produce a project the user could not have typed by hand, and the name field is never overwritten once the user has typed into it. |

## 2026-08-06: Developer API: structured outputs and embeddings

| Capability                                      | Decision            | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `response_format: json_object`                  | Wire                | Previously refused, honestly, because the field was validated and read nowhere, a caller could ask for JSON, get 200 OK and prose, and fail in their own parser with nothing pointing at the cause. Now REAL in both halves: `applyJsonObjectMode` appends a directive, and `extractJsonObject` (`lib/json-object-mode.ts`) parses the completion in the non-streaming response path before it is returned, so a 200 means the body genuinely parses as a JSON object. The extractor unwraps code fences and surrounding prose but deliberately NEVER repairs malformed JSON, guessing at a missing brace would hand the caller a document the model did not produce. A completion that cannot be parsed returns 502 with an actionable message rather than prose.                                                                                                                                                                                                                                       |
| `response_format: json_object` + `stream: true` | Cut (refused)       | A stream hands the caller bytes as they arrive, so by the time the payload could be validated it has already been delivered; buffering the whole response to check it would make `stream: true` a lie. Refused at the schema with a message naming the fix (`stream: false`), which is the only option that does not silently break one promise to keep the other.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `response_format: json_schema`                  | Cut (still refused) | Enforcing a caller-supplied schema needs either native per-provider support, which differs in shape and coverage across the providers this gateway routes to, or a validate-and-retry loop spending the caller's money on retries they did not request. The refusal names both alternatives (`json_object`, or `tools` + `tool_choice`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `POST /api/llm/v1/embeddings`                   | Wire                | The catalog already carried an embedding model with no route to reach it. `/v1/models` correctly did NOT advertise it, so this adds a capability rather than repairing a false claim. OpenAI-compatible shape, and NARROWER than OpenAI where it must be: `dimensions` and `encoding_format: 'base64'` are rejected via `.strict()` rather than accepted and ignored, because returning full-length float vectors to a caller who asked for 256 dimensions or base64 surfaces only later as degraded retrieval or a broken decoder. Goes through the same reserve → call → settle billing path as image generation rather than a lighter one, requires `Idempotency-Key`, and settles on EVERY exit including provider failure and a missing provider key, an unfinalized reservation holds quota the caller can neither use nor see. A ragged or short provider batch is rejected outright: misaligned vectors would attach embeddings to the wrong input text, and nothing downstream can detect that. |

## 2026-08-06: Notifications: the first send paths

`NotificationsSection.tsx:22-31` recorded that every push and email toggle had
been removed because "no email sender, no push dispatcher, and no
agent-task-finished listener exists at all", and instructed: "Re-add a group
once its underlying send path actually exists." Both send paths now exist, so
two groups are re-added, and only two.

| Capability                                  | Decision             | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Mobile push delivery                        | Wire                 | `lib/services/push-notification-service.ts`, the repo's first push SEND path. `POST /api/mobile/push-token` had been storing `mobile_devices.push_token` all along with NOTHING reading the column: tokens collected, never used. Sends via Expo in batches of 100, and clears tokens Expo reports as `DeviceNotRegistered`, left in place, a token from an uninstalled app is retried on every future notification forever. The device ROW is kept, because it still records a device the user owns and deleting it would lose the name and platform shown in settings. Never throws: a scheduled task must not be reported failed because a push provider had a bad minute. Token shape is validated locally, so a corrupted column cannot become an outbound request.                                                                                                          |
| Notification email                          | Wire (one channel)   | `lib/services/notification-email-service.ts` on the proven Resend primitive. The transport was EXTRACTED (`sendTransactionalEmail`) rather than copied, two divergent senders is the same drift that produced two secret-pattern lists and three keyboard-shortcut lists in this repo, and the copy always falls behind on retries or timeouts without anyone noticing until mail stops. The 7 existing support-email tests pass unchanged against the extraction. Uses `AGI_NOTIFICATIONS_FROM_EMAIL`, deliberately NOT the support address: a product notification arriving from the support mailbox trains users to reply where nobody reads, and mixes deliverability reputation between a watched address and an unwatched one.                                                                                                                                              |
| Schedule-completion notifications           | Wire                 | `lib/services/schedule-notification-service.ts`, called from `processClaimedScheduleRun` AFTER `finalizeScheduleRun` commits, a notification inside that transaction would hold the connection across a network call, and a rollback would leave the user notified about work that was never recorded. Chosen as the first consumer because it is the one event that genuinely happens while the user is not looking. OPT-IN per channel, failing CLOSED on a settings outage: notifying someone who never enabled it is the one mistake that cannot be taken back. The two channels are independent, a flaky push provider must not cost the user their email. A `cancelled` run sends nothing, because the user stopped it themselves. No task OUTPUT is included in either channel: notifications render on lock screens and email is neither access-controlled nor revocable. |
| Usage, budget, billing, and security emails | Deferred, scoped out | The backlog row listed six email channels and flagged that "billing and security emails carry compliance stakes, scope per channel". A billing email is a financial notice with retention and disclosure obligations; a security email is the channel an attacker most wants to suppress or spoof. Neither should be added as a by-product of wiring schedule notifications. The transport and the opt-in pattern they will need are now in place.                                                                                                                                                                                                                                                                                                                                                                                                                                |

## 2026-08-27: Declined P1 UI gaps, re-triaged against code

`audit/ui-gaps.csv` reported zero unresolved P1 gaps while 72 P1 rows sat at
`Not Planned`, each declined with a reason of the shape "X is declined
without/until Y". `unresolvedSeverityTally` treats `Not Planned` as resolved, so
the tracker read clean while the work was open. Ten agents re-read every row
against current code, with an adversarial pass over a cross-batch sample that
overturned three calls (`GAP-097`, `GAP-102`, `GAP-053`).

| Capability                                 | Decision    | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 53 declines reversed to `Open`             | Wire        | Each named blocker is a buildable shared contract, now recorded in the row's `owner` column so the decline is traceable to the thing that unblocks it. 22 of the 53 sit behind six primitives: `account-and-session-service` (5), `browser-runtime-owner` (4), `hook-and-mcp-reach` (4), `plugin-registry-hosting` (3), `recording-timeline-asset` (3), `native-desktop-lifecycle` (3). The rest are one or two rows each.      |
| 6 rows already built                       | Done        | `GAP-053` Desktop ships a full API-keys section with scope picker, one-time reveal and per-key revoke (`CloudAccountControls.tsx:240-320`); `GAP-073`, `GAP-074`, `GAP-085`, `GAP-103`, `GAP-117` likewise. These were declined for want of a service that has since shipped, and the ledger was never revisited.                                                                                                               |
| 13 declines upheld                         | Decline     | Genuine product decisions, not missing contracts. The canonical case is `GAP-032`: a model-training opt-in is refused because customer content is never used for training (`apps/web/app/privacy/page.tsx:133,447`), so the switch would imply a data path that does not exist. `GAP-019` likewise, Mobile is deliberately not the policy authority; Desktop enforces the approval floor.                                       |
| `GAP-102` plugin disable switch            | Cut or wire | Overturned from "already built" on reachability. `enabledPlugins` appears in exactly five places, all inside the component that draws the switch (`SkillsPluginsSettings.tsx:138,434,437,488,696`); no runtime reads it. A toggle that persists nothing is a UI with no runtime behind it, wire it to `plugin-enablement-enforcement` or remove the control.                                                                    |
| P1 ratchet                                 | Tightened   | `audit/ui-gaps-baseline.json` `maxUnresolved.P1` lowered 124 → 53, so `pnpm check:ui-gaps:monotonic` can only fall from here. The former 124 allowed 71 rows of silent regression.                                                                                                                                                                                                                                              |
| `subagents` capability cell                | Re-scored   | Previously scored a gap on Desktop and defended as "local-surface by construction". Both halves fail. Desktop is SHIPPED and reachable from mounted UI, `AgentTaskCreator.tsx:97,132` → `AgentTaskPanel.tsx:101` over commands registered at `lib.rs:2385-2388`. The "cannot move to a server" rationale is falsified in `tool-loop.ts:68-74`, which already imports the E2B executors. Web/Chrome/Mobile remain an honest gap. |
| `task` tool on the VS Code app-server      | Wire        | `extension-vscode/src/integrations/localRuntimeClient.ts:740-743` already spawns the CLI runtime that owns `task`, and the extension has a real local workspace, but `apps/cli/src/app_server.rs` exposes no task/subagent wiring. One hop from working; scheduled as a Tier A wiring gap rather than a build.                                                                                                                  |
| Swarm completion telemetry                 | Harden      | `apps/desktop/src-tauri/src/core/swarm/agent_spawner.rs:592-604` awaits `core.submit_goal(goal)`, receives a goal id, and immediately reports `{"status":"completed"}`. The `succeeded` and `speedup_ratio` figures surfaced by `AgentTaskMonitor` therefore count submissions, not finished work. Scheduled with Tier A.                                                                                                       |
| Git-worktree isolation for swarm subagents | Dormant     | `create_sandbox(use_git_worktree)` exists (`core/agi/sandbox.rs:689`, worktree setup at `:736-753`) but every caller passes `false` (`executor.rs:816`, `sandbox.rs:718`), and `agent_spawner.rs` never touches `SandboxManager`. No swarm subagent currently receives worktree isolation; do not cite it as an active safeguard.                                                                                               |
| Mobile `computer-use` cell                 | Re-scored   | Not "dead", deliberately disabled. `AddToChatSheet.tsx:726-737` renders the row with a lock icon and `status="Desktop"`, gated by a compile-time `FEATURES.computerUse: false` (`lib/v1FeatureFlags.ts:34`), and no native automation code exists anywhere in `apps/mobile/src`. There is no broken call path, because there is no call path.                                                                                   |
| `routes` cell                              | Cut (N/A)   | Scored as a Chrome UI-only stub. No such surface exists under either reading: the extension ships two HTML documents total (`apps/extension/manifest.json:77,80`) and every `publish` symbol in it is internal pub/sub. The cell should be N/A, not present-but-unwired.                                                                                                                                                        |

## 2026-09-05: Billing boundary audit, P0 slice G

| Capability                  | Decision       | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `organization_usage_ledger` | Cut (deferred) | `0076_enterprise_control_plane_tables.sql:149-166` carries schema, RLS and a trigger for a margin ledger (`provider_cost_usd`, `charged_amount_usd`, `gross_margin_usd`, `gross_margin_pct`) that duplicates `provider_cost_events`, the COGS ledger `recordSettledProviderCost` actually writes. `organization-usage-service.ts:1-21` documents in its own header that nothing writes to `organization_usage_ledger`, only account and organization erasure reference it. A dashboard built against it would report zero forever while looking authoritative. Decision: drop the table in a future migration numbered after 0167 (no data, no readers except erasure) and remove the erasure references at the same time; founder-gated like every migration above 0158. Not touched in this slice. |

## 2026-09-05: Agent runtime audit, P0 slice J

| Capability                                                | Decision      | Evidence and retained path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/tools/browser-tool` (D-P5-1)                    | Wire (staged) | Zero call sites: `isRunnableComputerAction` (`src/index.ts:344`) reads like the shared computer-use safety gate but no live path reaches it. The two wired drivers are the Chrome extension `cdpDriver.ts` and the desktop `BrowserExecutor`, each with its own allowlist and consent gate, and approval for a live call is decided by `resolveToolCallGate` in the web tool loop. Decision: the package becomes the shared action contract both drivers import (action schema, approval status, destination policy), no driver code in it; that is P5 scope. Until it lands, the README carries a "Not wired" section so a reader cannot mistake its policy for the enforced one. Not deleted: the contract has a named owner and a scheduled slice. |
| Six tool-call shapes, four approval vocabularies (D-P0-5) | Wire          | The tool primitive now exists once, in `crates/agiworkforce-protocol/src/tool_primitive.rs`, generated into `packages/contracts/types/src/generated/protocol` and surfaced as `@agiworkforce/types` `tool-primitive`. Web declares its action classes and destructiveness through it, desktop maps `RiskLevel`, `ToolSafetyTier` and `NEVER_REMEMBERABLE` onto it, the CLI maps its registry `read_only` flag and `ToolResult`, and `execpolicy::Decision` maps to the verdict in the crate that can see both. No executor changed behaviour in this slice; per-surface adoption of the definition, approval and audit records follows.                                                                                                               |

# docs/standards

Rules the code follows: naming, service-layer boundaries, API conventions,
accessibility, privacy, and documentation standards.

A standard worth never violating should also exist as a guard in `scripts/`.
Prose alone does not hold.

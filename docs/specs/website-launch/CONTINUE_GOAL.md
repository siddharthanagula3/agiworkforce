Continue the authorized localhost website launch goal in AGENT_GOAL.md. Read the current
PROGRESS.md checkpoint and relevant LAUNCH_PLAN.md, QA_ISSUES.md and QA_COVERAGE.json entries
under docs/specs/website-launch. Do not reload the whole repository or restart the audit.

If authorized actionable local work remains, perform the next highest-impact coherent batch:
inspect relevant evidence, record the failure/target, implement, run scoped checks and Browser
verification, update the existing records, then select the next task. Security/data integrity
and broken essential journeys lead; meaningful speed, usefulness and recovery fixes follow.
A patch, test batch, worker completion or progress report does not complete the goal.

Reuse unchanged passing evidence. Recheck failures, newly reachable assertions and evidence
invalidated by relevant changes only. A blocked task does not stop independent work. Production
work belongs in PRODUCTION_REMAINING.md and cannot keep this local run looping.

Honor new user instructions, pause, cancellation, authentication handoff, finite run limits and
context boundaries. Update the run controller before stopping for those conditions. Never reset
limits or create replacement runs without explicit owner authorization. Do not push, merge,
deploy, migrate production, broaden into native apps or silently substitute the selected model.
The hook does not grant permissions or prove correctness.

Before context pressure compromises work, save exact changed paths, verification, reusable
passes, blockers and next action in PROGRESS.md. Restore these pointers after compaction.
Completion requires scope accounting and current functional evidence, not a phrase or done flag.
Local verification is not production approval; preserve the historical GLOBAL NO-GO.

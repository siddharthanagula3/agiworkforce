// screens-c-cap.jsx — DEPRECATED in v1 (cloud-only feature)
// v1 = local-only, no caps, no usage quotas. Performance page moved to Section K.
// Keeping renderSectionC as a stub so app.jsx wiring continues working.
function renderSectionC() {
  return null;
}
Object.assign(window, { renderSectionC });
